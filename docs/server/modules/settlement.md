# settlement — 브랜드 매출 정산

- **모듈 경로**: `src/modules/settlement/`
- **주 클라이언트**: `klow_brand`(정산 탭 — 발송 이후 송장 원장·현장결제 라인·EFS 청구서), `klow_admin`(정산 처리 — 월별 후보 집계·월별 롤업·정산 확정).
- **데이터 모델**: 정산은 **송장(`Shipment`) 단위**로 마킹한다. 정산 상태는 `Shipment.settledAt`(정산 시각)·`Shipment.settledById`(처리한 어드민) 두 컬럼으로 표현하고, 금액은 **제품 정산가(`Shipment.items[].orderItem.settlementPriceKrw × quantity`) + 고객이 결제한 배송비의 브랜드 몫**을 합산해 산출한다(아래 배송비 이관 항목). 별도 `Settlement` 테이블 없이 송장에 직접 마킹하는 구조. 정산 단위 키는 `(orderId, brandId, shipmentId)` — 한 브랜드 = 한 송장이므로 송장 1장이 곧 정산 1건.
- **현장결제(onsite)는 Order 단위 정산**: `Order.channel='onsite'` 주문은 배송이 없어 `Shipment` 자체가 없으므로, 정산 상태를 `Order.settledAt`/`settledById`/`settledMonth`(KST `YYYY-MM` 라벨) 에 직접 마킹한다. 브랜드 귀속은 `OrderItem.productId → Product.brandId`(OrderItem 에 product 관계가 없어 별도 조회) 로 풀고, 멀티브랜드 주문이어도 자기 브랜드 라인만 합산한다. 월 버킷 기준은 `Order.paidAt` 이고, 2026-09 부터는 **송장 축도 같은 기준**이라 두 축이 통일됐다(아래 인식 시점 항목). 브랜드 정산탭이 송장 라인과 현장결제 라인을 같은 카드로 렌더하도록 서버가 `SettlementLineDTO`(`kind:'shipment'|'onsite'`, `settlementKrw`, `status`, `activityIso` 등)로 통일해 내려준다.
- **라인의 제품별 내역(`items`)**: 카드는 접힌 상태에서 `itemsLabel`(`"A 외 2건"`) 한 줄만 보이고, 2건 이상이면 펼쳐서 제품명 × 수량 × 정산액(`SettlementLineItemDTO`)을 보여준다. ⚠️ **2026-09-14 부터 브랜드 화면은 이 원화 금액을 그리지 않는다**(고객 결제 USD 만 보여준다) — 예전엔 접힌 상태에서 금액 아래 `제품 ₩X · 배송 ₩Y` 보조줄이 있었다. 아래 데이터 규칙은 그대로다. ⚠️ 배송비는 이 배열에 **넣지 않는다** — 제품별 내역이고 `itemsLabel` 이 **행 수로 "외 N건"을 세기 때문에** 한 행이 끼면 라벨이 어긋난다(브랜드 `settlementItems` / 어드민 `shipmentDisplayLines` 양쪽). 불변식은 **`Σitems == settlementKrw − shippingKrw`** 다(제품 성분은 뺄셈으로 파생한다 — 필드를 하나 더 두면 "제품 성분을 구하는 법"이 화면마다 갈린다). 송장 라인은 `GET /delivered` 응답의 `items[].orderItem`(`SHIPMENT_INCLUDE`)에 이미 전량 실려 있어 **클라가 그대로 펼친다**(서버 무변경). 현장결제만 `orderToOnsiteLine` 이 조회해 둔 필드를 라벨로 뭉개고 있어 `SettlementLineDTO.items` 로 함께 내려준다 — 항목 정산액은 `settlementPriceKrw × quantity` 라 **합이 `settlementKrw` 와 일치**한다. ⚠️ 이 배열은 조회 `where` 에서 이미 자기 브랜드 제품만 걸러진 것이라 멀티브랜드 현장 주문에서도 남의 제품이 새지 않는다.
- **정산 대상 필터 (settleable)**: **고객이 1원이라도 낸 주문**이 대상이다 — 무가 시딩(브랜드가 배송비를 내고 고객 결제액이 0)만 제외하고, 브랜드가 물품가를 청구한 시딩(`OrderItem.settlementPriceKrw > 0`)과 **물품가 없이 배송비만 받은 고객결제 시딩(`Order.shippingFeeUsd > 0`)** 은 **포함**한다. 또한 `paymentStatus=paid` + `status != 'cancelled'` 를 강제한다 — 환불은 `Order` 만 `refunded` 로 전이시켜 `Shipment` 쪽 상태가 그대로 남고, 시딩 송장 취소는 `Order.status='cancelled'` 로만 가면서 `paymentStatus` 를 `paid` 로 남기기 때문이다. 여기서 빼 브랜드 과지급을 막는다. 필터 정의는 이렇게 공유한다:
  - `SETTLEABLE_ORDER_WHERE = { paymentStatus: 'paid', status: { not: 'cancelled' }, OR: [{ isSeeding: false }, { items: { some: { settlementPriceKrw: { gt: 0 } } } }, { shippingFeeUsd: { gt: 0 } }] }` — Order 직접 필터. ⚠️⚠️ **세 번째 항을 빼면 안 된다** — 브랜드가 발급 시 "물품 가격"을 비워 둔 고객결제 시딩(전액 배송비, 프로덕션 실측 21건)이 정산 화면에서 **통째로 사라진다**. 산식만 바꾸고 이 항을 안 넣으면 "금액은 맞는데 행이 없는" 조용한 실패가 되고, 어드민 주문 목록이 `전액 배송비` 경고로 표시하던 건들이 정확히 이 케이스다. 무가 시딩은 `totalUsd`·`shippingFeeUsd` 가 둘 다 0 이라 계속 제외된다.
  - `settleableShipmentWhere(range?)` — Shipment 필터 **팩토리**. `{ status: { not: 'cancelled' }, order: { ...SETTLEABLE_ORDER_WHERE, paidAt: { gte, lt } } }` 를 만든다. ⚠️ 상수를 스프레드로 합치지 않고 팩토리로 둔 이유는 `{ ...상수, order: { paidAt } }` 가 `order` 키를 통째로 덮어 settleable 게이트를 지워 버리기 때문이다(embed 모듈이 `PUBLIC_PRODUCT_WHERE` 를 스프레드로 합쳤다가 겪은 것과 같은 사고 유형). ⚠️ 송장 조건이 `status: 'submitted'` 가 아니라 `{ not: 'cancelled' }` 인 것은, 발급 실패(`failed`)나 고아(`pending`) 송장에 묶인 매출이 조용히 사라지지 않게 하기 위해서다. `(orderId, brandId)` 당 취소 아닌 송장은 정확히 1장이라(재발급이 구 송장을 `cancelled` 로 돌린다) 이중 라인은 생기지 않는다.
  - 현장결제 축은 **팩토리 + 상수 3벌**이다(Shipment 가 없어 Order 로 판정): `ONSITE_ACTIVE_WHERE`(= `channel:'onsite'` + `paymentStatus:'paid'` + `status != 'cancelled'`) / `onsiteSettleableWhere(brand?)`(= ACTIVE + `settledAt: null`, `brand` 를 주면 `items.some(brandOwnedItemWhere)`) / `ONSITE_LEDGER_WHERE`(원장 — 아래 항목). ⚠️ 예전엔 `status: { not: 'cancelled' }` 가 **아예 없어서** 현장 축만 "취소됐지만 paid" 주문을 조용히 정산 대상에 넣고 있었다(송장 축엔 있었다 — 두 축이 "취소란 무엇인가"에 다른 답을 냈다). ⚠️ 상수가 아니라 팩토리인 이유는 브랜드 스코핑(`items`)을 팩토리가 소유해야 하기 때문이다 — `{ ...상수, items: {...} }` 로 쓰면 상수 쪽에 `items` 가 생기는 날 조용히 덮인다(`settleableShipmentWhere` 와 같은 이유).
  - **모든 read(목록·후보·월별 롤업) 와 실제 정산 write(settle) 가 같은 정의를 강제**한다. settle 의 `updateMany.where` 에도 같은 팩토리를 넣어, 시딩 송장이 후보에 안 떠도 마킹 단계에서 한 번 더 막아 ₩0 정산 누수를 차단한다.
- **⚠️⚠️ 고객이 낸 배송비가 브랜드 정산에 포함된다** *(2026-09 전환)*: 고객이 체크아웃·시딩 신청에서 내는 배송비는 **KLOW 요율표**(`SeedingRate` 500g 티어)가 정한 값이지 EFS 실비가 아니다. 예전엔 KLOW 가 그 선결제를 보유하고 [efs-billing](./efs-billing.md) 청구서에서 브랜드 부담을 그만큼 깎아 줬는데(`max(0, 실비 + 수수료 − 선결제)`), 지금은 **선결제 전액을 브랜드 정산으로 지급하고 EFS 실비 + 수수료를 전액 청구**한다. 요율표와 실비의 차액은 브랜드 손익이다.
  - **산식**: `settlementShippingKrw = floor(perBrandShareUsd(order, brandId, brandCount) × fxRateSnapshot × 0.95)`. 제품 정산가와 **같은 기준**(주문 시점 환율 · PG 5% 차감 · floor)이다 — 한 송장 안에서 두 성분의 수수료 기준이 다르면 브랜드가 자기 정산액을 설명할 수 없다. 단일 출처는 `shipments/shipment-settlement.ts` 의 `brandShippingSettlementKrw`.
  - ⚠️ **`perBrandShareUsd`(`pricing/chargeable-brands.ts`)의 소비처가 바뀌었다.** 그 함수는 여전히 **EFS 송장 27번 필드**에 박히는 값이므로 시그니처·동작을 건드리면 안 된다. 바뀐 건 두 번째 소비자가 "청구서의 선결제 차감"에서 "정산 지급액"으로 옮겨간 것뿐이다. **양쪽에서 동시에 읽으면 같은 배송비를 주고 + 깎아 주는 이중 반영**이 된다.
  - ⚠️ legacy 분모(`orderBrandCount`, 스냅샷 없는 주문의 균등분배)는 efs-billing 에서 `pricing/chargeable-brands.ts` 로 **이관**했다(짝인 `perBrandShareUsd` 와 같은 파일). **시딩 주문은 `shippingFeeByBrand` 스냅샷을 절대 남기지 않으므로 항상 이 폴백 경로**를 타는데, 시딩 `OrderItem` 이 `productId: null` + `brandId` 스냅샷이라 분모가 정확히 1 이 된다 — `brandId` 가지를 빠뜨린 분모 구현을 쓰면 시딩 배송비가 통째로 0 이 된다.
  - ⚠️ **`fxRateSnapshot` 이 없으면 배송비만 0 으로 두고 라인은 살린다**(`settlementShippingUnknown: true` → 어드민 amber 경고). efs-billing 의 '청구 보류'와 **정반대 방향이 맞다**: 저쪽은 모르면 안 받는 게 안전하지만, 이쪽은 모르면 안 주는 게 위험하다. 제품 정산가는 이미 동결된 KRW 라 fx 와 무관한데 라인을 빼면 멀쩡한 제품 정산금까지 화면에서 사라진다. **라이브 환율 폴백도 금지** — 지급 근거가 조회 시점마다 달라진다.
  - ⚠️⚠️ 금액은 **송장 단위로 계산하고 `(orderId, brandId)` 중복 제거를 하지 않는다.** (문서가 한동안 `loadShippingSettlements` 의 `claimed` Set 을 설명했는데 그 방어는 제거됐다 — `take/skip` 페이징과 발송대기/배송중 분리 쿼리를 가로지르면 뚫리면서 **같은 송장의 금액이 조회 문맥에 따라 달라졌다**.) 정본은 데이터 불변식이다: **`(orderId, brandId)` 당 `Shipment` 는 정확히 1 row** — `OrderItem.shipmentItem` 이 to-one 이고 재발급(`cancelAndReissue` → `cancelEfsShipmentOnly` → `retry`)이 **같은 row 를 pending 으로 리셋해 재사용**한다(`groupExistingShipment` / `isReissuableStatus`). 못 믿게 되는 날의 올바른 자리는 앱이 아니라 DB partial unique index 다.
  - ⚠️ **서버가 계산해 파생 필드로 내려준다** — 송장 축은 raw Prisma 행을 반환하므로 `settlementShippingKrw`/`settlementShippingUnknown` 을 map 으로 얹는다. 클라 미러(`klow_admin/lib/api/shipments.ts`, `klow_brand/lib/api-types.ts`)에 **재료(`order.shippingFeeUsd`/`shippingFeeByBrand`)를 선언하지 말 것** — 선언하는 순간 두 프론트가 legacy 균등분배·fx 폴백 규칙을 각자 베끼고, 세 벌이 되면 반드시 갈린다. 그래서 `SHIPMENT_INCLUDE` 도 넓히지 않고 정산 전용 `SETTLEMENT_SHIPMENT_INCLUDE` 를 따로 뒀다.
  - ⚠️ **배송현황 화면도 같은 값을 본다** — `shipments.service` 의 `listPendingForBrand`/`listConfirmedForBrand` 도 같은 헬퍼를 타므로 klow_brand `baseView` 와 정산탭이 같은 값을 본다. ⚠️ **2026-09-14 부터 두 화면 모두 그 금액을 그리지 않는다** — `settlementSum` 은 `freeSeeding`(무료 시딩 배지) 판정과 월 요약의 입금 상태 배지 재료로만 남았다. 한쪽만 제품가로 되돌리면 같은 송장이 두 화면에서 다른 판정을 받는다.
  - **현장결제(onsite)는 무변경** — `Order.shippingFeeUsd` 가 구조적으로 0 이다(부스 직접 전달).
  - ⚠️ **주문 목록의 합계와 정산액은 같은 수가 아니다.** 주문 목록은 `Order.totalUsd`(USD 센트,
    PG 청구액 그대로)이고 정산액은 그걸 주문 시점 환율로 환산하고 **PG 5% 를 뺀 KRW** 다
    (`정산 ≈ totalUsd × fx × 0.95`, 성분별 floor 라 원 단위 오차). 멀티브랜드 주문은 거기서 다시
    **그 브랜드 몫만** 잡히므로 주문 합계와 대조하면 안 된다. `SettlementLineDTO.totalUsd` 에
    `// USD 센트 (표시용 — KRW 와 합산 금지)` 주석이 붙어 있는 이유다.
  - ⚠️ 브랜드 **배송현황** 카드는 시딩을 무조건 "무료 시딩"으로 표시하면 안 된다 — 고객 결제
    시딩은 그 배송비가 정산액이 되므로, 실제로 정산금을 받는 건을 브랜드가 포기한 걸로 읽는다.
    판정은 `isSeeding` 이 아니라 **`freeSeeding`(= `isSeeding && 정산액 === 0`)** 이다
    (`klow_brand/src/components/shipping/shipping.model.ts` 의 `baseView`).
  - **마이그레이션·백필 없음.** 정산이 금액 스냅샷을 남기지 않아(`settle()` 은 `settledAt` 만 찍는다) 산식 변경이 **전 기간에 소급**된다 — 정산 실행 이력이 0건이라 안전하다.
  - 회귀 잠금: `settlement/__tests__/settlement-recognition.spec.ts`(배송비 가산·PG 5%·물품가 0 시딩 편입·무가 시딩 제외 유지·fx 결손 폴백·legacy 균등분배).
- **⚠️⚠️ 현장결제 주문은 단일 브랜드여야 한다** *(2026-09-04)*: `Order.settledAt` 은 **주문당 하나**라, 한 주문에 두 브랜드가 섞이면 한쪽을 정산 처리하는 순간 **남의 브랜드 라인까지 '입금 완료'** 가 된다(돈은 못 받았는데 미정산 표에서 사라지고 되돌릴 UI 도 없다). 스키마 주석이 "부스=단일 브랜드"를 전제로 적어 뒀지만 **강제하는 코드가 어디에도 없었다** — 카트는 브랜드관을 가로질러 localStorage 에 남고 klow_web 현장 체크아웃이 **카트 전체**를 보내므로, A 브랜드관에서 담아 둔 채 B 부스 QR 을 찍으면 실제로 섞였다(재현 확인).
  - **서버 게이트(정합성의 근거)**: `orders/onsite-brand.ts` 의 `onsiteMultiBrandError(products)` — `createOnsite` 가 가격 계산·`itemsCreate` 조립 **전**에 400 으로 거절한다. ⚠️ `brandId` 가 null 인 legacy 제품은 **별도 버킷**으로 센다(묶으면 legacy 와 정상 브랜드가 섞인 주문이 통과한다 — fail-closed).
  - **`settle()` 브랜드 스코핑**: 현장 분기에 `brandId` 스코핑이 **아예 없었다**(송장 분기엔 처음부터 있었다). 이제 `items: { some: brandOwnedItemWhere(brandId, productIds) }` 를 건다 — `orders/item-brands.ts` 의 "이 라인은 어느 브랜드 것인가" 단일 규칙을 재사용한다(현장 `OrderItem` 은 항상 `productId` 가 있고 `brandId` 는 비므로 그 헬퍼의 두 번째 가지는 **증명 가능하게 공집합**이다). `productIds` 는 트랜잭션 **밖**에서 미리 조회해 락 구간을 늘리지 않는다.
  - ⚠️⚠️ **`some` 이지 `every` 가 아니다.** 읽기(브랜드 상세의 onsite 조회)가 `some` 그레인인데 쓰기만 `every`("남의 브랜드 라인이 하나도 없음")로 좁히면, 레거시 혼합 주문이 후보 표에 떠서 **기본 전체 선택 → count 불일치 → 그 브랜드 그 달 정산이 통째로 400** 이 된다. 읽기를 같이 좁히면 이번엔 돈이 화면에서 사라진다. **레거시 혼합 주문은 기존 동작을 유지하고 신규 유입만 막는다** — 회귀 스펙이 그 기대를 명시적으로 잠가 둬서, 라인 단위 정산으로 옮길 때 *의도적으로* 뒤집게 된다.
  - **klow_web 쪽**(부스 앞에서 그 400 을 만나지 않게): `BrandStorefront` 의 현장 모드 동기화 effect 가 **모드가 실제로 전이할 때만** 타 브랜드 라인을 걷어내고(`otherBrandProductIds`) 남은 라인을 `mode` 를 실어 리프라이싱한다(`repriceCartForCountry(country, qc, 'onsite')`). ⚠️ **카트를 통째로 비우지 않는다** — `cart-reprice.ts` 헤더가 적어 둔 판단대로 손님 눈앞에서 카트가 날아가는 건 "국가 바뀜"보다 "결제 직전"에 가깝다. ⚠️ 전이 판정은 **`useOnsiteStore.getState()` 의 이전 값**으로 한다 — effect deps 에 `brand`(useQuery 객체)가 있어 refetch 마다 재실행되므로 `useRef(false)` 로 시드하면 **부스에서 방금 담은 것을 눈앞에서 지운다**(sessionStorage 영속이라 재마운트에서도 진짜 값이 복원된다).
  - 같은 수정이 **표시가 ≠ 청구가**도 덮는다: `createOnsite` 는 모든 아이템을 `onsitePriceLine` 으로 재계산하는데 카트 라인은 담을 때 가격을 스냅샷하므로, 진입 전에 담은 라인은 소매가를 보여주고 현장가로 청구됐다(반대 방향도 같다). `api.products.detail`·`qk.productDetail` 에 `mode` 슬롯이 **이미 있어** 신규 API 표면이 0 이다.
  - **상시 가드**: `klow_web/src/lib/cart.ts` 의 `brandRemainingCapacity` 가 "현장이면 `Infinity`" 에서 **"현장이고 카트에 다른 브랜드가 있으면 0"** 으로 바뀌었다(시그니처 무변경 → 호출부 4곳이 그대로 수렴). 현장 모드로 `/shop` 에 나가 다른 브랜드를 담는 경로를 브랜드관 effect 만으로는 못 막기 때문이다. ⚠️ **0 을 돌려주면 `addToCart` 가 조용한 no-op** 이 되므로(피드백은 호출부 책임) PDP 버튼 라벨과 그리드 하트 토스트에 사유를 반드시 붙인다 — 현장 사유는 '5개 한도'가 아니라 `brand.onsiteOneBrand` 다.
  - **마이그레이션·백필 없음.** ⚠️ 이 수정은 **신규 유입만 막는다** — 이미 섞인 주문은 못 고치므로 배포 전에 조회해 수기 확인한다.
  - 회귀 잠금: `orders/__tests__/onsite-brand.spec.ts`(legacy 버킷 분리 포함) + `settlement-recognition.spec.ts` 의 *settle() 현장결제 브랜드 스코핑* describe.
- **⚠️⚠️ 정산 완료 라인은 환불·취소 후에도 원장에 남는다** *(2026-09-04)*: `SETTLEABLE_ORDER_WHERE` 가 `paymentStatus: 'paid'` **정확 일치** + `status != 'cancelled'` 라, 환불(두 축을 동시에 깬다)이 나면 그 라인이 **어드민 원장·브랜드 정산탭 양쪽에서 흔적 없이 사라졌다.** `settledAt` 을 되돌리는 코드는 세 레포에 0건이라 DB 엔 settled 로 남는데 화면엔 없는 상태가 됐고, `SettledLedger` 가 막으려던 상황("어드민이 지급 근거를 잃는다")이 **가장 필요한 순간(환불 분쟁)에** 재현됐다. 브랜드 쪽에선 그 달 '받을 금액'과 '입금 완료' 배지가 **사후에 조용히 줄어** 통장 기록과 어긋났다.
  - **규칙**: *정산이 완료된 라인은 이후의 환불·송장취소와 무관하게 원장에 영구히 남는다. **미정산 상태에서 환불된 건은 남기지 않는다*** (지급한 적이 없으므로 기록할 것이 없고, 남기면 어드민 후보 표에 떠서 기본 선택되어 settle 이 400 전량 롤백한다). 운영 정책이 "정산 완료 후 환불돼도 브랜드에 클로백을 요청하지 않는다"이므로 **그 돈은 실제로 브랜드에게 갔고**, 화면에서 지우면 브랜드의 통장 기록과 어긋난다.
  - **where 는 합성 팩토리**다: `settlementLedgerShipmentWhere(range) = { OR: [ settleableShipmentWhere(range), settledVoidedShipmentWhere(range) ] }`. ⚠️ 별개 상수가 아니라 **기존 팩토리의 출력을 문자 그대로 포함**하므로 `원장 ⊇ 정산대상` 이 구성상 참이고 두 정의가 드리프트하는 것이 표현 불가능하다. `OR` 로 합쳐 스프레드가 아니라서 `order` 키 덮어쓰기 사고도 원천 차단된다. `settledVoidedShipmentWhere = { settledAt: { not: null }, ...VOIDED_SHIPMENT_WHERE, order: { paidAt: range } }` — ⚠️ **두 번째 가지에도 월 범위를 반드시 건다**(안 걸면 다른 달의 지급 건이 샌다). 현장 축 짝은 `ONSITE_LEDGER_WHERE` / `ONSITE_VOIDED_WHERE`.
  - ⚠️⚠️ **`settle()` 은 절대 원장을 쓰면 안 된다** — 쓰는 순간 환불된 건을 다시 정산할 수 있다. 그래서 이름을 나눠 넓어졌는지가 grep 한 번에 보이게 했고, 회귀 스펙이 **캡처한 write where 로 픽스처를 직접 걸러** 환불·취소 건이 0 임을 단언한다(조건을 손으로 다시 적지 않는다). 미정산 후보(`listAdminCandidates`)도 그대로다.
  - **`voided` 판정의 단일 출처**는 `shipments/shipment-settlement.ts` 의 `isVoidedShipment`(JS 술어) ↔ `VOIDED_SHIPMENT_WHERE`(Prisma where) **한 쌍**이다(`item-brands.ts` 관례 — 같은 파일에 나란히 둬야 갈릴 수 없다). 축은 셋: `Shipment.status='cancelled'` ∨ `Order.status='cancelled'` ∨ `Order.paymentStatus ∈ {refunded, cancelled}`. ⚠️ 클라가 `order.paymentStatus === 'refunded'` 만 보면 **시딩 송장 취소를 놓친다**(`performEfsCancel` 은 `Order.status` 만 바꾸고 `paymentStatus` 는 `paid` 로 남긴다) — 그래서 서버가 파생 필드 `voided` 로 내려주고 `attachShippingSettlement` 가 함께 얹는다.
  - ⚠️⚠️ **`SettlementLineDTO.status` 에 `'refunded'` 를 넣지 않는다.** 그 값은 어드민 후보 표가 지급 대상을 고르는 **선택 축**이라(`status !== 'settled'` 로 걸러 **기본 전체 선택**), 환불을 섞으면 정산완료+환불 라인이 자동 선택되어 **settle 이 400 전량 롤백**한다. 무효화는 **직교 축 `voided`**(표시 전용)로 내린다 — 덕분에 어드민 필터·기본 선택 로직을 한 줄도 안 고쳤고 배포 순서 제약도 없다. 같은 이유로 klow_brand 도 **정산 완료가 환불을 이긴다**(`settled` 우선 + `voided` 배지).
  - **월별 롤업은 이상치를 별도 축으로 낸다**: `listAdminMonthly` 의 **메인 쿼리는 그대로 두고** voided-settled 전용 두 번째 쿼리를 붙여 `voidedSettledKrw`/`voidedCount` 를 낸다. ⚠️ 메인을 원장으로 넓히면 `receivableKrw`/`settledKrw` 에 환불분이 섞여 **`settledKrw ≤ receivableKrw`** 와 **`unsettledCount === 0 ⇒ 그 달 마감`** 두 판정이 흐려진다(그 둘이 세 화면의 대사 기준이다). `shippingUnknownCount`·`belowCostItemCount` 와 **정확히 같은 관례**다. `lineCount` 에는 더한다(어드민 상세 헤더가 원장 행 수를 그대로 세므로 — 예전 `56 vs 39` 가 어긋난 그 자리). 두 쿼리는 조건상 **서로소**라 이중 계상이 불가능하다(main 의 각 conjunct 를 voided 의 disjunct 가 부정한다).
  - **이미 정산된 송장의 취소·재발급은 막지 않는다.** 재발급이 같은 row 를 재사용해 `settledAt` 이 살아남으므로 **이중 지급 위험이 구조적으로 없고**, 막으면 그 경로의 존재 이유(EFS `ChangeCnee` 의 city 가 varchar(10) 이라 미국 "City, State" 를 못 담아 취소+재발급으로 처리)를 못 쓴다. **환불 경로에 `settledAt` 가드도 걸지 않는다** — `settledAt` 은 "돈을 줬다"는 사실이고 환불이 그 사실을 되돌리지 않는다. 되돌리는 write 를 만들면 "지급했다가 취소한 이력"이 사라져 원장이 다시 거짓말을 한다.
  - **마이그레이션·백필 없음** · 라우트 수 불변 · cron 불변.
- **⚠️ 인식 시점 = 결제완료(`Order.paidAt`)** *(2026-09 전환)*: 예전에는 `Shipment.latestStatusCode === '33'`(EFS 배송완료)인 송장만 정산 후보가 됐고 월 버킷도 `latestStatusAt` 이었다. 그래서 **EFS 픽업 전(`'01'` 배송 예약)에 멈춘 송장에 묶인 매출이 어드민·브랜드 어느 화면에도 뜨지 않았다** — 실측으로 한 브랜드에서 ₩1,510,506 이 10~16일간 그렇게 숨어 있었다(고객이 결제를 마친 유료 시딩 3건). 지금은 결제만 완료되면 후보에 뜨고, 월 버킷·정렬도 `order.paidAt` 이다. 현장결제(onsite)가 원래 `paidAt` 기준이라 이 전환으로 두 축이 통일됐다.
  - **배송 상태는 게이트가 아니라 표시**다. 어드민 브랜드 상세는 `배송` 열(`latestStatusCode` → `lib/tracking-status.ts`)에서, 브랜드 정산탭은 `정산 예정 · 배송 중` 배지로 구분만 하고 **금액은 양쪽 다 받을 금액에 합산**한다. 지급 시점 판단은 어드민이 그 열을 보고 한다.
  - ⚠️ **배송완료 코드 조건이 취소 주문/송장을 막던 유일한 암묵적 방어막이었다.** 그 자리를 `Order.status != 'cancelled'` 와 `Shipment.status != 'cancelled'` 두 축이 명시적으로 대신한다 — 둘 중 하나라도 빼면 취소된 건이 정산 후보에 새고, 증상은 "금액만 틀린" 조용한 형태다.
  - 곁들여 해소된 잠재 버그: 시스템 전체는 `EFS_DELIVERED_CODES = ['33','47','74']`(efs.client.ts)를 쓰는데 정산만 `'33'` 단독이라 **47(수취인 수거)·74(종료 처리)로 끝난 송장이 영원히 정산되지 않았다.** 게이트에서 코드를 없애면서 함께 사라졌다(표시용 코드 집합은 klow_brand 가 3개 전부 미러한다).
  - 회귀 잠금: `settlement/__tests__/settlement-recognition.spec.ts`(배송 미완료 포함·무가 시딩 제외·취소 2축·결제월 버킷·정렬 기준·settle write).
- **KST(UTC+9) 기준 시간 처리**: 월 경계는 `src/common/kst-time.ts` 의 `parseKstYearMonth`/`previousKstYearMonth` 로 KST 기준 산출한다. 어드민 조회 `yearMonth` 는 `YYYY-MM` 형식이고 형식이 어긋나면 400(`yearMonth는 YYYY-MM 형식이어야 합니다`), 미지정 시 `previousKstYearMonth()` = 직전월 — 매월 15일 어드민이 직전월 결제분을 일괄 정산하는 운영 흐름. 모듈 로컬 `KST_OFFSET_MS = 9h` 는 `settle` 이 현장결제 주문에 찍는 `settledMonth`(KST `YYYY-MM`) 산출에 쓴다.
- **정산 확정(settle) 액션**: 한 트랜잭션에서 **송장(`shipmentIds`)과 현장결제 주문(`orderIds`)을 함께** `settledAt=now`·`settledById=adminId` 마킹한다(둘 다 비면 400 `정산할 대상을 선택하세요`). 송장 `where` 에는 `brandId` + `settledAt=null` + settleable(`settleableShipmentWhere()`) 을, 현장결제 `where` 에는 `channel='onsite'` + `paymentStatus=paid` + `settledAt=null` 을 강제하고, 각각 `updateMany.count` 가 요청 개수와 다르면 400 으로 롤백 — 부분 마킹을 방지한다("일부 송장이 정산 대상이 아닙니다" / "일부 현장결제 주문이 정산 대상이 아닙니다"). ⚠️ **배송완료를 요구하지 않는다** — 결제완료가 인식 시점이고, 배송 전 지급 여부는 운영 판단이라 화면의 배송 열로 넘긴다. 현장결제 주문에는 `settledMonth`(KST `YYYY-MM`)도 함께 찍는다. 응답은 `{ settledCount, onsiteSettledCount, settledAt }`. (참고: 정산 *사유/reason* 은 `SettleBody`(`brandId`/`shipmentIds`/`orderIds`) 에 없으며, 기록 필드는 `adminId`·`settledAt` 이다.)
- **브랜드 정산탭 조회 상한 = `BRAND_SETTLEMENT_TAKE`(1000)** *(2026-09-09)*: 예전엔 `take: 200` 하드캡이었는데, **월 필터를 클라가 하므로 그 상한이 곧 "브랜드가 볼 수 있는 과거의 길이"** 였다 — 200건을 넘는 브랜드는 오래된 달이 월 선택기에서 통째로 사라지고 그 달 합계가 과소 집계됐고, 화면 어디에도 신호가 없었다. 지금은 상한을 올리고 **닿으면 화면이 말한다**(klow_brand 절단 배너 — brand-crm 의 `truncated` 선례, "절단은 조용한 왜곡이다"). ⚠️ **응답 shape 은 배열 그대로 둔다** — `{rows, truncated}` 봉투로 바꾸면 배포 창의 구 klow_brand 가 배열이 아닌 것에 `.filter` 를 걸어 정산 화면이 통째로 백지가 된다(브랜드가 돈을 확인하는 화면이라 배포창 백지는 허용 불가). 절단 감지는 클라가 `length >= CAP` 휴리스틱으로 하고, 상수는 klow_brand `settlement.model.ts` 의 **의도된 크로스 레포 미러**다(`EFS_DELIVERED_CODES` 와 같은 관례 — 어느 쪽으로 갈려도 배너만 어긋나고 금액은 정확하다). ⚠️ `listOnsiteForBrand` 도 같은 상수를 쓴다(현장결제도 같은 버그를 갖고 있었다). 더 늘려야 하면 답은 "더 큰 숫자"가 아니라 `?yearMonth=` 쿼리 스코프다 — `SETTLEMENT_SHIPMENT_INCLUDE` 가 행당 1.5~3KB 라 5000 은 모바일에서 화면이 안 열린다.
- **고객 결제액 USD 파생 필드 `customerShippingUsdCents`** *(2026-09-09)*: 브랜드 정산탭이 원화 옆에 달러를 병기하는데, 그 달러가 **그 브랜드 몫**이려면 제품 USD(`OrderItem.unitPriceUsd`, 클라에 이미 있다) + **배송비의 브랜드 몫 USD** 가 필요하다. 후자는 재료(`shippingFeeByBrand`·다른 브랜드 라인 수)가 클라에 없어 서버가 계산해 얹는다 — `shipments/shipment-settlement.ts` 의 `brandShippingCustomerUsdCents` 는 `brandShippingSettlementKrw` 의 형제이고, 둘이 **같은 `perBrandShareUsd` 를 통과**해야 화면의 ₩ 와 $ 가 같은 몫을 가리킨다.
  - ⚠️ **이름에 `settlement` 접두를 쓰지 않는다.** 이 레포에서 그 접두는 **PG 수수료 차감 후 지급액**을 뜻해서(`settlementShippingKrw`·`settlementPriceKrw`), 같은 접두를 달면 다음 사람이 `settlementShippingUsd × fx === settlementShippingKrw` 를 기대하고 검산 코드를 짠다 — 실제로는 5% 어긋난다.
  - ⚠️⚠️ **`fxRateSnapshot` 을 보지 않는다.** KRW 쪽은 환율을 몰라 `null` 을 내지만 고객이 카드로 낸 달러는 환율과 무관한 확정 사실이다. `if (unknown) return 0` 식으로 단순화하면 그 건에서 브랜드가 "얼마를 받는지"도 "고객이 얼마를 냈는지"도 못 보게 된다(회귀 스펙이 이 성질을 잠근다).
  - ⚠️⚠️ **시딩 라인의 제품 USD 는 쓰면 안 된다** — `OrderItem.unitPriceUsd` 는 시딩에서 실결제액이 아니라 EFS 통관 신고가($8.50~$12.50 랜덤)라, 곱하면 **존재하지 않는 달러 매출**이 화면에 뜬다(주문 확인 메일이 같은 이유로 시딩 라인 금액을 숨긴다). ⚠️⚠️ **그렇다고 배송비 몫만 내면 과소 표시다** — 아래 2026-09-10 정정 참고.
  - ⚠️ 클라는 이 값이 `undefined`(서버 배포 전)면 **USD 줄 자체를 숨긴다.** 제품 USD 만 보여주면 배송비가 빠진 과소 금액이 `고객 결제` 라벨을 달고 뜬다 — 고치려던 과다 표시의 반대 방향 버그다.
  - ⚠️⚠️ **`USD × 환율 ≠ KRW` 라는 사실을 화면이 말해야 한다.** 원화는 PG 수수료를 뺀 뒤라, 라벨만 붙이고 설명을 안 넣으면 브랜드가 `$117.44 × 환율` 을 계산해 `₩111,762` 와 대조하고 "금액이 틀렸다"고 문의한다 — 기능이 문의를 줄이는 게 아니라 새 문의 카테고리를 만든다(klow_brand `MonthSummary` 하단 한 줄).
- **정산 내역 엑셀 `GET /v1/brand/settlement/export?yearMonth=`** *(2026-09-09)*: 브랜드가 정산 내역을 회계·세무에 넘길 방법이 화면 캡처뿐이었다. **서버가 조립한다** — 상태 판정(`isVoidedShipment`)·배송비 가산(`attachShippingSettlement`)·시딩 제품명 치환(`seedingItemNames`+`displayLines`)의 **원본이 전부 서버에 있고 클라는 그 미러**라(`shipmentToLine` 은 미러 4개의 조립물), 클라가 만든 배열을 받으면 (a) 조회 상한에 잘린 만큼만 나가고 (b) 미러가 갈리는 날 문서가 조용히 틀려진다. ⚠️ **여기엔 상한을 걸지 않는다**(문서는 전량이 목적). ⚠️ **송장번호·주문번호 셀은 문자열 타입 강제** — 숫자로 나가면 Excel 이 `9.26129E+21` 지수 표기로 바꿔 이 내보내기의 핵심 산출물이 파괴되고 앞자리 0 도 사라진다. ⚠️ 날짜는 `kstDateStr` — `toISOString().slice(0,10)` 은 KST 00~09시 건을 하루 전으로 내보내 화면과 어긋난다. ⚠️ 시트명 `정산내역` 은 `common/xlsx-sheet-names.ts` 의 `EXPORT_SHEET_NAMES` 에 **등록돼 있다** — 브랜드가 이 파일을 문의에 첨부하고 운영자가 배송비용·비교요율 탭에 잘못 올리는 경로가 실재하는데, 등록해 두면 400 으로 거절되고 안 하면 첫 시트 폴백으로 조용히 먹혀 그 국가 요율표를 덮는다. ⚠️ 열 구성은 **20열**이다 — 기존 10열(구분·결제일·현지 송장번호·주문번호·수취인·국가·도시·제품·고객 결제(USD)·유입 할인 링크) + 엑심베이 정산서 10열(정산액(KRW)·승인금액(KRW)·요청통화·요청금액·카드사·접속국가·이메일(PG)·바이어(PG)·제품(PG)·요청일시(PG)). ⚠️⚠️ **새 열은 전부 맨 끝에 붙인다** — 앞쪽 열의 위치가 안 밀려야 브랜드가 이 파일로 만든 회계 시트의 수식이 살아남는다(중간 삽입·중간 삭제 모두 같은 사고다). ⚠️ **`상태`·`정산일`·`송장번호(HAWB)` 3열은 2026-09-14 에 브랜드 요청으로 제거했다**(화면에는 그대로 있고 문서에서만 뺀 것이다 — HAWB 는 EFS 내부 번호라 브랜드가 어디서도 조회할 수 없고, 대조 축은 남은 `현지 송장번호`·`주문번호`다). 그 삭제로 남은 열이 **세 칸 왼쪽으로 밀렸다**(고객 결제(USD) L→I · 유입 할인 링크 M→J · 정산액(KRW) N→K · 요청일시(PG) W→T) — 같은 날 원화 3열 제거에 이은 두 번째 열 이동이라 사전 공지가 필요하고, **되살리는 것(revert 포함) 역시 열을 다시 민다**. 회귀 스펙이 `상태`·`정산일`·`송장번호` 가 **없음**을 음성 잠금한다. ⚠️ 원화 정산액 3열(제품·배송비·합계)은 2026-09-14 에 제거된 그대로이고, 새로 생긴 `정산액(KRW)` 은 **정산서로 확정된 행에만 값이 있다**(미보정 행은 빈 칸 — `0` 으로 채우면 "0원 정산"이라는 거짓이 된다). 회귀 잠금은 `settlement/__tests__/settlement-xlsx.spec.ts`.
- **정산탭 목록을 카드 스택 → 표로** *(2026-09-09)*: 브랜드 피드백 *"빈 공간이 너무 낭비되는 느낌"*. 라인마다 `rounded-2xl p-4` 카드였는데 `StudioShell` 이 **max-w-[1200px] 전폭**이라 좌측 텍스트 블록과 우측 금액 블록이 화면 양끝으로 벌어졌고, 상태 배지가 별도 행(`mt-3`)을 차지해 **한 건에 ~120px** 를 썼다. 어드민 주문목록·송장추적과 같은 표로 바꿨다(**7열**: 날짜·국가·수취인·제품·송장번호·고객 결제·상태 — `정산액` 열은 2026-09-14 에 빠졌다).
  - ⚠️ **klow_brand 의 일반 목록 관례는 `<ul className="divide-y">` 카드 스택**이다(CRM `CustomerList` 주석이 명시). 여기만 표를 쓰는 근거: 그 관례는 **좁은 분할 패널**(CRM 좌측 ~360px)을 위한 것이고, 정산 목록은 전폭에 8개 필드가 **행마다 같은 자리에** 정렬돼야 읽힌다. 물류비 청구 탭은 원래부터 `<table>` 이었으므로 한 페이지에 표기가 둘로 갈리지 않도록 `_components/DataTable.tsx` 로 합쳤다(`bare` 옵션 = 이미 카드 안이라 자기 테두리를 안 그림).
  - ⚠️ **klow_admin `components/ui/table.tsx` 를 가져오지 않는다** — 다른 레포의 디자인 시스템(neutral 팔레트)이고 여기는 브랜드 토큰(ink/sub/line/bg)을 쓴다.
  - ⚠️ **펼친 제품 내역은 `colSpan` 한 칸이 아니라 부모와 같은 셀 구조로 그린다.** 안쪽에 flex 로 그리면 금액이 위 열과 안 맞는다 — 표 폭이 `auto` 라 안쪽 고정폭으로는 절대 못 맞춘다. 셀을 그대로 쓰면 정렬이 구성상 보장된다(실측으로 ~80px 어긋났다).
  - ⚠️ **`<td>` 에 `max-w` + `truncate` 를 직접 걸지 말 것** — `table-layout:auto` 에서 무시돼 긴 이름이 표를 밀어낸다. 폭 제약은 셀 **안쪽 블록**이 가진다.
  - ⚠️ **제품 열에 `w-full`** 을 준다. 안 주면 여유 폭이 열마다 나뉘어 송장번호와 금액 사이에 설명 없는 빈 칸이 생긴다.
  - ⚠️ 숫자 열 헤더(`고객 결제`)는 `whitespace-nowrap` — 좁아지면 "고객 / 결제" 두 줄로 접혀 헤더 높이가 들쭉날쭉해진다.
  - 좁은 화면은 **가로 스크롤**로 흘린다(반응형 레이아웃을 두 벌로 나누면 정렬·펼침 상태가 갈린다 — CRM 이 같은 이유로 한 컴포넌트를 유지한다).
  - 헤더(`MonthSummary`)도 함께 압축: 월 스테퍼를 **받을 금액과 한 줄**로 올리고 안내 3줄을 한 문단으로 합쳤다(~190px → ~110px). ⚠️⚠️ 그 문단의 **"실제 지급은 원화로 입금된다"** 문장을 빼지 말 것 — 2026-09-14 에 헤더 숫자가 달러가 된 뒤로, 없으면 브랜드가 "달러로 받나?"로 읽거나 그 숫자에 환율을 곱해 입금액으로 계산한다(예전의 *달러 ≠ 원화* 문장이 막던 문의가 방향만 뒤집혀 돌아온 형태다). 화면에 원화 정산액이 하나도 없으므로 이 문단이 그걸 말하는 **유일한 자리**다.
  - 곁들여 `globals.css` 에 `input[type=search]` 네이티브 클리어 버튼 숨김 — `NameSearchBox` 가 자체 ✕ 를 겹쳐 두므로 안 숨기면 ✕ 가 두 개로 보인다(배송·시딩 화면에도 같이 적용된다).
- **브랜드 정산탭 조회**: 요약(summary) 엔드포인트는 없다 — 브랜드 화면이 원장 3종을 직접 받아 집계한다. `listDeliveredForBrand`(`GET /delivered`)는 **결제완료 주문의 그 브랜드 송장 전부**(settleable · 취소 제외)를 발송대기·배송중·배송완료·정산완료 구분 없이 200건까지 내려주고(클라가 `settledAt`/`latestStatusCode` 로 분류), ⚠️ 예전에 있던 `brandConfirmedShippedAt != null`(브랜드 패킹 확인) 게이트는 **되살리면 안 된다** — 매출은 결제로 확정되는데 그 플래그는 브랜드가 버튼을 눌러야 켜져서, 안 누른 채 몇 주가 지나면 브랜드가 자기 정산금을 어느 화면에서도 못 본다(위 ₩1,510,506 이 정확히 이 경우였다). `listOnsiteForBrand`(`GET /onsite`)가 현장결제 라인을, [efs-billing](./efs-billing.md) 청구서 3종 라우트가 "낼 돈"을 같은 탭에 채운다.
- **어드민 월별 롤업(monthly)**: `listAdminMonthly(yearMonth)` 는 candidates(미정산만)와 달리 **이미 정산된 브랜드도 포함**해 브랜드별 `receivableKrw`/`settledKrw`/`lineCount`/`unsettledCount` + 계좌정보를 낸다. 정산 실행(`settledAt`) = 입금 완료라 `unsettledCount === 0` 이면 그 달 정산·입금 마감으로 읽는다. 송장과 현장결제 **모두 그 달 `Order.paidAt`** 기준으로 롤업한다.
- **⚠️ 어드민·브랜드 두 화면이 같은 숫자를 보게 하는 규칙** *(2026-09 정리)*: 정산은 서로 다른 3개 쿼리(`listAdminMonthly` / `listAdminBrandCandidates` / 브랜드의 `listDeliveredForBrand`+`listOnsiteForBrand`)가 각자 다른 모집단·상한으로 도는 구조라, 값이 조용히 갈리기 쉽다. 아래는 실제로 갈렸다가 맞춘 것들이다.
  - **현장결제 건수는 주문 단위로 센다.** `listAdminMonthly` 만 `OrderItem` 마다 `lineCount`/`unsettledCount` 를 올려서, 한 주문에 그 브랜드 제품이 여럿이면 건수가 부풀었다 — 실측으로 어드민 목록이 `56건` 인데 상세 표는 `39행`, 브랜드 화면도 `39장` 이었다(금액은 정확했다). 상세와 브랜드 조회가 모두 주문 1건 = 1행이므로 롤업을 거기 맞췄다(`orderId` 를 select 에 추가하고 `Set` 으로 센다). ⚠️ 아이템 단위로 되돌리면 같은 불일치가 돌아온다.
  - ⚠️⚠️ **브랜드 상세(`listAdminBrandCandidates`)는 정산완료 라인도 함께 내려준다.** 예전엔 `settledAt: null` 로 좁혀서 **정산을 실행하는 순간 그 화면이 0건**("정산할 송장이 없습니다")이 됐다 — 어드민이 "무엇을 얼마에 지급했는지" 확인할 화면이 어디에도 남지 않아, 브랜드가 "몇 건 누락됐다"고 할 때 대조할 수단이 DB 직접 조회뿐이었다(브랜드는 자기 정산 탭에서 그 라인을 계속 본다). 미정산/정산완료 분리는 **클라가 `settledAt`(송장)·`status==='settled'`(현장)로** 하고, 실제 마킹은 `settle()` 이 `settledAt: null` 을 다시 강제해 이중 지급을 막는다. 현장결제 축은 `ONSITE_ORDER_WHERE`(원장, settledAt 조건 없음)와 `ONSITE_SETTLEABLE_WHERE`(미정산만)로 나뉜다 — 후자는 **브랜드별 후보 집계와 settle 전용**이다("정산할 브랜드"를 고르는 큐라 미정산만 보는 게 맞다).
  - **어드민 상세 헤더 건수(`shipments.length + onsiteOrders.length`)** 도 같은 축이다 — 예전엔 현장결제를 빼고 세서 아래 표에 나오는 건이 헤더에 없었다.
  - **어드민 미러 타입 `klow_admin/src/lib/api/settlement.ts` 의 `SettlementLineDTO` 는 서버 타입의 미러다.** 필드를 빠뜨리면 그 값이 어드민 화면에서 영영 안 보인다 — `items`(제품별 내역)·`activityIso` 가 실제로 누락돼 있어, 서버는 내려주는데 **어드민만 현장결제 제품 내역을 못 봤다**(브랜드는 카드를 펼쳐 봤다).
  - **어드민 상세 표 3개(미정산 송장 · 현장결제 · 정산 완료)는 주문 목록과 같은 열(`유형`·`이메일`)을 쓴다** *(2026-09-03)*. 배지 정본은 `klow_admin/src/lib/order-status.ts` 의 `ORDER_TYPE_BADGE`/`ORDER_TYPE_HINT` 하나라 두 화면이 같은 어휘로 대사된다(라벨을 다시 적으면 갈린다).
    - 서버는 `SettlementLineDTO.email` 을 추가로 싣는다 — ⚠️ `buyer` 는 `fullName || email` **폴백**이라 이름이 있으면 이메일이 화면에서 사라져 대사 키로 쓸 수 없다. 조회 select 는 원래 `email` 을 읽고 있어 **쿼리·마이그레이션 변경이 없다**. klow_brand 미러는 이 필드를 선언하지 않아 무시하므로 그쪽은 무변경이다.
    - 송장 라인의 유형은 `settlement-cells.ts` 의 `shipmentOrderType()` 이 낸다. ⚠️ `orderTypeOf`(주문 목록)의 **channel 분기를 쓰지 않는 이유**는 취향이 아니라, 현장결제는 배송이 없어 `Shipment` 자체가 없으므로 이 축에 **구조적으로** 올 수 없기 때문이다(현장 표는 상수 `'onsite'` 를 넘긴다). 결제주체가 없는 레거시 시딩(claim 없음)은 주문 목록과 같은 규칙으로 무가(`seedingBrand`)로 본다.
    - ⚠️ 이때 **`ShipmentDTO`(`klow_admin/src/lib/api/shipments.ts`) 미러에도 같은 병이 있었다** — `SHIPMENT_INCLUDE` 는 원래 `order.isSeeding` 과 `seedingClaim.link.paymentBy` 를 내려주는데 손으로 쓴 미러가 그 둘을 선언하지 않아 어드민이 유형을 계산할 수 없었다(위 `items`·`activityIso` 와 같은 사례). **송장을 쓰는 화면에 값이 안 보이면 서버가 아니라 이 두 미러를 먼저 볼 것.**
    - 배포 순서는 **klow_server → klow_admin**. 반대면 이메일 열만 빈칸이고 400 은 나지 않는다(추가 전용 필드).
- **브랜드 정산 탭의 기본 선택 월** = **받을 돈이 있는 가장 최근 달**(`ledgerMonths[0]`). ⚠️⚠️ *(2026-09-04 정정)* 예전 `dataMonths` 는 정산 라인 월 + **EFS 청구서 월**을 합친 배열이었다 — 청구서는 EFS **픽업월**, 정산은 **결제월**이라 다른 축이고, 섞으면 청구서만 있는 달이 최신이 되어 브랜드가 **'받을 금액 ₩0' 빈 화면을 먼저 본다**(아래 `months[0]` 문제가 다른 축으로 되살아난 형태). 지금은 기본 선택용 `ledgerMonths`(정산 라인만)와 선택 가능한 `months`(+ 청구서 월 + 이번 달)가 분리돼 있다 — 목록에 청구서 월을 계속 넣는 이유는 그 달을 열어 청구서를 볼 수 있어야 하기 때문이다. ⚠️ `months[0]` 로 되돌리지 말 것 — 그 배열은 이번 달을 항상 포함시켜 최신순 정렬한 결과라 **언제나 이번 달**이고, 정산은 회고적이라 이번 달은 대개 비어 있다. 실측으로 8월에 ₩1,510,506 을 받을 브랜드가 9월에 탭을 열면 "정산 내역이 없어요" 빈 화면을 먼저 봤다(어드민이 `previousKstYearMonth()` 로 여는 것과 같은 이유). 월 **목록**에는 이번 달을 계속 넣는다(내역이 없어도 열어볼 수 있게).
- **어드민 목록의 월 유지·라벨** *(2026-09-04)*:
  - **보던 달이 유지된다.** 상세가 백링크와 정산 실행 후 `router.push` 에 `?yearMonth=` 를 실어 보냈는데 **목록이 그 쿼리를 읽은 적이 없어**, 7월분을 정산하고 나오면 목록이 직전월(8월)로 튀었다("정산했는데 목록에 없다"). ⚠️ **URL 쿼리로 고치지 않는다** — 이 어드민은 iframe 탭 셸이라 탭 dedupe 키가 쿼리를 떼어낸다(`orders/page.tsx` 주석이 이 버그를 "미작동으로 남아 있는 증거"로 지목하고 있었다). `sessionStorage`(`klow_admin.settlement.yearMonth`, `tracking`·`orders` 와 같은 패턴)로 복원하고 **상세 쪽 쿼리는 제거**했다(두 메커니즘이 공존하면 어느 쪽이 이기는지 알 수 없다).
  - **'미수금 합계' 는 이제 진짜 미수금**(`Σ(receivableKrw − settledKrw)`)이다. `receivableKrw` 는 정산완료를 포함한 그 달 **총액**인데 그대로 '미수금'으로 표시하고 있었고, 서버가 내려주는 `settledKrw` 는 어드민 타입에만 있고 **어디서도 안 쓰였다**. 총액은 `그 달 총액` 칸으로 분리했고, 브랜드 카드의 큰 숫자도 미수금 기준이라 `입금 완료` 배지가 붙은 브랜드는 0 이 된다(화면이 스스로 모순되지 않는다).
- **브랜드에게도 '배송비 확인 중' 을 보여준다** *(2026-09-04)*: `settlementShippingUnknown` 이 klow_brand 미러에 **선언만 되고 한 곳에서도 읽히지 않아**, fx 스냅샷이 결손이면 브랜드는 배송비가 빠진 적은 금액을 아무 신호 없이 봤다(어드민만 `배송비 미상` 경고를 봤다). 위 "미러에 필드를 빠뜨리면 값이 화면에서 영영 안 보인다"(`items`·`activityIso`·`email`)의 **브랜드판**이다.
- **브랜드 화면의 실패 표시** — 헤더 합계는 세 원장의 합이라 **하나만 실패해도 값이 거짓**이다. 예전엔 실패해도 `loading` 이 풀리며 **₩0 이 정상값처럼** 렌더됐고(부분 실패는 더 조용해서 현장결제만 실패하면 그만큼 적은 금액이 확정 표시됐다), 시딩 탭은 `error={null}` 하드코딩이라 **통신 실패가 "정산 내역이 없어요"로 위장**됐다. 지금은 합계가 `—` 로 빠지고 탭마다 `ErrorBlock` + 재시도가 뜬다. ⚠️ EFS 청구서(낼 돈)는 합계와 무관해 실패 판정에서 제외한다.
- **할인 링크 유입 배지 `promotionName`** *(2026-09-10)*: 브랜드 질문 *"프로모션 페이지에서 결제된 건은 정산탭 어디에 포함되나"*. 답은 **`일반주문` 탭**이다 — 프로모션 주문은 `Order.promotionId` 만 채워진 일반 web 주문이고(`channel='web'`, `isSeeding=false`) 탭 분기가 `isSeeding` 하나뿐이라 아무 표시 없이 섞였다. 할인분은 `settlementPriceKrw` 스냅샷에 이미 역산돼 있어 **금액은 처음부터 정확했지만**(할인은 브랜드 부담), 브랜드가 [promotions](./promotions.md) 화면의 구매수·매출과 대조할 식별자가 없었다. 이제 서버가 파생 스칼라 `promotionName` 을 얹고 klow_brand 가 수취인 아래 `{이름} 링크 유입` 보조줄로 그린다(검색 대상에도 포함 — 링크 이름을 치면 전 기간·전 탭에서 그 링크 건이 잡힌다).
  - ⚠️⚠️ **`Order.promotionId` 는 브랜드 스코프가 아니다.** klow_web 체크아웃은 `useAppStore.promotionCode`(**localStorage 영속**)를 **카트 구성과 무관하게** 싣고(`checkout/page.tsx`), 서버도 code 로만 조회한다(`resolvePromotionForOrder` — 카트와 대조하지 않는다). 그래서 A 브랜드 할인 링크를 한 번 방문했던 손님이 나중에 `/shop` 에서 B 브랜드 제품을 사면 그 주문의 `promotionId` 는 **여전히 A 브랜드 프로모션**이다. 게이트 없이 이름을 내리면 **B 브랜드 정산탭에 남의 브랜드 링크 이름이 뜬다.** 판정의 단일 출처는 `shipments/shipment-settlement.ts` 의 `promotionNameForShipment`(`Promotion.brandId === Shipment.brandId` 일 때만 이름을 낸다) — `isVoidedShipment` 와 같은 관례로 좁은 입력 타입 + 순수 함수를 나란히 뒀다. (금액 쪽은 `promotionPriceFor` 가 `productId` 를 키로 써서 애초에 안전하다 — 이 유출 위험은 **표시 축에만** 있다.)
  - ⚠️ **"할인이 적용됐다"는 뜻이 아니다** — 스키마 주석대로 `promotionId` 는 `enabled`·세일가 유무와 무관한 **유입 귀속**이라, 링크를 꺼 둔 채 들어온 정상가 주문에도 붙는다. 그래서 화면 문구가 `할인 적용`·`할인링크` 가 아니라 **`{이름} 링크 유입`** 이고, 툴팁이 (a) 유입 ≠ 할인 적용 (b) 링크를 꺼도 기록됨 (c) **promotions 화면 구매수와 집계 단위가 달라 숫자가 정확히 같지 않다** 를 한 번에 말한다. (c) 를 빼면 `달러 ≠ 원화` 와 똑같은 방식으로 **문의를 줄이는 대신 새 문의 카테고리를 만든다** — promotions 집계는 **주문 단위 · 전 기간 · `Order.totalUsd` 전액**(멀티브랜드면 남의 브랜드 몫 포함)이고, 정산탭 행은 **(주문 × 브랜드) = 송장 단위 · 원장 스코프**다.
  - ⚠️ **시딩에는 구조적으로 안 붙는다** — `seeding.service` 의 `order.create` 두 경로 어디도 `promotionId` 를 박지 않는다. 그래서 시딩 탭엔 배지가 뜨지 않고, 그 사실이 promotions 집계의 `isSeeding: false` 와 저절로 일치한다. 현장결제도 `createOnsite` 가 `promotionId` 를 안 박아 `SettlementLineDTO` 는 무변경이다(프로모션은 `PricingCtx.onsite` 에서 쿼리조차 안 나간다).
  - ⚠️ 재료는 `SETTLEMENT_SHIPMENT_INCLUDE` 에 넣고 **정산 전용 include 로 또 쪼개지 않았다.** 쪼개면 `attachShippingSettlement` 의 제네릭 제약이 이 축을 요구할 수 없게 되어(배송현황 호출부가 그 필드를 안 갖는다) **"include 는 안 넓혔는데 필드만 읽는" 조합을 컴파일로 막지 못한다.** 대가는 배송현황 조회 2곳의 `Promotion` PK 조회 1왕복인데, `promotionId` 가 전부 null 이면 Prisma 가 그 쿼리를 아예 안 쏜다.
  - ⚠️ 클라 미러(`klow_brand/lib/api-types.ts`)에 **재료(`order.promotionId`)를 선언하지 말 것** — 선언하는 순간 프론트가 브랜드 스코프 판정을 베끼기 시작한다(배송비 산식과 같은 규칙). 내리는 것은 이미 걸러진 평평한 스칼라 하나다.
  - **정산 내역 엑셀에도 `유입 할인 링크` 열이 붙는다** — ⚠️ **맨 끝에 append 한다. 중간에 끼우지 말 것**: 브랜드가 이 파일을 받아 만든 회계 시트는 열 위치에 묶여 있어 중간 삽입이 그쪽 수식을 조용히 한 칸씩 민다. 화면엔 링크별 합계가 없고 조회 상한도 있으므로 **링크별 SUM 은 이 엑셀에서만 구할 수 있다**(export 는 전량이다). ⚠️⚠️ 그 "열 위치가 밀린다" 사고가 **2026-09-14 에 실제로 한 번 일어났다** — 원화 3열을 지우면서 뒤쪽이 당겨져 `고객 결제(USD)` O→L, 이 열이 P→M 으로 이동했다(중간 *삭제*도 중간 삽입과 같은 사고이고 회피할 방법이 없어 사전 공지로 처리했다. revert 역시 열을 다시 민다 — 롤백도 파괴적 변경이다).
  - **마이그레이션·백필 없음** · 라우트 수 불변 · cron 불변. `Order.promotionId` 는 2026-07부터 채워져 있어 **과거 건에도 소급해서 배지가 붙는다**(금액은 불변). 배포 순서는 **klow_server → klow_brand** — 역순이면 `promotionName` 이 `undefined` 라 배지가 안 뜰 뿐 400 은 없다(그래서 클라는 `line.promotionName &&` 로만 읽고 관계 객체를 파고들지 않는다).
  - 회귀 잠금: `settlement-recognition.spec.ts` 의 *할인 링크 귀속* describe(최상위 케이스가 **남의 브랜드 링크 → null**) + `settlement-xlsx.spec.ts`(열이 마지막 · 숫자처럼 생긴 링크명도 문자열 셀).
- **정산탭 탭 라벨 `시딩·제품가` → `시딩`** *(2026-09-10)*: 2026-09 배송비 이관 이후 시딩 정산액도 **제품 + 배송비**인데(`settlementSum`) 탭 라벨만 이관 이전 뜻으로 남아, 브랜드에게 "배송비는 정산에 안 들어간다"고 잘못 말하고 있었다. 같은 화면이 이미 모순이었다 — 행은 `제품 ₩X · 배송 ₩Y` 로 쪼개 보여주고, 바로 위 안내문은 "제품 판매액과 **고객이 결제한 배송비**를 합한 금액"이며, 서버 엑셀 `kind` 는 이미 `'시딩'` 이었다. ⚠️ 같은 커밋에서 **라벨 리터럴 중복을 없앴다** — `SourceTabs` 와 `elsewhereHint`("'X' 탭에 N건 있어요")에 리터럴이 두 벌이라, 한쪽만 고치면 **안내문이 존재하지 않는 탭 이름을 가리키는데 둘 다 `string` 이라 타입 에러가 없다**. 그 안내는 "브랜드가 손에 든 송장번호가 어느 탭 건인지 모른다"를 풀려고 있는 것이라 이름이 어긋나면 목적이 정확히 무효가 된다 → `settlement.model.ts` 의 `SOURCE_TAB_LABEL` 단일 출처 + `LEDGER_TABS`(원장 3탭). klow_brand 단독 · 서버/엑셀 무변경(오히려 화면과 문서의 용어가 처음으로 일치한다).
- **⚠️⚠️ 시딩의 '고객 결제' 달러 = `Order.totalUsd`(제품가 + 배송비)** *(2026-09-10 정정)*: 브랜드 지적 *"고객이 결제한 금액은 배송비가 아니라 제품+배송비 아니냐"*. 맞다 — 유료 시딩의 바이어 청구액은 `shippingFeeUsdCents + productPriceUsdCents`(`seeding.service` 의 `checkout`)이므로, 브랜드가 발급 시 **물품 가격을 넣은 링크**는 고객이 제품가도 낸다. 위 항목이 *"`unitPriceUsd` 는 통관 신고가라 쓰면 안 된다"* 에서 **"배송비만 보여준다"로 과교정**했고, 그만큼 고객 결제액이 과소 표시됐다(전액 배송비 건은 `totalUsd === shippingFeeUsd` 라 두 규칙의 결과가 같아 **증상이 물품가 있는 링크에서만 나타났다** — 그래서 조용했다).
  - **규칙**: 시딩은 `Order.totalUsd` 를 그대로 쓴다. 일반 주문은 종전대로 `Σ(items[].unitPriceUsd × qty) + customerShippingUsdCents` 다.
  - ⚠️ **일반 주문에서 `totalUsd` 로 되돌리지 말 것** — 멀티브랜드에서 남의 브랜드 라인까지 세어 과다 표시된다(2026-09-09 에 고친 바로 그 버그). **시딩만 안전한 이유는 구조적으로 단일 브랜드**이기 때문이다: 1 링크 = 1 브랜드 = 1 주문 = 1 송장이라 `totalUsd` 가 곧 그 브랜드 몫이다(`perBrandShareUsd` 의 legacy 분모가 시딩에서 정확히 1 이 되는 것과 같은 사실).
  - ⚠️ `OrderItem.unitPriceUsd` 는 **여전히 쓰면 안 된다**(EFS 통관 신고가). 항목별 달러도 시딩에서는 계속 안 그린다 — 물품가는 링크 단위 총액(`SeedingLink.productPriceKrw`)이라 제품 행으로 쪼갤 근거가 없다.
  - ⚠️ 화면의 **`배송비` 캡션을 제거**했다(값이 제품가를 포함하게 됐으므로 캡션이 거짓말이 된다). 캡션을 되살리려면 값부터 되돌려야 한다.
  - ⚠️ 무가 시딩은 `totalUsd === 0` → `—`(고객이 낸 게 없다). 애초에 `SETTLEABLE_ORDER_WHERE` 가 제외해 이 화면에 오지 않지만, 0 을 `$0.00` 으로 그리는 것보다 없다고 말하는 게 맞다.
  - ⚠️ 레거시 불일치 위험은 없다 — 시딩 주문을 만드는 세 지점(`claim` 무가 · `checkout` 신규 · 재진입 update)이 **`totalUsd` 와 `shippingFeeUsd` 를 항상 함께** 쓰므로 `totalUsd >= shippingFeeUsd` 가 성립한다.
  - 화면(klow_brand `customerPaidUsdCents`)과 엑셀(`shipmentToExportRow`)이 **같은 규칙**이어야 한다 — 한쪽만 고치면 브랜드가 본 숫자와 받은 파일이 갈린다.
  - 회귀 잠금: `settlement-recognition.spec.ts` 의 *정산 내역 엑셀의 고객 결제 달러* describe(⭐⭐ 물품가를 받은 시딩이 제품가+배송비 · ⭐ 일반 주문은 `totalUsd` 를 안 쓴다). ⚠️ 그 픽스처 팩토리의 `totalUsd` 기본값을 **배송비와 같게** 뒀다 — 0 으로 두면 "배송비는 받았는데 청구액은 0" 이라는 존재할 수 없는 행이 되어 이 스펙이 거짓으로 실패한다.
- **브랜드 화면에서 원화 정산액 제거 — `고객 결제(USD)` 만 노출** *(2026-09-14)*: 브랜드 요청. 스튜디오 주문 탭의 `정산 ₩111,762`·제품별 `₩X · KRW/개`, 정산탭 상단 '받을 금액'·표의 `정산액` 열·`제품 ₩X · 배송 ₩Y` 보조줄, 정산 내역 엑셀의 원화 3열을 전부 걷어냈다.
  - ⚠️⚠️ **그 결과 브랜드가 "원화로 얼마 입금받는지"를 볼 수 있는 KLOW 표면이 0개가 된다.** 정산탭에 남는 유일한 원화는 물류비 청구서(**낼 돈**, rose-600)뿐이라, 이제 그 화면에서 **원화 = 낼 돈, 달러 = 받을 돈**이다. 되돌리기 어려운 제품 결정이고 아래 문구 규칙이 전부 이 전제 위에 있다.
  - ⚠️ **`settlementSum`/`line.settlementKrw` 계산은 남는다** — 배송현황 카드의 `freeSeeding`(= 정산액 0) 배지와 월 요약의 입금 상태 배지(입금 완료 / 일부 정산 / 지급 예정)가 그 값의 유일한 소비자다. "원화를 뺐는데 왜 KRW 가 남아 있지" 하고 지우면 **배지가 통째로 사라진다**.
  - ⚠️⚠️ **최대 위험은 헤더 달러 합계의 `null` 처리였다.** `customerPaidUsdCents` 의 null 은 **두 사건**이 뭉쳐 있다 — 무가 시딩(`totalUsd === 0`)은 고객이 *정말* 0원을 냈고, 일반 주문 `shippingUsdCents == null` 은 *모른다*. 셀에서는 둘 다 `—` 가 맞지만 합계에서 `?? 0` 으로 뭉개면 이 화면이 이미 세 번 겪은 **조용한 과소 표시**(`shippingUnknown` · 부분 실패 ₩0 · 절단 배너)가 **화면에서 가장 큰 숫자 자리**에서 재현된다. 그래서 합계는 전용 함수 `sumCustomerPaidUsdCents` 가 `{cents, unknownCount}` 로 돌려주고 화면이 `N건은 고객 결제액을 확인 중` amber 한 줄로 **건수를 말한다**(합계를 통째로 `—` 로 빼지 않는 이유: 원장 부분 실패와 달리 **빠진 범위가 정확히 알려져 있다** — 절단 배너와 같은 판단이다).
  - ⚠️ **`배송비 확인 중` 경고는 지우지 않고 `상태` 셀로 옮겼다**(문구는 `배송비 정산 확인 중`) — 화면에서 원화가 사라져도 **통장에 들어갈 돈은 여전히 배송비만큼 적다**. 지우면 2026-09-04 에 고친 무신호 버그가 형태만 바꿔 부활한다. ⚠️⚠️ 반대로 `고객 결제` 셀에 붙이면 **적극적 거짓**이다 — 달러는 환율 스냅샷과 무관하게 정확하고 회귀 스펙이 그 성질을 잠근다. 보류된 것은 원화 정산액이므로 금액이 아니라 **상태** 축이 맞다.
  - ⚠️ **상단 라벨을 `받을 금액` + 입금 화살표로 되돌리지 말 것** — 그 조합에 달러 숫자가 붙으면 "달러로 입금받는다"로 읽히고, 시각 신호가 안내 문단보다 강해 문장으로 부정해도 안 먹는다 → `고객 결제 금액 (USD)` + `CreditCard` 아이콘.
  - ⚠️ 펼친 제품 행의 손으로 쓴 `<TD />` 배열을 `SETTLEMENT_COLUMNS` **파생**으로 바꿨다 — 주석이 스스로 *"유일하게 파생하지 않는 소비자"* 라고 적어 둔 부채가 열이 빠지면서 실제로 물렸다. 열 `key` 는 리터럴 유니온이라 오타가 컴파일에서 잡힌다(⚠️ 배열에 `as const` 를 붙이면 원소마다 다른 객체 타입의 유니온이 되어 `numeric` 같은 선택 필드를 **읽는 쪽**이 막힌다).
  - ⚠️⚠️ **엑셀 열 위치가 밀렸다** — 원화 3열 삭제로 `고객 결제(USD)` O→L, `유입 할인 링크` P→M. "중간 삽입 금지" 주석이 경고하던 바로 그 사고이고 중간 *삭제*도 같은 사고라 회피 불가능해, 회계 시트를 쓰는 브랜드에 **사전 공지**로 처리했다. ⚠️ **revert 도 열을 다시 밀므로 롤백조차 파괴적 변경**이다.
  - **마이그레이션·백필·라우트 수·cron 전부 불변 · klow_web·klow_admin 무변경.**
  - 회귀 잠금: `settlement-xlsx.spec.ts` 에 **음성 잠금**(원화 열이 *없다*)을 신설했다 — 열 추가는 되돌리기 가장 쉬운 변경이라 "왜 없는지"를 스펙이 직접 말하지 않으면 다음 사람이 친절하게 되살린다(그때 깨지는 게 아무것도 없다). klow_brand `scripts/check-settlement.ts` 에는 합계 null 의미론 4건을 더했다. ⚠️ 그 스크립트는 **2건이 실패한 채 방치**돼 있었다(2026-09-10 시딩 달러 정정을 스펙만 못 따라갔다 — 코드가 맞았다). 상시 실패가 있으면 새 실패를 구분할 수 없어 **선행 커밋으로 녹색 기준선부터 복구**했다.
  - 배포 순서는 **klow_brand → klow_server**(사내 관례 역행 — 두 레포 사이에 와이어 계약 결합이 전혀 없고, 이 순서면 과도기에 브랜드가 원화를 파일로는 얻을 수 있다. 반대면 "엑셀에서 열이 사라졌다" 문의가 먼저 온다).
- **⚠️⚠️ 엑심베이 정산서 업로드 → 정산 원화 보정** *(2026-09-14)*: 브랜드 정산 원화는 지금까지 **주문 시점에 우리가 동결한 환율**(`Order.fxRateSnapshot`)로 계산한 **추정치**였다. 그런데 엑심베이가 실제로 승인·정산한 원화는 그 값과 다르다 — 2026-08 정산서 실측으로 **같은 달 안에서도 실환율이 1370 ~ 1404 로 드리프트**한다(8/24~27 ≈ 1370, 8/15~18 ≈ 1397~1404). 어드민이 정산서를 올리면 그 주문의 승인 원화가 확정되고, **그때부터 그 값이 지급 정본**이 된다.
  - **저장**: 신규 `PgSettlementRecord`(주문 1건 = 1행). ⚠️⚠️ **`orderId @unique` 가 재업로드 멱등성의 키**다(upsert) — `[orderId, yearMonth]` 복합 unique 로 만들면 같은 주문이 두 달에 각각 적재돼 **어느 쪽이 정산 정본인지 알 수 없어진다**. 마이그레이션 `add_pg_settlement_record` 는 `CREATE TABLE` 뿐 → **롤링 배포 안전 · 백필 없음**.
  - **산식**: `pricing/formulas.ts` 의 `settlementKrwFromPgGrossKrw(gross) = floor(gross × 0.95)`. ⚠️ `settlementKrwFromCustomerUsd` 와 **같은 `PAYMENT_FEE_RATE` · 같은 floor** 를 쓴다 — 보정 전후로 수수료 기준이 갈리면 브랜드가 "왜 이달만 계산이 다르냐"를 설명할 수 없다. ⚠️ 입력은 **PG 수수료 미차감 총액**(정산서 `승인금액` 열)이다.
  - **안분**: 정산서는 **주문 1건 = 1행**인데 정산은 **(주문 × 브랜드) = 송장 1건**이라, 멀티브랜드 주문은 **현재 몫 비율**(제품 정산가 + 배송비 몫)대로 나눈다(`pg-correction.ts` 의 `splitByWeight`). ⚠️⚠️ **잔차를 반드시 배분해 `Σ안분 === net` 을 성립**시킨다 — 각자 floor 하면 합이 1~2원 모자라고 그게 "주문 승인액과 정산액 합이 안 맞는다"는 문의가 된다(최대잔여법, 소수부 동률은 key 로 고정해 **결정적**). ⚠️ 단일 브랜드는 나눗셈 자체를 하지 않는다(흔한 경로에 반올림이 낄 자리를 없앤다). ⚠️ 분모가 0 이면 보정하지 않고 `unavailable` 로 떨어뜨린다(NaN 이 금액에 들어가는 것보다 낫다).
  - ⚠️⚠️ **금액을 쓰는 경로는 전부 `effectiveSettlementKrw(추정치, 보정)` 를 거친다** — 브랜드 원장·현장 라인·어드민 후보/월별 롤업/브랜드 상세·엑셀. 한 곳이라도 추정치를 직접 읽으면 **같은 송장이 화면마다 다른 금액**이 된다(이 모듈이 반복해서 겪은 실패 유형). ⚠️ `??` 다(`||` 아님) — 보정값 0 은 "정말 0원"이라 유효하다.
  - ⚠️ **`shippingKrw`(그중 배송비 성분)는 보정하지 않는다** — 보정값은 주문 단위 총액이라 제품/배송 성분으로 쪼갤 근거가 없다. 같은 이유로 어드민 화면은 **보정된 행에 `제품 ₩X · 배송 ₩Y` 분해를 그리지 않는다**(`총액 − 배송비` 로 만든 '제품'은 그 순간 **지어낸 값**이다).
  - ⚠️ **대상 축은 건드리지 않는다** — `settleableShipmentWhere`·`settle()` 무변경. 보정은 순수하게 **금액 축**이라, 보정 기록이 없는 건도 정산 대상 그대로이고 **전 기간의 기본 상태에서 금액이 한 푼도 안 바뀐다**(기존 회귀 스펙 43건이 전부 무수정 통과하는 것이 그 증거다).
  - **파일 실측 구조**(`TransactList` 시트, 68건): 요약 블록 → `Total Count : 68` → **헤더** → 데이터 → 각주. ⚠️⚠️ **헤더 행 번호를 고정하면 안 된다** — 요약 블록이 통화 × 거래구분 **조합 수만큼 가변**이라 조합이 하나 늘거나 주는 달에 통째로 깨진다(`요청일시`·`Ref.`·`승인금액` 을 모두 가진 첫 행을 스캔한다. 이 스캔이 "엉뚱한 파일을 올렸다"에 대한 방어도 겸한다).
  - ⚠️⚠️ **모든 셀이 문자열이고 금액에 콤마가 있다**(`'90,600'`). `parseFloat('132,000') === 132` 라, 이 레포가 결제 확정 경로에서 **정확히 그 버그로 주문을 영구 고착**시킨 적이 있다(`payment.service.parsePgAmount`).
  - **조인 키**는 `Ref.` = `Order.id`(`payment.service` 가 `order_id: order.id` 로 보낸다)이고, **`거래번호(Eximbay)` = `Order.pgTid`** 가 2차 키다. ⚠️ 둘이 어긋나면 조인이 틀렸을 수 있으므로 **적용하지 않고 경고**한다(추측해서 돈을 옮기지 않는다).
  - ⚠️⚠️ **환불은 같은 주문에 두 행으로 온다** — 원거래 Sale 행에 `취소='취소완료'` 가 찍히고 별도 `Refund` 행이 하나 더 생긴다(실측: `Ref.` 가 중복되는 유일한 케이스). **둘 다 빼야** 그 주문이 보정에서 완전히 빠진다 — 한쪽만 빼면 환불된 주문의 승인 원화가 그대로 정산 정본이 된다.
  - ⚠️⚠️ **승인통화가 USD 인 행은 원화가 파일에 아예 없다**(실측 68건 중 5건, **전부 AMEX 계열**) → `approvedKrw: null` = `unavailable`. **0 으로 폴백하면 그 주문의 브랜드 정산액이 0원으로 확정 지급된다.** 스키마에 `@default(0)` 을 주지 않은 이유이기도 하다.
  - ⚠️ 같은 주문번호가 제외 후에도 둘 이상 남으면 **전부 버린다** — 임의로 고른 그 값이 곧 지급액이 되므로 추측하지 않는다.
  - **라우트 3개**(`POST /admin/settlement/pg/preview` · `apply` · `GET /admin/settlement/pg`)는 **슈퍼관리자 전용**이고, `efs-billing` 의 `importPreview`/`importApply` 와 같은 2단계다(서버 무상태 · 적용은 **선택값 JSON**, 파일 재전송 없음). ⚠️ **새 Nest 모듈을 만들지 않고** `SettlementModule` 의 provider/controller 로 넣었다 — `test/app.e2e-spec.ts` 가 모듈 수·cron 수를 단언하는데 그 둘을 바꿀 이유가 없다.
  - ⚠️ 미리보기 `ok` 는 **`matchedCount > 0`** 으로만 판정한다 — "금액이 달라진 행 수"로 게이트하면 이미 반영된 달을 다시 올려 표시 정보만 채우는 정상 경로가 막힌다(`efs-billing` CompareModal 이 같은 실수로 청구중량 백필을 도달 불가로 만든 적이 있다).
  - **회귀 잠금**: `settlement/__tests__/pg-settlement-parse.spec.ts`(가변 요약 블록 · 콤마 금액 · 환불 쌍 양쪽 제외 · USD 승인 null · KST→UTC · 중복 제외 · 형식 거절) + `pg-correction.spec.ts`(단일=나눗셈 없음 · **Σ안분 === net** · 결정성 · 분모 0 → null · 보정 없으면 추정치 그대로 · 보정값 0 보존).
  - 배포 순서는 **klow_server → klow_admin → klow_brand**. ⚠️ **배포만으로는 아무것도 달라지지 않는다 — 정산서를 업로드하는 순간이 곧 보정이다.**
- **반대 방향(낼 돈)**: 브랜드가 KLOW 에 내는 EFS 배송비 후청구는 [efs-billing](./efs-billing.md) 모듈이다 — 이 모듈(받을 돈)과 돈의 방향이 반대이고, 브랜드 정산탭에서 나란히 보인다.
- **관련 파일**: `settlement.service.ts`(settleable 필터 정의·`settleableShipmentWhere` 팩토리·`listDeliveredForBrand`·`listOnsiteForBrand`·`listAdminCandidates`·`listAdminBrandCandidates`·`listAdminMonthly`·`settle`), 2 개 컨트롤러(brand · admin — 브랜드 컨트롤러는 `EfsBillingService` 도 주입해 청구서 열람 라우트를 함께 노출). 송장 include 형태는 [shipments](./shipments.md) 의 `SHIPMENT_INCLUDE`/`SHIPMENT_LIST_OMIT` 재사용. 주문 매출/시딩 구분은 [orders](./orders.md), 브랜드 구독 게이트는 [subscription](./subscription.md) 참고.

## brand-settlement.controller.ts (`@Controller('v1/brand/settlement')`)

> 전체 라우트 `BrandGuard`. 호출 브랜드(`requireBrandId`) 범위로 스코프.

| Method | Path                          | 기능                                                                       |
|--------|-------------------------------|----------------------------------------------------------------------------|
| GET    | `/v1/brand/settlement/delivered` | 결제완료 주문의 송장 전부(발송대기 + 배송중 + 배송완료 + 정산완료, settleable 만, `BRAND_SETTLEMENT_TAKE`) |
| GET    | `/v1/brand/settlement/onsite`    | 현장결제(onsite) 정산 라인(`SettlementLineDTO[]`, `BRAND_SETTLEMENT_TAKE`) |
| GET    | `/v1/brand/settlement/export`    | **정산 내역 엑셀**(`?yearMonth=YYYY-MM` 필수) — 그 달 전량, 조회 상한 없음 |
| GET    | `/v1/brand/settlement/efs-statements` | 전달받은 EFS 배송비 청구서 목록 — [efs-billing](./efs-billing.md)   |
| GET    | `/v1/brand/settlement/efs-statements/:yearMonth` | 청구서 상세(동결 rows 스냅샷)                 |
| GET    | `/v1/brand/settlement/efs-statements/:yearMonth/invoice` | **청구서 엑셀** — 동결 rows 에서 렌더              |
| GET    | `/v1/brand/settlement/efs-statements/:yearMonth/excel` | [legacy] 동결 xlsx 재스트리밍(R2) — klow_brand 전환 후 제거 |

## admin-settlement.controller.ts (`@Controller('admin/settlement')`)

> 전체 라우트 `AdminGuard` + `SuperAdminGuard` (슈퍼관리자 전용 — 자금 지급 처리).

| Method | Path                                   | 기능                                                              |
|--------|----------------------------------------|------------------------------------------------------------------|
| GET    | `/admin/settlement/candidates`         | 지정 월(`?yearMonth=YYYY-MM`, 기본 직전월) 결제완료+미정산 송장 **+ 현장결제 미정산분**을 브랜드별 합계(+계좌정보·`belowCostItemCount`) |
| GET    | `/admin/settlement/monthly`            | 같은 월의 브랜드별 받을 금액 롤업(정산 완료분 포함 — `receivableKrw`/`settledKrw`/`unsettledCount`) |
| GET    | `/admin/settlement/candidates/:brandId`| 특정 브랜드의 해당 월 정산 대상 송장 상세 + 현장결제 주문(`onsiteOrders`) |
| POST   | `/admin/settlement/settle`             | 선택 송장·현장결제 주문(`{brandId, shipmentIds[≤500], orderIds[≤500]}` — 둘 중 하나 이상) settled 마킹(트랜잭션, adminId 기록, `@HttpCode(200)`) |

## admin-pg-settlement.controller.ts (`@Controller('admin/settlement/pg')`)

> 전체 라우트 `AdminGuard` + `SuperAdminGuard` (슈퍼관리자 전용 — 브랜드 지급액을 바꾸는 경로다).
> 경로 첫 세그먼트가 달라 위 `@Get('candidates/:brandId')` 와 충돌하지 않는다.

| Method | Path                            | 기능                                                                 |
|--------|---------------------------------|----------------------------------------------------------------------|
| POST   | `/admin/settlement/pg/preview`  | 엑심베이 정산서 xlsx 파싱 + 대사(multipart `file` + `yearMonth`) — **저장하지 않는다** |
| POST   | `/admin/settlement/pg/apply`    | 어드민이 고른 행만 upsert(`{yearMonth, items[≤1000]}`, `@HttpCode(200)`) |
| GET    | `/admin/settlement/pg`          | 그 달 반영 현황(`recordCount`/`appliedCount`/`unavailableCount`/`byBrand`) — 목록 배지 |
