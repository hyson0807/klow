# KLOW 진행표

**이 문서 하나로 단계별 작업을 진행한다.** 사용자는 새 세션마다 아래 한 줄만 보낸다.

```
docs/PROGRESS.md 를 읽고 다음 단계를 진행해 줘.
```

한 세션은 **한 단계만** 한다. 단계를 끝내면 `§7 진행 기록` 과 `§0 지금 할 것` 을 고치고 인계 메모를
채운 뒤 커밋하고, 사용자에게 새 세션에서 위 한 줄을 다시 보내라고 안내하고 **멈춘다**. 다음 단계를
이어서 시작하지 않는다. 대화가 길어지면 품질이 떨어지기 때문이다.

**이 문서가 상태의 유일한 정본이다.** 트랙 문서(`docs/<트랙>/`)는 **스펙**을 갖고,
"지금 어디까지 왔나"와 "무엇이 참이어야 끝인가"는 여기 있다. 두 곳이 어긋나면 이 문서가 이긴다.

---

## 0. 지금 할 것

**체계 3단계 잔여 — 상태 장치 정리의 남은 절반** (2026-09-22 기록)

근거: 3단계가 `진행 중(부분 완료)` 다. **진행 중인 단계를 먼저 닫는다**(WIP 원칙). 파일 구조 정리는
끝났고 남은 것은 제목 상태 마커 제거 · 폴더 README blockquote 포인터화 · 체크박스 3종 처리다
(`§9` 3단계 인계 메모 참고). 작고 기계적이다.

다음 후보 (위를 못 하게 되면):

1. 체계 2단계 — Key Facts 이관. **값이 가장 크다**(`CLAUDE.md` 424KB 중 402KB(95%)가 매 세션
   자동 로드된다). 3단계 잔여보다 이게 급하면 순서를 바꿔도 된다
2. storefront-sales-analytics 운영 배포 — 첫 실전. 독립적이고 짧다

막힘: aws-fargate 2단계 (AWS 크레딧 승인 대기, 2026-09-11~)

---

## 1. 세션 절차

1. **다음 단계 찾기** — `§0 지금 할 것` 의 첫 줄을 한다. 사용자가 다른 것을 지정했으면 그쪽을 한다.
   `§0` 이 비었거나 근거가 **2주 넘게 낡았으면 멈추고 사용자에게 우선순위를 묻는다** — 추측해서
   고르지 않는다.
2. **인계 메모 읽기** — `§9` 에서 같은 트랙의 직전 메모를 읽는다. 문서와 다르게 구현한 것, 확인하지
   못한 것이 거기 있다.
3. **"읽을 것"만 읽는다** — 그 단계가 지정한 것만. 전부 읽지 않는다. `CLAUDE.md` 는 자동 로드되므로
   따로 읽지 않는다. 단계가 지정한 `decisions/*.md` 만 더 읽는다.
4. **작업 레포로 `cd`** — 워크스페이스 루트에서 서브레포 파일을 절대 stage 하지 않는다.
5. **구현** — `§4 공통 규칙` 을 지킨다.
6. **검증** — `§4` 의 검증 3층 중 해당하는 것 + 그 단계 고유의 완료 기준.
7. **문서 동기화** — 컨트롤러가 바뀌었으면 `server/modules/<module>.md`(**필수**). 새 결정·함정이면
   `decisions/<주제>.md` 에 항목 추가 + `CLAUDE.md` 결정 색인에 한 줄.
8. **기록** — `§7` 표 갱신 + `§9` 인계 메모(10줄 안쪽) + **`§0 지금 할 것` 갱신**.
   **표를 안 고쳤으면 커밋하지 않는다.**
9. **커밋** — 레포마다 따로 커밋한다. 메시지는 각 레포의 최근 커밋 관례를 따른다.
   **push 하지 않는다.**
10. **보고하고 멈춤** — 한 일 / 검증 결과 / 고친 문서 / 사용자가 할 일(있으면)을 알리고, 마지막 줄에
    재진입 한 줄을 안내한다.

### 새 계획을 추가할 때 — 계획 세션

사용자가 아래처럼 보내면 **계획만 하고 코드는 건드리지 않는다.**

```
docs/PROGRESS.md 를 읽고 <할 일>을 계획에 추가해 줘.
```

1. **진입 판정을 먼저 한다** — `§2 진입 기준` 에 하나도 안 걸리면 **트랙을 만들지 않는다.** "그냥
   지금 하면 되는 일"이라고 말하고 확인을 받은 뒤 바로 한다. 단계가 하나뿐인 계획은 표에 올릴
   이유가 없다.
2. **조사한다** — 건드릴 코드·문서를 훑어 전제를 확인한다. 모르는 채로 쪼갠 단계는 착수 즉시
   틀어진다.
3. **정책 공백은 묻는다** (`§3`). 추측해서 계획에 박지 않는다 — 계획서에 박힌 추측은 다음 세션이
   사실로 읽는다.
4. **단계로 쪼갠다** — 한 단계 = 한 세션. **레포 경계와 마이그레이션이 자연 분할선**이고 배포
   순서와도 일치한다. ⚠️ **마이그레이션은 항상 독립 단계로 뗀다** — 스키마 + 백필 + 코드 + 배포를
   한 세션에 넣으면 반드시 넘친다.
5. **첫 1~2단계만 `§5` 템플릿으로 정밀하게 쓴다.** 나머지는 제목과 순서만 남긴다. 뒤 단계의 전제는
   앞 단계가 끝나야 확정되므로, 미리 쓰면 착수 시점엔 틀려 있다 — **틀린 명세는 없는 명세보다
   나쁘다.**
6. **계획 문서는 트랙 폴더 안에 쓴다** — `docs/<트랙>/` 를 만들고 그 안에 둔다. `custom-domain/`·
   `mcf/` 가 선례이고 구성은 셋이다.

   | 파일 | 역할 |
   |---|---|
   | `README.md` | 결정 요약 · 읽는 순서 · 각 문서의 정본 범위 |
   | `flow.md` | 전체 흐름과 설계 논거 ("왜 이렇게 하나") |
   | `implementation-plan.md` | **정본 — 실제 빌드 스펙.** 단계별 명세와 착수 게이트(불변식) |

   작은 트랙이면 `implementation-plan.md` 하나로 시작해도 되지만 **폴더는 만든다** — 나중에
   쪼갤 때 링크가 안 깨진다. ⚠️ 어느 문서에도 **상태를 적지 않는다.** 상태는 `§7` 뿐이다.
7. **기록** — `§6` 에 트랙 섹션(폴더 링크 + 다음 단계만) + `§7` 표 행 + `§0 지금 할 것` 갱신.
   `README.md` 문서 지도에도 폴더를 등재한다. 커밋하고 push 하지 않는다.
8. **멈춘다.** 방금 계획한 단계를 이어서 실행하지 않는다 — **계획도 한 단계다.** 실행은 다음
   세션에서 재진입 한 줄로 시작한다.

### 사용자가 표에 없는 일을 바로 시켰을 때

**막지 않는다.** 진행표를 보지 않고 바로 한다. **끝날 때만** `§2 진입 기준` 에 걸리는지 판정해서,
걸리면 트랙·행을 만들고 아니면 모듈 문서와 커밋으로 닫는다. 진행표는 입구가 아니라 **출구**에서
작동한다.

⚠️ 그 요청이 `진행 중` 인 단계와 같은 파일을 건드리면 **먼저 경고한다** — 그 단계의 인계 메모가
무효화된다.

---

## 2. 진입 / 퇴출 기준

**진행표는 할 일 목록이 아니라 "세션 경계를 넘는 일"의 목록이다.** 한 세션에 시작해서 끝나는 일은
그 세션의 보고와 커밋이 곧 기록이고, 표에 들어오지 않는다.

### 진입 — 하나라도 해당하면 행이 된다

1. 2세션 이상 걸릴 것 같다
2. 레포 2개 이상을 **순서대로** 배포해야 한다
3. Prisma 마이그레이션을 동반한다
4. 외부 의존·승인 대기가 있다 (Eximbay·AWS 크레딧·Amazon Role·Vercel·카카오 검수)
5. **착수와 완료가 세션으로 끊긴다 — 스테이징 완료 후 운영 배포 대기 포함**

단일 레포 버그 수정·문구 변경·어드민 한 화면 수정은 아무리 급해도 표에 안 들어간다. 모듈 문서와
커밋 메시지가 이미 그 일을 하고 있고, 그걸 깨면 안 된다. 착수 결정이 안 난 백로그(예:
`brand-subscription.md` §11 의 미구현 4건)도 진입 기준 미달이다 — 착수하기로 하는 순간 행이 된다.

### 퇴출 — 시간이 아니라 사건으로

**`완료` + 운영 배포 확인 + 그 트랙의 다음 단계가 없거나 완료.** 완료 행은 `§8 최근 완료` 로 내리고,
상한(10행 또는 4주)을 넘으면 인계 메모와 함께 `archive/progress-2026-H2.md` 로 옮긴다.

⚠️ **회수 대상은 상태 행과 인계 메모이지 스펙이 아니다.** 완료 단계의 스펙은 트랙 문서에 원문 그대로
남는다. 트랙 전체가 끝나면 트랙 문서를 `archive/` 로 옮기고 📦 보관 배너를 단다.

---

## 3. 멈추고 사용자에게 물어볼 때

- 마이그레이션이 필요한데 Neon DB 브랜치가 없을 때
- 문서에 없거나 미결정인 정책을 정해야 할 때. **추측해서 구현하지 않는다**
- 문서끼리 서로 달라서 어느 쪽이 맞는지 판단할 근거가 없을 때
- 프로덕션 DB·운영 배포·외부 벤더 콘솔에 손대야 할 때
- 단계가 너무 커서 한 세션에 끝낼 수 없을 때: 멈추지 말고 **일관된 단위까지 끝내고 커밋한 뒤**
  `진행 중(부분 완료)` 로 기록하고 남은 일을 인계 메모에 적는다

---

## 4. 공통 규칙

전 단계에 걸리는 klow 고유 제약. 정본은 `../CLAUDE.md` 이고 여기엔 **세션 절차상 언제 걸리는지**만
적는다.

- **레포 독립성** — 5개 서브레포는 각자 독립 git 레포다. 워크스페이스 루트에서 서브레포 파일을
  stage·commit 하지 않는다. 작업 전에 해당 디렉토리로 `cd`.
- **Prisma** — `npx prisma migrate dev --name <이름>` 만 쓴다(예외: 순수 rename). 스키마를 바꾸는
  작업은 **git 브랜치 + Neon DB 브랜치를 함께** 판다. 드리프트를 리셋으로 풀지 않는다 — 그 DB 의
  데이터가 전부 지워진다.
- **검증 3층** (klow_server 를 건드린 단계는 전부)
  1. `npm run typecheck` — tsconfig **2개**를 돈다. ⚠️ `npx tsc --noEmit` 만 쓰면 `src/` 밖(seed·
     backfill 스크립트)이 조용히 깨진다
  2. `npm run test:e2e` — 34개 모듈 DI 그래프 + **cron 개수**. 새 cron 을 추가하면 기대 목록도 고친다
  3. `npm run start` — env 가드 + 실 DB 접속 + 라우트 매핑 수(부팅 로그가 정본)
- **배포 순서** — 보통 `klow_server → klow_admin → klow_brand → klow_web`. 뒤집으면 400(구 zod 가
  새 필드 거부) · 404(라우트 없음) · **조용한 strip**(non-strict zod 가 새 필드를 버린다)이 난다.
  단계마다 순서와 "뒤집으면 뭐가 깨지는지"를 명시한다.
- **마이그레이션 롤링 안전성** — nullable `ADD COLUMN`·`CREATE TABLE` 은 안전. `DROP COLUMN`·
  `DROP TABLE`·enum 값 제거는 **단일 레플리카 컷오버**가 필요하다.
- **모듈 문서 갱신 계약** — 컨트롤러를 추가·변경하면 `server/modules/<module>.md` 를 함께 갱신한다.
  이건 선택이 아니라 완료 기준의 필수 항목이다.
- **WIP 상한** — 동시 `진행 중` 은 **2 트랙**까지, **마이그레이션을 동반하는 단계는 동시에 1개**까지.
  같은 파일을 반쯤 고친 단계가 둘이면 인계 메모가 서로를 무효화한다.
- **상설 환경 변수 표를 만들지 않는다** — 5개 레포의 `.env.example` 이 정본이다. 여기 복사하면
  드리프트 지점이 하나 더 생긴다. env 를 실제로 추가하는 단계에서는 그 단계의 `할 일`·`완료 기준`
  안의 한 줄로 적는다.

---

## 5. 단계 명세 템플릿

```markdown
#### N. <단계 이름>

- **읽을 것**: <섹션 단위로. 전부 읽지 않는다>
- **건드리는 레포 · 배포 순서**: klow_server → klow_admin (뒤집으면 …) / 또는 `배포 없음`
- **스키마·데이터 위험**: 없음 / <마이그레이션명> · 롤링 안전 여부 · 백필 · DB 브랜치 필요 여부
- **할 일**: <레포별로 묶어서>
- **완료 기준**: <이 단계 고유의 확인만. 공통 3층은 §4 에 한 번>
- **(해당 시) 불변식**: custom-domain §9 F21·F30  ← 번호만. 전문 복사 금지
- **(선택) 선행 조건 / 시점**
```

필드 규칙:

- **읽을 것** — 이 체계의 유일한 컨텍스트 통제 장치다. 파일이 아니라 **섹션**으로 지정한다
  (`purchase-plan.md §12~§15`). 건드리는 모듈의 `server/modules/<m>.md` 와 필요한
  `decisions/<주제>.md` 만 적는다.
- **스키마·데이터 위험** — 없으면 **`없음` 이라고 명시적으로 쓴다.** 빈칸은 "생각 안 함"과 구분이
  안 된다.
- **할 일** — 한 세션에서 끝낼 분량. 길어지면 `2-1`, `2-2` 로 쪼개 별도 세션으로 강제한다.
- **완료 기준** — 그 단계 **고유의** 확인만. 실행 가능한 형태(명령·curl·화면)로 쓴다.

트랙이 이미 상세 계획서를 가졌으면 여기엔 **읽을 것 + 완료 기준만** 쓰고 본문은 트랙 문서로 링크한다.
같은 내용을 두 곳에 쓰지 않는다.

---

## 6. 트랙

각 트랙의 **전체 로드맵은 트랙 문서에 있다.** 여기엔 다음에 할 단계만 명세한다.

### 체계 — 진행 관리 체계 전환

전환 자체를 자기 진행표 위에서 굴린다. 체계가 도는지 확인하는 가장 싼 방법이다.
1~3단계는 전부 docs 레포만 건드리므로 서브레포 검증이 필요 없다.

#### 2. CLAUDE.md Key Facts → `docs/decisions/` 이관

- **읽을 것**: `../CLAUDE.md` 의 `## Key Facts`(자동 로드되므로 이미 손에 있다), 이 문서 §2
- **건드리는 레포 · 배포 순서**: klow(docs) 만. `배포 없음`
- **스키마·데이터 위험**: 없음
- **할 일**
  1. **먼저 이관 직전 값을 측정해 기록한다**(아래 완료 기준이 이 값과 대조한다):
     ```bash
     cd /Users/hyson/welkit/klow
     awk '/^## Key Facts/{f=1;next} /^## /{f=0} f' CLAUDE.md | grep '^- \*\*' \
       | awk -F':\\*\\*' '{h=substr($0,1,index($0,":**")); \
           if (h ~ /202[0-9]-[0-9][0-9]/) {d++; db+=length($0)+1} else {n++; nb+=length($0)+1}} \
           END{printf "dated %d / %d B\nundated %d / %d B\n", d, db, n, nb}'
     ```
     2026-09-22 실측: **날짜 붙은 71개 = 381,957 B · 날짜 없는 17개 = 20,244 B**
     (Key Facts 섹션 전체 402,204 B / `CLAUDE.md` 423,729 B).
     ⚠️ 그 사이에 항목이 늘 수 있으니 **하드코딩된 숫자가 아니라 착수 시점 측정값을 기준으로 쓴다.**
  2. 날짜가 붙은 71개를 주제 파일 9개에 배정하는 표를 만든다 —
     `settlement` · `efs-billing` · `payment` · `shipping-seeding` · `products` · `storefront` ·
     `custom-domain` · `brand-account` · `platform`
  3. 스크립트로 **원문 그대로** 옮긴다. 항목이 전부 `- **제목 (날짜):** 본문` 한 줄 형태라 기계적으로
     분배된다. **본문을 다시 쓰지 않는다**
  4. `decisions/README.md` 에 색인 71행 생성 (`| 날짜 | 제목 | 파일 |`)
  5. `../CLAUDE.md` 의 `## Key Facts` 는 **날짜 없는 상시 사실 17개(20,244 B)만** 남기고,
     `## 결정 기록` 을 신설해 71줄 색인을 둔다. 색인 한 줄 형식:
     `` - `2026-09-10` ⚠️⚠️ PG 실패 콜백이 결제완료로 확정된 사고 — [payment](docs/decisions/payment.md#...) ``
     `⚠️⚠️` 등급은 본문 grep 으로 기계 판정한다
  6. `../CLAUDE.md` 에 규칙 한 줄 추가: *어떤 코드를 건드리기 전에 그 주제의 `docs/decisions/*.md` 를
     읽는다. `⚠️⚠️` 는 돈·장애 직결이다.*
- **완료 기준**
  - **바이트 합 보존**: 착수 시점에 측정한 `dated` 바이트 == `decisions/*.md` 본문 합 (항목 수 71 일치)
  - `wc -c ../CLAUDE.md` 가 약 424KB → 약 52KB (= 상시 사실 20KB + 색인 11KB + 나머지 규칙 21KB)
  - 색인 71줄 ↔ decisions 항목 71개가 1:1 (링크 앵커 전수 대조, 깨진 앵커 0)
- ⚠️ **이 색인이 자동 로드 상실의 방어책이다.** 71줄(약 11KB)은 계속 매 세션 실리므로 "이런 결정이
  있었다"는 여전히 알고, 본문만 필요할 때 읽는다. 색인을 빼면 과거 함정을 놓치기 시작한다.

#### 3. 상태 장치 정리

- **읽을 것**: `README.md` 문서 지도, 각 트랙 README 상단, 이 문서 §2 퇴출 기준
- **건드리는 레포 · 배포 순서**: klow(docs) 만. `배포 없음`
- **스키마·데이터 위험**: 없음
- **할 일**
  1. 트랙 문서 제목의 **상태 마커 제거**(`✅ 코드 완료 · ⚠️ 운영 미배포`, `📝`, `📄`). 위험
     마커(`⚠️`·`🔴`)와 `📦 보관 배너`는 남긴다
  2. 폴더 README 의 "현재 상태" blockquote → `> 상태: [진행표](../PROGRESS.md#N) 참조` 한 줄로 대체
  3. 체크박스 3종 처리 — `storefront-sales-analytics` 15개 삭제(이미 거짓) ·
     `brand-subscription` 4개는 문장 목록으로 · **`aws-fargate-migration` 4개는 남긴다**(런북 실행 중
     손으로 체크하는 절차) + 상단에 "실행 중 임시 표시이며 상태의 근거가 아님" 명시
  4. 실행이 끝난 런북 archive 이동, 인덱스 미등재 문서 5종 처리(Meta 심사 원고 3 · EFS 문의 초안 ·
     `deploy-fixed-pricing-runbook.md`)
- **완료 기준**
  - `grep -rn '^\s*- \[ \]' .` → `aws-fargate-migration.md` 4개만
  - `ls *.md` 전부가 `README.md` 문서 지도에 등재
  - 트랙 문서 제목에 상태 문자열이 남아 있지 않다
- ⚠️⚠️ **런북을 아카이브하기 전에 그 릴리스가 운영에 실제로 나갔는지 확인한다.**
  `deploy-custom-domain-runbook.md` 는 **운영 미배포**라 아카이브 대상이 아니다. 실행이 끝난 것만
  옮긴다 — 안 그러면 아직 필요한 절차서가 "완료된 과거"로 묻힌다.

### storefront-sales-analytics — 브랜드관 성과 대시보드

스펙: [`storefront-sales-analytics.md`](./storefront-sales-analytics.md).
API·불변식 정본은 [`server/modules/storefront-stats.md`](./server/modules/storefront-stats.md).

구현 완료 · **배포 대기**. 대상은 klow_server · klow_brand(klow_web 변경 없음 · 마이그레이션 없음).
배포 절차는 그 문서 §8. **배포가 끝나면 문서를 `archive/` 로 옮긴다**(문서가 스스로 그렇게 적어 뒀다).

### 일정에 없는 트랙 — custom-domain · mcf

**문서는 그대로 두되 `§7` 표에는 올리지 않는다** (2026-09-22, 사용자 결정 — 당분간 구현 계획 없음).
행이 없는 것은 빠뜨린 게 아니다. **다시 시작할 때 `§1 계획 세션` 으로 트랙을 되살린다.**

| 트랙 | 문서 | 멈춘 지점 |
|---|---|---|
| custom-domain | [`custom-domain/`](./custom-domain/) | P0~P4 코드 완료·스테이징 배포·**운영 미배포** / P6 는 `purchase-plan.md` §19 기준 A~E 완료, F(klow_brand `/settings/domain`) 남음. ⚠️ 두 문서의 P6 상태 기술이 서로 어긋나 있다 — 재개 시 먼저 대조할 것 |
| mcf | [`mcf/`](./mcf/) | 문서 완료 · 백엔드 미구현. klow_brand 목업은 `develop/mcf2` 브랜치에만 있다. ⚠️ `implementation-plan.md` §8-4(배송비 선결제분 귀속 A/B)가 미확정이라 재개 전에 정해야 한다 |

### aws-fargate — klow_server Railway → AWS ECS Fargate

스펙: [`aws-fargate-migration.md`](./aws-fargate-migration.md) 의 0~6단계.

1단계(플랫폼 무관 준비 작업 — `CRON_ENABLED`·`enableShutdownHooks`·Dockerfile) 완료.
**2단계 이후는 AWS 크레딧 승인 대기 = `막힘`.**

- 해제 조건(사건): **AWS Activate 크레딧 승인**
- ⚠️ `TRUST_PROXY_HOPS` 는 Railway 엣지에 맞춰 **실측한 값 2** 다. AWS 에서 **재실측**한다 —
  추측으로 넣으면 2026-09-04 사고(웹훅 전부 403 · rate limit 전역 뭉침)가 재현된다
- ⚠️ 루트 `Dockerfile` 을 Railway 가 자동 감지하므로 그 파일을 고치면 **운영 빌드가 바뀐다**

---

## 7. 진행 기록

**상태**: `대기` / `진행 중(부분 완료)` / `막힘(사유)` / `완료`
**운영 배포**: `✗` 미배포 / `✓` 배포됨 / `—` 해당 없음
**커밋**: 변경 없는 레포는 `-`, 기록 시점에 알 수 없는 자기 커밋은 `(이 커밋)`

⚠️ **커밋 해시 없이 `완료` 를 쓸 수 없다.**
⚠️ `막힘` 행은 다음 단계 선택에서 **구조적으로 제외**된다. 해제 조건을 **사건으로** 적는다.
⚠️ 예산: **40행 / 600줄.** 넘으면 `§8 최근 완료` 를 archive 로 회수한다.

| # | 트랙 | 단계 | 상태 | 운영 배포 | 날짜 | 커밋 (server/admin/brand/web/docs) |
|---|---|---|---|---|---|---|
| 1 | 체계 | 진행표 신설 | 완료 | — | 2026-09-22 | - / - / - / - / (이 커밋) |
| 2 | 체계 | Key Facts → decisions 이관 | 대기 | — | | |
| 3 | 체계 | 상태 장치 정리 | 진행 중(부분 완료) | — | 2026-09-22 | - / (이 커밋) / - / - / (이 커밋) |
| 4 | storefront-sales-analytics | 운영 배포 | 대기 | ✗ | | |
| 5 | aws-fargate | 2. AWS 기반 구성 | 막힘 (AWS 크레딧 승인 대기, 2026-09-11~) | — | | |

> custom-domain · mcf 는 **일부러 빠져 있다** — `§6 일정에 없는 트랙` 참고.

---

## 8. 최근 완료

완료 행을 인계 메모와 함께 여기로 내린다. 상한 **10행 또는 4주**, 밀려나면
`archive/progress-2026-H2.md` 로 옮긴다.

*(아직 없음)*

---

## 9. 인계 메모

단계를 끝낸 세션이 채운다. **10줄 안쪽**으로, 다음 세션이 알아야 할 것만.

```
- 문서를 고친 것:
- 확인하지 못한 것:
- 남은 일 (이 순서로):
- 다음 단계가 알 것:
```

⚠️ `부분 완료 → 완료` 전이 시 메모는 **누적이 아니라 교체**한다.

⚠️ **승격 규칙** — 단계를 `완료` 로 바꿀 때 인계 메모를 훑어 승격 대상을 `decisions/` 로 옮긴다.
판정 한 줄: **"이 단계가 끝난 뒤에 코드를 만지는 사람이 이걸 몰라서 사고를 내는가?"** 예면 결정
로그(영구), 아니면 인계 메모(아카이브와 함께 소멸).

### 3단계 — 상태 장치 정리 (부분 완료)

- 한 것: **docs/ 파일 구조 정리.** 최상위 md **18 → 5개**. `reference/` 신설(현행 시스템 설명 6종) ·
  실행 끝난 런북 3종 + EFS 문의 초안 + Meta 심사 원고 3종(`archive/meta-app-review/`)을 archive 로
  이동(📦 배너 + 현행 문서 포인터 + `archive/README.md` 표 등재) · 문서 지도를 종류별로 재편
- 링크: 이동으로 **17개 파일의 href** 를 재작성하고, 텍스트가 위치를 잘못 말하던 **49곳**을 함께
  고쳤다(`[`../architecture.md`](../reference/architecture.md)` 류). 서브레포 2곳도 고쳤다 —
  klow_admin(`CLAUDE.md`·`docs/design-system.md`), klow_server(주석 2곳, 커밋 별도)
- 곁들여 고친 것: `custom-domain/README.md` 가 **이미 삭제된** `server/modules/embed.md` 를
  가리키고 있었다(`c45714b` 카페24 임베드 제거 때 링크를 안 고쳤다) → `archive/embed.md` 로 정정
- 확인하지 못한 것: 없음. 링크는 기계 검증했다(아래 도구)
- **남은 일 (이 순서로)**: ① 트랙 문서 제목의 **상태 마커 제거**(`✅ 코드 완료 · ⚠️ 운영 미배포`,
  `📝`, `📄`) — 위험 마커 `⚠️`·`🔴` 와 `📦` 는 남긴다 ② 폴더 README 의 "현재 상태" blockquote →
  `> 상태: [진행표](../PROGRESS.md#N) 참조` ③ 체크박스 3종 처리(`storefront-sales-analytics` 15개
  삭제 · `brand-subscription` 4개는 문장 목록으로 · **`aws-fargate-migration` 4개는 남긴다**)
- 다음 단계가 알 것: **링크 검증 도구를 레포에 넣어 뒀다** — `python3 docs/tools/linkcheck.py`
  (인자 없으면 `docs/` + `CLAUDE.md`, 깨진 링크가 있으면 종료 코드 1). **현재 기준선은 0개**다.
  문서를 옮기기 전후로 돌려 0을 유지한다. 펜스 코드블록과 `` `...` ``·`` ``...`` `` 코드 스팬은
  건너뛴다 — 문서 안의 예시 링크까지 세면 오탐이 쌓여 아무도 도구를 안 믿게 된다
- ⚠️ 링크 **텍스트**도 같이 봐야 한다. href 만 고치면 `[`../architecture.md`](../reference/architecture.md)`
  처럼 표시 문자열이 옛 위치를 말한다(이번에 49곳). 자동 수정은 `[`POST /v1/orders/quote`](orders.md)`
  처럼 **텍스트가 경로가 아니라 API 라벨**인 경우를 오검출하니 결과를 눈으로 훑을 것(2건 밟고 되돌렸다)
- ⚠️ 2단계(Key Facts 이관) 때 확인할 것: CLAUDE.md 의 **카페24 임베드 항목(2026-08-12)이 스테일**
  이다 — 그 기능은 `c45714b` 에서 제거됐고 모듈 문서도 `archive/embed.md` 로 갔다

### 1단계 — 진행표 신설

- 문서를 고친 것: `PROGRESS.md` 신설, `README.md`(문서 지도에서 상태 제거 + 진행표·계획 트리거
  링크), `../CLAUDE.md`(진입 두 줄 + 진행표 규칙)
- 같은 세션에서 사용자 지시로 두 가지가 더 들어왔다: **계획 세션 경로 신설**(`§1` — 계획 문서는
  반드시 `docs/<트랙>/` 폴더 안에)과 **custom-domain · mcf 를 표에서 제외**(당분간 구현 계획 없음
  → `§6 일정에 없는 트랙` 으로 이동)
- 확인하지 못한 것: **트랙 상태는 각 트랙 README 의 "현재 상태" blockquote 만 읽은 얕은 시드다.**
  커밋·브랜치·실배포와 대조하지 않았다. 표에 남은 `storefront-sales-analytics`·`aws-fargate` 두
  행도 마찬가지다 — 착수하는 세션이 먼저 실제 배포 상태를 확인할 것
- 남은 일 (이 순서로): ① 2단계 Key Facts 이관 ② 3단계 상태 장치 정리 ③ 첫 실전
  (storefront-sales-analytics 운영 배포)
- 다음 단계가 알 것: Key Facts 실측치(2026-09-22, `833ed0e` 기준)는 **섹션 402,204 B / 88항목**,
  그중 **날짜 붙은 71개가 381,957 B**, 날짜 없는 상시 사실 17개가 20,244 B 다.
  ⚠️ **착수 시점에 다시 측정할 것** — 그 사이 항목이 는다(§6 2단계에 측정 명령이 있다).
  항목이 전부 `- **제목 (날짜):** 본문` **한 줄**
  이라 `awk '/^## Key Facts/{f=1;next} /^## /{f=0} f'` + `grep '^- \*\*'` 로 기계 분리된다.
  ⚠️ 한글이 섞여 `sort` 가 `Illegal byte sequence` 를 내므로 `LC_ALL=C` 를 붙인다
- 이 단계에서 세운 규칙 중 되돌리기 쉬운 것 둘: **제목 상태 마커 제거**(제목이 앵커라 상태가 바뀔
  때마다 링크가 깨진다)와 **`aws-fargate` 체크박스는 남긴다**(런북 실행 절차라 정당한 유일 용도)
