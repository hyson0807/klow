# KLOW Workspace

This directory is the **workspace root** for the KLOW K-beauty platform. It contains five sibling projects, each operated as **its own independent git repository**.

## Workspace Layout

```
/Users/hyson/welkit/klow/
├── klow_web/      ← Public mobile webapp     (Next.js 14, port 3001, reads /v1/* via TanStack Query)
├── klow_admin/    ← Admin dashboard          (Next.js 14, port 3000, internal CRUD, pure UI client)
├── klow_brand/    ← Brand self-onboarding    (Next.js 14, port 3002, posts /v1/brand/*)
├── klow_server/   ← Backend API              (NestJS 10, port 4000, owns Prisma + Neon + R2)
├── klow_search_server/ ← 인플루언서 검색 백엔드 (NestJS :4100, 자체 Neon DB + R2 bucket `search`; 프론트는 klow_admin 인플루언서 탭 — 자세히는 klow_search_server/CLAUDE.md)
├── docs/          ← Workspace docs (README.md 인덱스 · architecture.md · server/ = API 엔드포인트 문서 · archive/ = 완료 노트)
├── CLAUDE.md      ← This file
└── .gitignore     ← Excludes the subprojects from the workspace repo
```

## Repo Independence — IMPORTANT

**Each subdirectory is its own git repository with its own history.** The workspace root may be a (separate, lightweight) git repo for docs/CLAUDE.md only.

- **NEVER** stage or commit files inside `klow_web/`, `klow_admin/`, `klow_brand/`, `klow_server/`, or `klow_search_server/` from the workspace root.
- The workspace `.gitignore` already excludes these folders.
- When working on a specific subproject, **`cd` into that subdir first**. Its own `CLAUDE.md` or `README.md` (if any) takes precedence over this one.
- When making cross-cutting changes, do them in each repo's own commit/branch — don't try to coordinate via the workspace root.

## Architecture in One Paragraph

`klow_server` (NestJS, port 4000) is the single source of truth for the database (Neon Postgres via Prisma) and file storage (Cloudflare R2). It exposes four URL surfaces: `/admin/*` for full CRUD by `klow_admin`, `/v1/*` for read access + user flows by `klow_web` (and later, mobile/native apps), `/v1/brand/*` for self-service write access by `klow_brand` (BrandGuard-protected, scoped to the caller's own brand), and `/webhooks/*` for external PG callbacks. All three frontends are pure UI clients — none have Prisma installed. See `docs/architecture.md` for the full picture, and `docs/server/README.md` for the per-module API endpoint reference.

## Where Things Live

| What                          | Where                                                                                                   |
|-------------------------------|---------------------------------------------------------------------------------------------------------|
| Database schema               | `klow_server/prisma/schema.prisma`                                                                      |
| Migrations                    | `klow_server/prisma/migrations/`                                                                        |
| API 엔드포인트 문서           | `docs/server/README.md` (모듈 색인) + `docs/server/modules/<module>.md` — 컨트롤러 변경 시 함께 갱신     |
| Server modules                | `klow_server/src/modules/` (admin-auth, audit-logs, auth, brand-applications, brand-auth, brand-domains, brand-scraper, brands, cart, contact, curated-influencers, customers, embed, instagram, orders, payment, products, promotions, reviews, seeding, settlement, shipments, shipping, shop, stats, subscription, translation, upload) |
| Server validation (zod)       | `klow_server/src/common/validation/` (도메인별 파일 + index.ts 배럴 — import 경로는 `common/validation` 유지)     |
| 가격 커널 (공유)              | `klow_server/src/pricing/` (배럴 — formulas/fx/country-price/promotion/price-line/chargeable-brands). **`modules/` 의 형제**이고 6개 모듈이 의존한다 |
| 제품 카탈로그 게이트          | `klow_server/src/modules/products/product-selects.ts` (노출·구매 가능 판정만 — 가격 계산은 위 `src/pricing/`)  |
| 제품 텍스트 로케일화           | `klow_server/src/modules/products/product-translation.service.ts` (MT 캐시 + overlay) · `product-translation-overrides.ts` (브랜드 수동 번역 순수 로직) · klow_brand `studio/_hooks/useProductTranslations.ts` + `_components/TranslationDriftModal.tsx` |
| Admin pages (보호)            | `klow_admin/src/app/(authed)/` (products, brands, brand-subscriptions, brand-withdrawals, reviews, orders, refunds, returns, shipments, tracking, sales-report, settlement, promotions, influencers, customers, seeding-cost, shipping-countries, shipping-rates, audit-logs, admins) |
| Admin pages (공개)            | `klow_admin/src/app/login/`, `klow_admin/src/app/accept-invite/[token]/`                                |
| Admin API client              | `klow_admin/src/lib/api/` (도메인별 파일 + index.ts 배럴 — import 경로는 `@/lib/api` 유지). 하부 `client.ts` 가 `BASE`·`fetchJson`·`postMultipart`·`downloadFile`·`extractApiError` 소유 (credentials:'include' + 401 자동 /login 리다이렉트, 단 `/admin/auth/*` 는 자체 처리) |
| Admin upload helper           | `klow_admin/src/lib/upload.ts`                                                                          |
| Admin 인증 클라이언트         | `klow_admin/src/lib/admin-auth.ts`, `klow_admin/src/hooks/useCurrentAdmin.ts`, `klow_admin/src/hooks/useIdleLogout.ts` |
| Admin 인증 모듈               | `klow_server/src/modules/admin-auth/` (service, controller, admins controller, totp, invitation, audit interceptor) |
| AdminGuard + CurrentAdmin     | `klow_server/src/modules/admin-auth/admin.guard.ts`, `klow_server/src/modules/admin-auth/super-admin.guard.ts`, `klow_server/src/modules/admin-auth/current-admin.decorator.ts` |
| Admin 시드 스크립트           | `klow_server/prisma/seed/seed-admin.ts` (`npm run seed:admin`, env `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD`) |
| klow_web pages                | `klow_web/src/app/` (product, `[brandSlug]`, brand, shop, cart, checkout, orders, track, seed, my, login, signup, legal, faq, customer-center) |
| klow_web UI i18n (앱 전역)    | `klow_web/src/i18n/` (en 단일 원본 → `npm run i18n:fill` 로 ja/zh/vi/th/id/ru 생성) + `useT`/`useLabels` 훅. 가이드: [`klow_web/docs/i18n.md`](./klow_web/docs/i18n.md) |
| klow_brand pages              | `klow_brand/src/app/` (랜딩 `/`, signup, legal, `(authed)/`{studio(제품 관리), promotions, creators, seeding, instagram, settings}) |
| Instagram 연동 모듈           | `klow_server/src/modules/instagram/` (client, service, connect/resource 컨트롤러, refresh cron, token crypto) + `klow_brand/src/app/(authed)/instagram/`. 가이드: `docs/instagram-integration.md`, 엔드포인트: `docs/server/modules/instagram.md` |
| klow_brand API client         | `klow_brand/src/lib/api.ts` + `klow_brand/src/lib/upload.ts`                                            |
| klow_brand 인증 훅            | `klow_brand/src/hooks/useBrandSession.ts`, `klow_brand/src/hooks/useRequireBrandAuth.ts`                |
| klow_brand 로그인/가입 UI     | `klow_brand/src/app/signup/` + `klow_brand/src/components/{LoginModal,SignupModal,Modal,OtpCodeInput}.tsx` + `klow_brand/src/hooks/useCountdown.ts` + `klow_brand/src/lib/phone.ts` (/login 페이지 없음 — 헤더/CTA에서 모달로 처리) |
| Brand 인증 모듈               | `klow_server/src/modules/brand-auth/` (service, controller, brand-session 쿠키 헬퍼)                    |
| klow_brand 설정 > 로그인 번호 | `klow_brand/src/app/(authed)/settings/_components/PhoneSection.tsx` (계정당 번호 N개 추가/삭제/대표지정) |
| klow_brand 설정 > 이메일 연결 | `klow_brand/src/app/(authed)/settings/_components/EmailSection.tsx` (전화 계정에 이메일+비번 연결 / 구글 계정 비번 설정) |
| Brand 신청 / 제품 모듈        | `klow_server/src/modules/brand-applications/` (public + admin 컨트롤러, 승인/거부 트랜잭션)              |
| BrandGuard + CurrentBrandUser | `klow_server/src/modules/brand-auth/brand.guard.ts`, `klow_server/src/modules/brand-auth/current-brand.decorator.ts` |
| klow_web API client           | `klow_web/src/lib/api.ts`                                                                               |
| klow_web TanStack Query hooks | `klow_web/src/hooks/`                                                                                   |
| klow_web 인증 훅              | `klow_web/src/hooks/useSession.ts`, `klow_web/src/hooks/useAuthGate.ts`                                 |
| klow_web 세션 동기화          | `klow_web/src/hooks/useSessionSync.ts` (+ `components/common/SessionSyncMount.tsx` 레이아웃 마운트)      |
| klow_web 장바구니 스토어      | `klow_web/src/store/useCartStore.ts` (`syncedUserId` 기반 자동 서버 replication)                        |
| klow_web 로그인/가입 화면     | `klow_web/src/app/login/`, `klow_web/src/app/signup/`, `klow_web/src/components/auth/`                  |
| 서버 인증 모듈                | `klow_server/src/modules/web-auth/` (service, controller, password/session, email/phone OTP, Solapi SMS, google strategy) |
| UserGuard + CurrentUser       | `klow_server/src/modules/web-auth/user.guard.ts`, `klow_server/src/modules/web-auth/current-user.decorator.ts` |
| Admin toast feedback          | `klow_admin/src/components/Toast.tsx` (`useToast()`) + wired into `klow_admin/src/hooks/useFormState.ts` |

## klow_server 코드 구조 규칙 (2026-08 정리에서 확정)

`src/` 를 정리하면서 세운 불변식이다. 어기면 정리 전 상태로 되돌아간다.

1. **`src/` 는 정확히 3레벨: `src/<area>/<module>/<file>.ts`.** 모듈 안에 `__tests__/` 외 하위 폴더를 만들지 않는다. 이 깊이를 지키기 때문에 소스 파일의 상대경로가 최대 `../../` 로 끝나고(`__tests__/` 만 한 단계 더 깊어 `../../../`) **`@/` 경로 별칭이 필요 없다** (별칭은 의도적으로 도입하지 않았다 — `nest build` 가 순수 tsc 라 `tsc-alias` 없이는 `dist/main` 이 `MODULE_NOT_FOUND` 로 죽는다).
2. **`src/common/` 은 `src/modules/` 를 절대 import 하지 않는다.** 지금 0건이고, `eslint.config.mjs` 의 `no-restricted-imports` 가 `src/common/**` 에서 이걸 막는다(위반 시 에러 + 이 문서를 가리키는 메시지). ⚠️ **tsc 는 이걸 못 잡는다** — 경로가 문법적으로 맞으면 방향이 틀려도 통과한다. 그래서 규칙으로 못박았다.
3. **`common/` 입주 조건 — 순서가 있다.**
   - **(불변식, 우선)** `common/` 안의 파일이 import 하는 것은 반드시 `common/` 에 있어야 한다. 이게 규칙 2를 성립시킨다.
   - **(휴리스틱, 차선)** 그 밖에는 **서로 다른 모듈 2개 이상이 쓰고 + 도메인 로직이 없을 때**만 올린다. 둘 중 하나라도 안 되면 소비자 모듈 안에 둔다. 도메인 상수(쿠키 이름 등)는 소유 모듈이 갖는다 — `common/cookies.ts`(배관) vs `modules/web-auth/session.ts`(`klow_sid`) 가 그 예다.
   - ⚠️ **소비자를 셀 때 `common/` 내부 소비자도 센다.** 안 세면 규칙이 스스로 규칙 2를 깨뜨린다 — `common/reserved-slugs.ts` 는 모듈 소비자가 `brand-auth` 하나지만 `common/validation/shared.ts` 도 쓰므로 내리면 `validation` 이 `modules/` 를 import 하게 된다(2026-08 정리에서 실제로 내렸다가 되돌렸다).
   - ⚠️ 기존 예외: `common/constants.ts` 는 도메인 그 자체(HS 코드·EFS 필드길이)라 휴리스틱상 내려가야 하지만 `klow_admin/src/lib/constants.ts` 와의 **의도된 크로스 레포 미러**라 남는다. 새 파일에 이 예외를 확대 적용하지 말 것.
4. **모듈 파일은 평면.** 분류는 폴더가 아니라 **파일명 접미사**가 한다: `.controller` / `.service` / `.module` / `.cron` / `.client` / `.strategy` / `.adapter` / `.mapper` / `.types` / `.prompts`. 컨트롤러는 여기에 **URL surface 접두**를 더 붙인다: `admin-` / `brand-` / `public-` / `webhook-`. 순수 헬퍼는 접미사 없는 명사(`brand-weights.ts`). `helpers/` 같은 폴더를 새로 만들면 분류 축이 둘이 되어 더 나빠진다.
5. **`@Cron` 은 서비스 메서드가 아니라 `*.cron.ts` 파일에.** 파일 목록만으로 스케줄 작업 전체가 보여야 한다.
6. **가격 계산은 `src/pricing/`, 카탈로그 게이트는 `modules/products/product-selects.ts`.** 경계를 흐리지 말 것.
7. **배럴 패턴**(`common/validation/`, `src/pricing/`)은 `index.ts` 에 re-export 만 두고 새 코드는 도메인 파일에 넣는다.

**검증 3층** (파일을 옮기거나 모듈 배선을 바꾼 뒤 반드시):

1. **`npm run typecheck`** — `tsconfig.json`(src + 스펙) **과 `tsconfig.scripts.json`(prisma/·scripts/·test/) 둘 다** 돌린다. ⚠️ **`npx tsc --noEmit` 만 쓰면 안 된다** — `tsconfig.json` 은 `rootDir: ./src` + `exclude: [prisma, test]` 라 `src/` 밖을 구조적으로 못 본다. 그래서 `src/` 를 리팩터링하면 거기서 import 하는 seed/backfill 스크립트가 조용히 깨지고 나머지 검증이 전부 초록불로 통과한다(2026-08 정리에서 백필 3개가 실제로 이렇게 죽었다).
2. **`npm run test:e2e`** — `test/app.e2e-spec.ts` 가 **DB 없이**(PrismaService 를 스텁으로 override — ⚠️ `onModuleInit` 을 가진 provider 가 하나 더 있다: `BrandDomainsService` 가 오리진 스냅샷을 프라이밍하는데, 스텁에는 `brandDomain` 이 없어 **ERROR 로그 두 줄이 남는다**. 그건 의도된 fail-closed 경로이고 스펙은 그대로 통과한다) `AppModule` 을 `init()` 까지 띄운다. 두 가지를 잡는다: ① 31개 모듈 DI 그래프(provider 미등록·미export·순환 모듈), ② **cron 9개 등록 여부**. ⚠️ `@Cron` 클래스를 모듈 providers 에 안 넣으면 **조용히 실행되지 않는다** — typecheck 는 통과하고 로그도 안 남는다. 새 cron 을 추가하면 그 스펙의 기대 목록에 이름을 넣을 것.
3. **`npm run start`** — env 가드 + 실제 DB 연결 + 라우트 매핑(현재 311개). 1·2 가 커버하지 못하는 건 `main.ts` 의 fail-closed env 검사와 실 DB 접속뿐이다.

⚠️ `npm run lint` 는 `--fix` 를 물고 있어 **리팩터링과 무관한 파일의 기존 포맷 부채까지 건드린다.** diff 를 깨끗하게 유지하려면 `npx eslint <바꾼 파일>` 로 좁혀 쓸 것.

⚠️ `nest-cli.json` 이 `deleteOutDir: false` + `incremental: true` 라 **파일을 대량 이동한 뒤 증분 빌드가 `dist/` 를 반쪽만 남긴다**. 이동 후 첫 빌드 전엔 `rm -rf dist *.tsbuildinfo`.

## Key Facts

- **Server port:** `4000` (NestJS)
- **Admin port:** `3000` (Next.js dev)
- **klow_web port:** `3001` (Next.js dev, chosen to avoid the admin)
- **klow_brand port:** `3002` (Next.js dev, chosen to avoid web/admin)
- **DB:** Neon Postgres dev branch (Singapore region) — credentials in `klow_server/.env`
- **Object storage:** Cloudflare R2 bucket `klow`, public base `https://pub-cac46f90807b402a9079c58c5e8287bb.r2.dev`
- **R2 quirk:** AWS SDK v3.729+ adds CRC32 checksums that R2 cannot validate. `r2.service.ts` sets `requestChecksumCalculation: 'WHEN_REQUIRED'` to disable this. If presigned uploads break, check this first.
- **CORS:** `klow_server/src/main.ts` already whitelists `http://localhost:*` via regex, so both admin (3000) and klow_web (3001) work out of the box. Swap to an explicit origin list before deploy.
- **Auth (user):** 이메일+비밀번호(OTP 이메일 인증) + Google OAuth. DB `Session` + httpOnly 쿠키(`klow_sid`). `UserGuard`는 실제 세션 검증(klow_server `src/modules/web-auth/`). klow_web은 `useSession` / `useAuthGate` 훅으로 게이트한다. 자세한 규칙은 `docs/architecture.md`의 **User Authentication** 섹션 참고.
- **User profile & cart 영속화:** `User`에 `country` + `CartItem` 테이블. 비로그인 상태에서 고른 배송 국가/카트는 클라이언트 `localStorage`에만 있다가, 로그인 직후 `SessionSyncMount` 훅이 `PATCH /v1/auth/me` + `PUT /v1/cart/merge`(수량 max-merge)로 서버에 승격하고 이후에는 카트 스토어 mutation이 자동으로 `/v1/cart/*`에 replicate된다. Me 탭은 닉네임·국가 편집을 `PATCH /v1/auth/me`로 처리한다.
- **제품 태그 자유 텍스트 + 고정 키워드 폐지 (2026-07-30):** 제품의 **주요 고민(`Product.concerns`)·추천 피부 타입(`Product.recommendedFor`)은 고정 enum 이 아닌 자유 텍스트 태그**다. 저장 정본은 **영문**(`EnglishTagList` — ASCII printable 만, concerns 8개 / recommendedFor 6개 × 40자)이고, 브랜드는 한글로 입력하면 `TagChipInput` 이 칩 추가 시점에 `POST /v1/brand/translate` 로 영문화하며 제출 직전 `resolveEnglishForSubmit` 이 놓친 한글을 일괄 번역한다(⚠️ 번역 API 는 요청당 20개 상한 → 20개씩 청크). 고객에게는 `ProductTranslation` MT 캐시(`concerns`/`recommendedFor` **`Json?`** 컬럼 — `null`=미번역 self-heal 마커 / `[]`=번역했고 원문 없음, `String[]` 은 null 을 못 담아 Json 을 쓴다)가 로케일별 번역을 실어 내려주고, klow_web PDP 는 `product.concerns`/`recommendedFor` 를 **클라 사전 매핑 없이 그대로** 렌더한다. 어드민 폼은 같은 태그 입력이지만 번역 없이 영문 직접 입력(`TagsInput verbatim asciiOnly`). 자동저장은 non-ASCII 태그가 하나라도 있으면 patch 를 보내지 않는다(`englishTagArraySpec` — 안 그러면 400 이 제품명·고시 저장까지 막는다). 같은 커밋에서 **고정 키워드 기반 기능 전부 제거**: `CONCERNS`/`SKIN_TYPE_LABELS` enum·`concern-tags.ts`·**discover 모듈 전체**(`/v1/discover*` → `GET /v1/products?sort=popular` 로 대체, 평점·리뷰수 순)·`/v1/shop/today`+어드민 샵설정·klow_web 온보딩 피부/고민 스텝(국가 선택 전용 모달로 축소)·SkinCard·`/my` 피부 프로필·`/shop` ConcernPicks·SkinTwinCreators·`/feed` 고민 탭·`/discover` 크리에이터 필터·어드민 리뷰 피부 필터. DB 컬럼 8개 drop(`User.skinType/concerns`, `Creator.skinType/concerns`, `Video.concerns/forSkinTypes`, `Review.userSkinType`, `ShopSettings.todaysPickConcern`) — ⚠️ **DROP COLUMN 이라 롤링 배포 비안전**(단일 레플리카 컷오버). 기존 데이터는 `npm run backfill:product-tags-english`(기본 dry-run, `-- --apply` 로 반영, **멱등**)이 `hydration`→`Hydration`·`건성`→`Dry skin` 으로 변환하며 `prisma.product.update` 로 `updatedAt` 을 올려 MT 캐시를 무효화한다. 매핑 동결 테이블은 `prisma/legacy-tag-labels.ts`(백필·시드 공용). 배포 순서·검증은 [`docs/deploy-free-text-product-tags-runbook.md`](./docs/deploy-free-text-product-tags-runbook.md).
- **추천 피부 타입 고정 프리셋 (2026-08-13):** 위 자유 텍스트 태그 중 **`recommendedFor` 만** 고정 선택지 5종(지성·건성·민감성·복합성·수부지 → `Oily skin`/`Dry skin`/`Sensitive skin`/`Combination skin`/`Dehydrated oily skin`)이 생겼다. ⚠️ **이유는 UX 가 아니라 오번역**이다 — 태그가 한 단어라 MT 가 뷰티 문맥을 잡을 근거가 없어 `지성` 이 `Oily skin` 이 아니라 **`Intellect` 로 저장**됐다. 자유 입력은 그대로 살아있고 선택지가 얹힌 형태이며, **`concerns` 는 손대지 않았다**. 스키마·컬럼·`EnglishTagList(6)` 검증·API 계약 전부 불변이라 **마이그레이션도 백필도 없다**(저장 정본은 여전히 영문 1개 값). 사전 정본은 `klow_server/src/modules/products/skin-type-presets.ts`(`{ko, en, labels: Record<TranslatableLocale, string>}` — Record 라 locale 이 늘면 컴파일이 깨진다)이고 `klow_brand/src/lib/skin-type-presets.ts` 가 **의도된 크로스 레포 미러**(+한국어 별칭 `지성피부`/`오일리` 등, 서버엔 없다). 앞 4개 영문 정본은 `prisma/legacy-tag-labels.ts` 의 동결 `SKIN_TYPE_EN` 과 **일부러 같은 값**이라 2026-07-30 백필된 제품이 데이터 이관 없이 첫날부터 물린다(값만 맞추고 **그 동결 파일을 import 하지는 않는다**). ⚠️⚠️ **치환은 `product-translation.service.ts` 의 `localize()` overlay 에서 한다 — `translateAndCache()` 가 아니다.** 후자는 스테일 판정(`!t || sourceUpdatedAt < updatedAt || concerns === null`)에 걸린 행만 고쳐서 **이미 캐시된 로케일 행이 영원히 오번역을 서빙**한다(실측: 한 제품의 ru 캐시가 `чувствительной кожи`(소문자·생격 MT 부산물) + `stale=false` 였고, overlay 라서 캐시 쓰기 없이 `Чувствительная кожа` 로 교정됐다). overlay 진입 시점의 `r.recommendedFor` 는 아직 원문 영어라 조회 키로 쓸 수 있고, **원문과 캐시 길이가 어긋나면 캐시를 그대로 둔다**(오라벨링 방지). ⚠️ `translateAndCache` 로 옮기려는 사람을 위한 함정: 그쪽 재조립 `translated[i++]` 는 **공유 커서를 부수효과로** 올려서 `preset ?? translated[i++]` 로 쓰면 `??` 단락에 i++ 가 안 돌아 off-by-N 이 된다. 클라 쪽 실질 차단기는 **`ProductForm.translateTagOnAdd` 의 한국어 단락 처리**다(프리셋 버튼을 안 쓰고 "지성"을 타이핑해도 MT 를 안 탄다) — `resolveEnglishForSubmit` 도 같은 규칙을 **`sanitizeToAscii` 前에** 태운다(뒤에 태우면 한글이 지워져 사전 조회가 빗나간다). `TagChipInput` 은 `presets` prop 이 있을 때만 토글을 그리고 기존 `add()` 를 `skipTranslate` 로 재사용하며(상한·중복·정규화가 한 곳에 남는다), 칩은 `지성 Oily skin` 로 보이되 **저장·삭제·중복검사는 전부 영문 값 기준**이다. klow_brand 목업(`ProductDetailPreview`)·AI 소개서 프롬프트(`scraper.prompts.ts` 87·151행)도 같은 5개 문자열을 쓴다. 어드민·klow_web 은 무변경(영문 직접 입력/서버값 그대로 렌더). 회귀 잠금은 `products/__tests__/skin-type-presets.spec.ts`(정본이 `EnglishTagList` 통과 — 깨지면 자동저장이 영구 400) + `skin-type-overlay.spec.ts`(**캐시 히트에서 재번역 없이 치환**). 배포 순서는 **klow_server → klow_brand**(반대면 서버가 모르는 정본을 브랜드가 써 넣어 그동안 영어가 노출된다).
- **카테고리 표시명 번역 + 직접 입력 (2026-08-14):** 제품 카테고리가 **고정 7종 셀렉트에서 "직접 입력"까지** 열렸다(klow_brand 스튜디오). 직접 입력은 `Product.categoryKey = null` + 자유 텍스트 `Product.category`(한글로 쓰면 `POST /v1/brand/translate` 로 영문화)로 저장된다 — **스키마·API 계약 무변경**이다(`category` 는 원래 자유 문자열 1~60자, `categoryKey` 는 원래 nullable enum). ⚠️ 대신 **그 제품은 klow_web 쇼핑 카테고리 바·추천 필터 어디에도 안 잡힌다**(치과재료 선례와 동일 — 검색·브랜드관·PDP 는 정상). 같은 작업에서 **`Product.category` 를 처음으로 로케일화**했다: 종전엔 `ProductTranslation` 에 컬럼 자체가 없어 klow_web PDP 가 영문 원문을 그대로 렌더했다. 방식은 **하이브리드** — 고정 7종은 `products/category-presets.ts` 큐레이션 사전, 직접 입력은 신규 `ProductTranslation.category` MT 캐시. ⚠️ **프리셋을 못박은 이유는 `지성 → Intellect` 와 똑같은 병이 이미 퍼져 있었기 때문**이다: 카테고리가 한 단어라 MT 가 뷰티 문맥을 못 잡아 `serum` → `血清`/`Huyết thanh`(**혈청**), `cream`(zh) → `奶油`(유제품), `mask`(zh) → `面具`(가면), `mist` → 6개 중 5개 로케일이 **기상 안개**(薄雾/Sương mù/หมอก/Kabut/Туман)로 나와 있었고, 이게 klow_web **쇼핑 카테고리 바에 실제 노출 중**이었다. 그래서 **세 곳의 미러**(서버 `category-presets.ts` 정본 · klow_web `src/i18n/locales/*` 의 `labels.category.*` · klow_brand `src/lib/i18n.ts` 목업)를 같은 커밋에서 함께 고쳤다 — 안 맞추면 `/shop` 칩 단어와 PDP 단어가 갈린다. ⚠️ 치환은 피부 타입과 같이 `localize()` **overlay** 이고, 추가로 **`if (!t) continue` 앞**에 둔다(프리셋은 캐시 행이 없어도 적용돼야 한다 — 신규 제품 첫 조회·번역 실패). ⚠️ `categoryLabel()` 은 **키뿐 아니라 저장된 영문 표시명까지 대조**한다 — 어드민 폼은 `category` 를 `categoryKey` 와 **독립된 자유 텍스트**로 받으므로 `categoryKey='serum'` + `category='Vitamin C Ampoule'` 인 제품이 정당하게 존재하고, 키만 보고 치환하면 운영자가 정한 이름을 프리셋이 뭉갠다(어긋나면 MT 폴백). ⚠️ 스테일 마커가 `t.concerns === null` → **`t.category === null`** 로 이동해 배포 직후 **기존 캐시 전 행 × 전 로케일이 한 번씩 재번역**된다(제품당 1회·읽기 경로에서 점진 해소·백필 없음). 마이그레이션 `20260814123933_add_product_translation_category` 는 nullable ADD COLUMN 이라 **롤링 배포 안전**. 회귀 잠금은 `products/__tests__/category-presets.spec.ts`(정본 `en` 이 서버 검증 통과 + klow_brand 라벨과 일치) + `category-overlay.spec.ts`(**캐시 히트에서 재번역 없이 치환** + **캐시 미스에서도 프리셋 적용**). 자세히는 [`docs/server/modules/products.md`](./docs/server/modules/products.md).
- **제품 태그·핵심 성분 드래그 정렬 (2026-08-19):** `concerns`(주요 고민) / `recommendedFor`(추천 피부 타입) / `keyIngredients`(핵심 성분) 세 배열은 **저장 순서 그대로가 고객 화면 순서**인데, 브랜드가 그 순서를 정할 수단이 없어 입력 순으로만 쌓였다(바꾸려면 전부 지우고 재입력). klow_brand 스튜디오 제품 편집 폼에서 **드래그로 재정렬**할 수 있게 했다 — 칩은 `TagChipInput`(칩 전체가 핸들, sortable id = 칩 값 문자열 — `add()` 의 대소문자 무시 dedupe 가 유일성을 보장한다), 성분 행은 신규 `KeyIngredientRows.tsx`(⚠️ **그립에서만** 드래그가 시작된다 — 행 전체를 핸들로 만들면 입력칸 텍스트 선택이 죽는다). ⚠️ **klow_server·klow_web 은 무변경**이다 — 순서가 이미 끝에서 끝까지 보존된다(zod·저장·`localize()` 어디에도 정렬·dedupe 가 없고, klow_web PDP `ProductAccordionCard` 는 서버 배열을 변형 없이 렌더하며 성분 `01/02/03` 번호도 배열 위치에서 파생된다). 스키마·마이그레이션·백필·배포 순서 제약 전부 없다(klow_brand 단독 배포). ⚠️ **앞으로도 이 세 배열을 서버·클라 어디서도 정렬하거나 중복제거하지 말 것** — 넣는 순간 브랜드가 정한 순서가 조용히 뭉개진다. ⚠️ 재정렬이 안전한 이유는 `recommendedFor` 프리셋 overlay 가 원문↔캐시를 **인덱스로 짝짓는데도** 재정렬이 `prisma.product.update` 를 타 `@updatedAt` 이 올라가 캐시가 스테일로 재번역되기 때문이다 — **raw SQL·`updateMany` 로 순서만 바꾸면 로케일 라벨이 한 칸씩 밀린다.** ⚠️ 성분 행 id 는 `useRef` 로 들고 추가/삭제/재정렬 때 값 배열과 같이 움직인다(`{name, effect}` 는 타이핑마다 새 객체이고 둘 다 빈 값일 수 있어 값 기반 id 가 불가능, 인덱스 id 는 드롭 애니메이션이 죽는다). ⚠️ `KeyIngredientRows.onChange` 는 **함수형 setState** 를 받는다 — blur 번역이 비동기라 `apply` 시점에 최신 배열을 읽어야 다른 칸에 타이핑한 값을 덮어쓰지 않는다. ⚠️ 그립·프리셋 버튼은 `type="button"` + `preventDefault` 가 둘 다 필요하다(`Field` 가 자식을 `<label>` 로 감싼다 — 없으면 제품 폼이 통째로 제출되거나 첫 입력칸이 포커스된다). 자동저장은 두 필드 모두 **순서 민감 `JSON.stringify` 비교**라 순서만 바꿔도 PATCH 가 나간다(`useProductAutoSave.ts`). 자세히는 [`docs/server/modules/products.md`](./docs/server/modules/products.md).
- **국가별 가격 배열 상한 98 → 250 (2026-08-14):** `countryPrices` zod 상한(`common/validation/product.ts` 의 `COUNTRY_PRICE_ROWS_MAX`, 브랜드·어드민·현장 3곳 공용)을 올렸다. ⚠️ **98은 `SeedingRate` 요율표 국가 수(= 주문 가능국 수)를 그대로 베낀 값이라 여유가 정확히 0** 이었다. klow_brand 가격 탭에 **"모든 국가 무료배송" 일괄 토글**이 생기면서 주문 가능국 전체가 한 배열로 나가므로, 운영팀이 배송비용 탭에서 국가를 하나만 추가하거나 지금은 주문 불가인 국가에 옛 핀/할인 행이 남아 있으면 99행이 되어 **가격뿐 아니라 제품 저장 전체가 400** 으로 죽는다. 스키마·마이그레이션·백필 없음(`ProductCountryPrice.iso2` 는 FK 가 없어 저장 측 제약도 없다). ⚠️ 배포 순서는 **klow_server → klow_brand**(반대면 그 사이 일괄 토글이 400).
- **Auth (admin):** Email + Password (argon2id) + TOTP 2FA. `Admin` / `AdminSession` / `AdminInvitation` / `AdminLoginAttempt` / `AdminAuditLog` 테이블 + httpOnly 쿠키(`klow_admin_sid`, 24h, 60분 idle — `ADMIN_SESSION_TTL_HOURS`/`ADMIN_IDLE_TIMEOUT_MINUTES` 로 조정). `AdminGuard`가 실제 세션 검증(klow_server `src/modules/admin-auth/`). 슈퍼관리자 초대 기반 프로비저닝 (공개 가입 없음, 시드로 첫 super → `/admins`에서 초대). 로그인 실패 5회 → 15분 락. 모든 admin mutation은 `AdminAuditInterceptor`가 `AdminAuditLog`에 자동 기록(GET 제외, password/code/token 류 redact). TOTP secret은 AES-256-GCM 암호화(`ADMIN_TOTP_ENCRYPTION_KEY` 환경변수, 회전 금지).
- **Auth (brand):** **전화번호 + SMS OTP (Solapi) 가 메인 인증**이고, Email + Password (argon2id) + 이메일 OTP, Google OAuth 는 보조 옵션이다. `BrandUser` / `BrandSession` 테이블 + httpOnly 쿠키(`klow_brand_sid`, 7일 기본). `BrandGuard`가 세션 검증(klow_server `src/modules/brand-auth/`). 공개 자체 가입(invitation 없음, TOTP 없음 — 마찰 최소화). klow_brand 가 `/v1/brand/auth/*` 호출. **`BrandUser.email` / `phone` / `googleId` 모두 nullable + @unique** — 같은 사람이 세 방식으로 따로 가입하면 별개 BrandUser 가 생긴다(완전 독립 정책). 비밀번호는 phone-only 가입자에겐 없음(`passwordHash` nullable). OTP 인증은 `EmailVerification`(이메일) + 신규 `PhoneVerification`(전화) 두 테이블 분리 — `purpose` 로 분기(`brand-signup-otp` / `brand-signup-token` / `brand-signup-phone-otp` / `brand-signup-phone-token` / `brand-login-phone-otp` / `brand-login-phone-token`). 코드는 6자리 / 10분 TTL / 5회 시도제한 / 60초 재발송 쿨다운(phone 전용, `PhoneVerification.lastSentAt`). KR `010` only — 클라(`klow_brand/src/lib/phone.ts`)는 010-1234-5678 자동 포맷, 서버(`brand-auth.service.ts` `normalizePhone`)는 digits 만 저장. SMS 발송은 `klow_server/src/modules/web-auth/sms.service.ts` 가 `solapi` SDK 의 `send()` 호출 — `SOLAPI_API_KEY` / `SOLAPI_API_SECRET` / `SOLAPI_SENDER` 환경변수(운영 전 솔라피 콘솔에서 발신번호 사전등록 필수). dev 에서 key 가 비어있으면 콘솔로 OTP 로깅(`[DEV sms] ...`). SMS 발송 엔드포인트는 `THROTTLE_TIGHT` (5회/분 per IP) — 솔라피 비용 폭주 + enumeration 동시 차단. 클라 모달은 6자리 입력 컴포넌트 `OtpCodeInput` + 카운트다운 훅 `useCountdown` 으로 공통 추출 (signup 페이지·LoginModal·SignupModal 3곳 재사용). Google OAuth 는 `klow_web` 과 동일한 `GOOGLE_CLIENT_ID/SECRET` 을 공용으로 쓰고 callback URL 만 `GOOGLE_BRAND_CALLBACK_URL` 로 분리 (`/v1/brand/auth/google` → `/google/authorize` → `/google/callback`). 콜백 후 `BRAND_FRONTEND_URL` 로 redirect. **로그인 수단 사후 연결 (2026-07)**: 가입 시점엔 세 방식이 별개 계정이지만, **가입 후 설정 페이지에서 서로 붙일 수 있다** — 전화 가입 계정이 `POST /v1/brand/auth/email/{send-otp,verify-otp,link}` 로 **이메일 OTP + 비밀번호를 한 흐름에** 붙이면(`EmailSection`) 기존 `login()`·`ForgotPasswordFlow`·`changePassword` 가 추가 코드 없이 살아난다. 구글 가입 계정(이메일 有·비번 無)은 OTP 없이 `POST /v1/brand/auth/set-password` 로 비밀번호만 설정하면 구글+이메일 둘 다 된다. 이메일은 계정당 1개(`BrandUser.email @unique`, 스키마 변경 없음), purpose `brand-link-email-otp`/`-token` + 전용 메일 템플릿(`sendEmailLinkCode`). 에러코드 `email_already_linked`/`email_required`/`password_already_set`. 자격 추가일 뿐이라 **다른 세션을 무효화하지 않는다**. 부수효과: 비번이 생기면 `removePhone` 의 `last_login_method` 가드가 풀리고, 어드민 담당자 표기가 전화번호→이메일로 바뀐다. **로그인 전화번호 N개 (2026-07)**: `BrandUserPhone(brandUserId, phone @unique, isPrimary, verifiedAt)` 로 **계정당 번호 최대 5개** — 등록된 어느 번호로 OTP 를 받아도 같은 BrandUser 로 로그인된다(번호 변경·담당자 폰 분리에도 계정 유지). **`BrandUserPhone.phone` 이 "이 번호를 누가 쓰는가"의 단일 진실**이라 가입 중복검사·로그인 조회가 전부 이 테이블을 보고(A 계정 보조번호로 B 계정 신규가입 차단), **`BrandUser.phone` 은 `isPrimary` 행의 비정규화 미러**로 남아 알림톡 수신번호 폴백(`payment.service`)·NicePay `buyerTel`(`subscription.service`)·`senderPhone` prefill·어드민 `submittedBy.select` 가 무수정으로 계속 읽는다. 추가/삭제/대표지정은 설정 페이지 `PhoneSection` 에서만(`/v1/brand/auth/phones/*`, BrandGuard, purpose `brand-add-phone-otp`/`-token`). 이메일·구글 가입 계정이 첫 번호를 붙이면 그 즉시 전화 로그인이 열린다. 마지막 번호인데 비번·구글도 없으면 삭제 차단(400 `last_login_method`), 대표 삭제 시 최고참 번호 자동 승계. 마이그레이션 `20260728050713_add_brand_user_phones` + 백필 `npm run backfill:brand-user-phones` (**배포 순서: migrate → 백필 → 코드 → 백필 재실행** — 마지막 재실행이 배포 창에서 구 코드로 가입해 미러만 가진 계정을 주워 담는다). **비밀번호 변경/찾기 (2026-07)**: `passwordHash` 있는 계정만 — 설정 페이지 `change-password`(현재 비밀번호 확인, 현재 세션 제외 전 세션 무효화) + LoginModal 내 `password-reset/{send-otp,verify-otp,confirm}`(이메일 OTP 3스텝, purpose `brand-password-reset-*`, confirm 시 전 세션 무효화 후 자동 로그인). 미가입 404 `user_not_found` / 구글·전화 계정 400 `password_not_set`. `me` 응답에 `hasPassword` 포함(설정 섹션 노출 분기). 자세히는 `docs/server/modules/brand-auth.md`.
- **제품 가격 모델 (판매가 고정 → 정산 유동 / 물류비 분리, 2026-07):** 브랜드가 **원가(`Product.costKrw`) + 마진**을 입력 → **정산가(`Product.salePrice`) = 원가 + 마진**(⚠️ `salePrice` 는 이름과 달리 판매가가 아니라 정산가를 담는다 — 역사적 명명). **미핀 국가의 default 판매가(USD 센트)는 두 모델**: (1) **브랜드 신모델**(`Product.basePriceFxRate` = 저장 시점 fx 스냅샷 有) → `ceil(정산가/(1−0.05)/basePriceFxRate ×100)` — **frozenFx 로 판매가는 환율에 고정, 정산가/마진만 라이브 fx 로 유동**. (2) **어드민/legacy**(스냅샷 無) → 단일 `Product.basePriceUsd`(직접 입력). **⚠️ 2026-07-28 전환: 판매가에서 물류비를 완전히 분리**했다 — 이전엔 국가별 2kg 물류비의 절반이 판매가 마크업으로 섞여 있었지만 이제 안 들어가고, 따라서 **미핀 default 판매가는 목적국과 무관한 단일 값**이다(국가차는 핀·할인에서만). 정산 역산도 `floor(청구USD × fx × 0.95)` 로 **물류비 차감 없음**. PG 5% 는 `÷0.95`(마진 net 회수). **국가별 핀**: `ProductCountryPrice(productId, iso2, priceLocal?, discountPct, freeShipping)` — `priceLocal`(현지통화 고정 판매가)이 있으면 그 국가만 판매가 고정·정산 유동, 없으면 위 default 상속. 셋 다 기본값인 국가는 행이 없고, 저장은 **replace-all** 이라 클라가 늘 전체 배열을 보낸다. (`marginKrw` 는 구 모델 잔재로 **dormant**.) **완성도/구매 게이트 = `hasSellablePrice`**(`basePriceUsd>0 OR (basePriceFxRate≠null AND salePrice>0)`, `SELLABLE_PRICE_WHERE`). **국가별 할인 우선, 없으면 글로벌 `Product.discount`(legacy 취소선) 폴백**(스택 안 함). **서버가 목적국별 최종가를 계산해 응답에 싣는다** — 공개 read `?country=`(미지정 US), 응답 `customerPriceUsd`/`listPriceUsd`/`customerDiscountPercent`/`freeShipping`. 1라인 단일 출처 `pricing/price-line.ts priceLine(row, cp, fxRate, currencyUsdRate, promotionPriceLocal)` — 표시(`attachCustomerPricing`)·주문·견적이 모두 거쳐 **표시가 == 청구가** 보장.
- **결제 배송비 + 무료배송 (2026-07-28):** 고객은 판매가와 **별도로** 배송비를 낸다 — `500g 요율/fx × 청구 대상 브랜드수`(한 브랜드 = 한 송장). 그 선결제분은 브랜드 실측 물류비의 **선납금**이고, 입고 실측과의 차액만 브랜드에 후청구한다(실측 ≤ 선결제면 청구 없음). **⚠️ 2026-07-30 전환: 기준이 `2kg 요율의 절반` → `500g 요율 그대로`** 로 바뀌었다(절반 나눗셈 폐지). 요율표는 고정비 비중이 커서 무게 비례가 아니라 **98개국 중 96개국에서 배송비가 올랐고**(중앙값 +24%, 내려간 곳은 US·PH), 그만큼 브랜드 후청구액은 줄었다. 공개 응답 필드는 `logisticsCost2kgKrw` → **`customerShippingKrw`**(무게 중립 rename, 서버가 과도기 동안 구 이름 병기). 캐리어 근사만 2kg 유지(`CARRIER_FALLBACK_WEIGHT_G`). `Order.shippingFeeByBrand` 스냅샷 덕에 **기존 주문 소급 영향 없음** — 마이그레이션·백필 없음. **무료배송은 국가별 설정**(`ProductCountryPrice.freeShipping`, **2026-07-29 전환** — 구 전역 `Product.freeShipping`·`Brand.freeShippingAll` 컬럼과 두 PATCH 엔드포인트는 제거됨)이라 같은 제품도 미국은 무료·일본은 유료가 된다. 판정은 `resolveFreeShipping(row, iso2)` 단일 출처이고 **행이 없는 국가 = 유료**(fail-closed — `(cp)` 대신 `(row, iso2)` 를 받아 목적국 필터를 빠뜨려도 배송비가 안 샌다). 켜면 고객이 배송비를 안 내고 실측 **전액**이 브랜드 몫. 배송비가 브랜드 단위라 **그 브랜드 라인이 전부 무료일 때만** 면제된다(`pricing/chargeable-brands.ts` `chargeableBrandIds(lines, iso2)` — 주문 생성·견적 공유). 브랜드별 금액은 `Order.shippingFeeByBrand`(JSON `{brandId: 센트}`, Σ == `shippingFeeUsd`)에 스냅샷해 송장 발급이 EFS 27번을 정확히 안분한다(legacy 주문 null → 균등분배 폴백). ⚠️ 물류비/캐리어 미설정국·EFS 제외구역 차단은 **요금이 아니라 배송 가능 여부 게이트**라 무료배송이어도 그대로 막는다. 박스 규격(`Product.weightG` + `box{Length,Width,Height}Cm`)은 **제품 공용**(국가별 아님) 값으로, 국가별 예상 배송원가(청구중량)와 캐리어 무게 분기의 입력일 뿐 가격/고객 결제 배송비와 무관 — 제품 create/PATCH payload 로 저장한다(전용 `/box` 엔드포인트 제거). ⚠️ 공개 응답의 `freeShipping` 은 `?country=` 기준 파생값이라 카트/스토어에 스냅샷하면 배송지 변경 시 어긋난다 — klow_web 카트 라인엔 이 필드가 없고, 배송비 금액·라벨은 `POST /v1/orders/quote` 만 정본이다. 브랜드 UI: **국가별 판매가 설정 모달** 하나에서 그 나라 판매가·할인·무료배송 스위치를 정하고, 같은 모달에서 박스 규격(실무게·치수)을 넣어 "고객 부담 vs 무료배송" 브랜드 부담액을 비교한다 — 박스 규격만 제품 공용이라 어느 나라 모달에서 고쳐도 전 국가에 반영된다. 실측 후청구 정산은 어드민 **배송비 청구** 탭(efs-billing).
- **가격 관련 나머지:** **제품 무게는 판매가·고객 결제 배송비에 영향 없음**(캐리어 분기에만 쓰임 — 아래 배송비 항목). 배송비 정본은 **`SeedingRate(iso2, weightG, costKrw)` 국가×무게 요율표 하나**(어드민 **배송비용** 탭 `/seeding-cost`) — **2026-07-29 통합**으로 구 `ShippingCountry.productLogisticsCostKrw`·어드민 물류비용 탭·`seed:product-logistics-cost` 는 제거됐다(컬럼만 dormant 잔존 — 과거 백필 스크립트가 참조). 주문은 `shipping.service.resolveProductShipping(iso2, addr, brandWeights)` 가 `{customerShippingRateKrw(500g), 브랜드별 캐리어}` 를 1회 산출하고, 공개 응답 `GET /v1/shipping-countries` 는 거기서 파생한 `customerShippingKrw`(구 `productLogisticsCostKrw` 자리)를 싣는다. 미설정국·**배송지원(enabled) 제외국**·EFS 제외구역은 **구매 차단**, 캐리어는 `Order.shippingCarrierByBrand`(JSON) 스냅샷. 프론트: **klow_web 은 서버값 직접 렌더**, **klow_brand·klow_admin** 은 `src/lib/cost-pricing.ts`(÷0.95 미러, 물류비 없음)로 판매가/마진/손익 미리보기. 마이그레이션 `20260718062503_add_product_base_price_fx`(basePriceFxRate) + `20260728063909_pricing_free_shipping`(무료배송·박스규격·브랜드별 배송비) + `country_free_shipping`(무료배송 국가별 전환 — `ProductCountryPrice.freeShipping` 추가 / `Product.freeShipping`·`Brand.freeShippingAll` 드롭. ⚠️ DROP COLUMN 이라 롤링 배포 비안전 — 단일 레플리카 컷오버 또는 2단계 배포). 백필 `npm run backfill:drop-logistics-markup`(**멱등 아님** — 기본 dry-run, `-- --apply` 로만 반영. 기준국 US 기준으로 판매가를 보존하므로 다른 국가는 판매가가 이동한다 — dry-run 델타 표 확인 필수). (구 미러 컬럼 `{efs,ems,dhl}RateKrw`·`efsFuelSurchargePerKgKrw` 는 2026-06-23 드롭. `ShippingRate` 는 시딩 EMS/DHL 비교가 전용, 구 `emsSpecialFeePerKgKrw`·`dhlFuelSurchargeRate` 는 dormant.) 자세히는 [`docs/pricing-model.md`](./docs/pricing-model.md) + [`docs/server/modules/shipping.md`](./docs/server/modules/shipping.md).
- **현장(박람회 부스) QR 판매 + 국가별 현장가 (2026-08):** 브랜드가 부스에서 QR 을 띄우면 손님이 자기 폰으로 스캔해 즉석 결제한다(`?mode=onsite` → klow_web 간소 체크아웃, **배송 없음** — 부스 직접 전달). 가격 모델은 **스튜디오 제품 가격 설정과 동일한 2층 구조**다: **기준가는 USD 단일 값**(국가 무관), **국가별 핀은 현지통화 major 로 입력·저장**, **청구는 항상 USD 센트**. 다만 입력이 `원가+마진` 두 칸이 아니라 **`정산가(₩)` 한 칸**이고(즉석 판매라 값 하나로 끝냄), 국가 모달에 **할인·무료배송·배송원가 비교가 없다**(배송이 없으므로). 저장은 `Product.onsitePriceUsd`(청구 정본, USD 센트) + `Product.onsiteSettleKrw`(브랜드가 친 ₩ 원문 — **표시 전용**, USD 왕복 ceil/floor 잔차 ~13원이 입력칸에 되비치는 걸 막는다) + `Product.onsiteExcluded` + **`ProductOnsiteCountryPrice(productId, iso2, priceLocal)`**, 전부 `PUT /v1/brand/products/onsite` 한 번(디바운스 700ms)에 나간다. ⚠️ **`ProductCountryPrice` 에 컬럼을 붙이지 않고 테이블을 나눈 이유는 가격 모델이 달라서가 아니라 쓰기 격리다** — 양쪽 저장이 모두 replace-all(`deleteMany`→`createMany`)이라 한 테이블에 두면 스튜디오 저장이 현장 핀을, 현장 저장이 일반 핀·할인·무료배송을 조용히 전멸시킨다. 가격 산출 단일 출처는 **`pricing/price-line.ts onsitePriceLine(row, ocp, fxRate, currencyUsdRate)`** — 표시와 청구(`orders.createOnsite`)가 같은 함수를 거쳐 표시가==청구가를 보장한다. 현장/일반 분기는 **`PricingCtx.onsite`** 로 `attachCustomerPricing()` 안에서 끝나고(호출부가 DTO 를 사후에 안 덮어쓴다), 현장 원본 4종은 선언적 `StrippedPricingKeys` 로 공개 응답에서 벗겨진다 — 예전 ad-hoc `delete` 방식에선 새 컬럼 `onsiteSettleKrw` 가 곧바로 누락돼 샜다. 우선순위는 **목적국 핀 > `onsitePriceUsd` > `defaultListUsd(row)`**(신모델 `salePrice`+`basePriceFxRate` 파생 / legacy `basePriceUsd`). ⚠️ 마지막 폴백이 `basePriceUsd ?? 0` 이던 시절엔 `basePriceUsd` 가 null 인 신모델 제품이 현장에서 **$0 으로 팔렸다**(회귀 가드 `__tests__/onsite-pricing.spec.ts`). 손님 국가는 **배송지가 아니라 가격 기준국**이고(`POST /v1/orders/onsite` 의 `countryCode?`, 미지정 US, 파생은 `pricingIso2()` 단일 출처), klow_web 은 브랜드관 진입 시 고른 `useAppStore.country` 를 그대로 보낸다. **손님이 직접 바꿀 수 있다** (2026-08) — 브랜드관 좌상단 지구본(`BrandStorefront` 의 `AppChrome`, 현장/일반 공통 헤더라 분기 없음)이 기존 국가 선택 모달(`OnboardingModal`)을 열고, `country` 가 상품 쿼리 queryKey 에 있어 현장가가 즉시 재조회된다. ⚠️ **카트 라인은 담을 때 `customerPriceUsd` 를 스냅샷**하므로 국가만 바뀌면 표시가 != 청구가가 된다 → `OnboardingModal.select()` 가 **가격 기준국이 실제로 바뀔 때만**(`persistedCountry ?? DEFAULT_COUNTRY` 비교라 `null→US` 는 제외) 카트를 비우고 토스트로 알린다. 이 규칙은 모달 한 곳에 있어 `/shop` 헤더 칩·`/my` 경유 변경에도 똑같이 적용된다. `useAppStore.country` 는 localStorage 라 **부스 공용 태블릿이면 다음 손님에게 남는다** — 손님 개인 폰 전제는 그대로 유효하다. 핀이 있는데 그 통화 FX 행이 없으면 `billingRate` 가 차단한다(안 막으면 `¥3,420` → `$3,420`). 국가별 **할인·프로모션·무료배송은 현장에 적용하지 않는다**. **단 제품별 표시용 할인율 `Product.onsiteDiscountPct` 는 있다 (2026-08-13)** — 브랜드가 세팅한 가격(기준가 파생 + 국가 핀)이 **이미 할인가**라는 전제로 그 위에 **취소선만 역산**한다(`listPriceUsd = listPriceUsdCents(청구가, pct)` — 소매 글로벌 `Product.discount` 취소선과 같은 헬퍼 재사용). ⚠️ **청구가·정산가는 pct 와 무관하게 불변**이고, 그 불변식을 주석이 아니라 구조로 강제한다 — `onsitePriceLine()` 은 pct 를 **인자로 받지 않고** `orders.createOnsite` 의 select 에도 **그 컬럼이 없다**. 핀이 있는 국가는 취소선도 **핀 통화에서 직접** 파생해(`listPriceLocal = 핀 × 100 / (100 − pct)`, ⚠️ `÷(1−pct/100)` 은 부동소수 잡음) 내려보낸다 — USD 센트로 왕복하면 판매가는 정확한데 취소선만 최소단위 1 어긋난다(A$30 핀 20% → A$37.50 대신 A$37.51). ⚠️ **통화 소수자리 반올림은 서버가 하지 않는다**(`PricingCtx` 에 통화코드가 없고 서버엔 자릿수 테이블 자체가 없다) — klow_web `formatCurrency`(Intl)가 처리하므로 klow_brand 미리보기(`onsiteListLocal`)도 **`ceilToCurrency` 를 쓰면 안 된다**(쓰면 브랜드 화면과 손님 화면이 1 만큼 갈린다). zod 는 `discountPct: .nullish()` 로 받는다 — ⚠️ `settleKrw` 의 `.default()` 패턴을 따라 `.default(0)` 을 주면 배포 창의 **구 klow_brand 탭이 저장할 때 켜둔 할인율을 조용히 0 으로 지운다**(안 보내면 서버가 현재 값 유지). `saveOnsiteConfig` 의 `changed` 비교에도 넣어야 **할인율만 바꿨을 때 저장이 나간다**. 목록 select 는 명시적이라 `onsiteDiscountPct: true` 를 빠뜨리면 취소선이 PDP(include 경로)에만 뜨고 브랜드관 그리드에는 안 뜬다. klow_web 은 서버가 `customerDiscountPercent > 0` 을 내리는 순간 기존 취소선 게이트가 자동으로 켜지고, 배선은 `listPriceLocal` 을 `customerPriceLocal` 과 같은 경로(`formatLocal(amountOf(...))`)로 흘려주는 것뿐이다(카트 요약도 소계·할인을 major 합산으로 바꿔 **소계 − 할인 = 총액**을 맞춘다). 마이그레이션 `add_onsite_discount_pct` 는 `ADD COLUMN NOT NULL DEFAULT 0` 한 줄이라 **롤링 배포 안전 · 백필 없음**, 배포 순서는 **klow_server → klow_brand**(반대면 저장이 조용히 무시된다). 정산은 `Order.channel='onsite'` 로 **송장 없이 Order 레벨**에서 처리되고 어드민 정산 탭 `/settlement/[brandId]` 의 "현장결제" 섹션이 이미 받아 준다 — `Order.totalUsd` 가 국가별 가격으로 잡히므로 자동으로 정확해진다. 마이그레이션 `add_onsite_country_prices`(추가 전용 → **롤링 배포 안전**, 백필 없음). 자세히는 [`docs/server/modules/products.md`](./docs/server/modules/products.md) `mode=onsite` 항목 + [`docs/server/modules/orders.md`](./docs/server/modules/orders.md) + [`docs/server/modules/brand-applications.md`](./docs/server/modules/brand-applications.md).
- **배송비 요율표 (국가×무게, 시딩·일반주문 공용):** `SeedingRate(iso2, weightG, costKrw)` 표(운영팀이 원가·캐리어·할증·마진을 미리 반영한 `KLOW_시딩_가격표` 고객_가격표, 98개국×71무게티어 0.1~30kg)에서 **무게 올림 조회한 값을 그대로 쓴다** — 런타임 캐리어 비교·할증 가산 없음. **시딩**은 발급 무게의 티어 값이 곧 배송비, **일반 주문**은 **500g 티어 값 그대로**가 고객 결제 배송비(청구 대상 브랜드당 1회)다. 서비스는 `shipping/logistics-rate.service.ts` `LogisticsRateService`(`resolveCost`/`quoteByWeight`/`customerShippingRateByIso2`) — ShippingModule 소유·export(SeedingModule→ShippingModule 의존이 이미 있어 반대면 순환). **캐리어 무게 분기**(`ShippingCountry.seedingCarrierSplitWeightG`: 무게≤분기값 EFS / 초과 EMS, 미설정 시 국가 고정 `productCarrier`)도 2026-07-29 부터 **시딩·일반주문 공용**. 국가 고정 캐리어는 `EFS`/`EMS`/**`EMS_PREMIUM`**(2026-08 추가, 표시명 `EMS-PREMIUM` — prisma enum 값에 하이픈을 못 써 정본은 언더스코어이고 프론트 `carrierLabel()` 이 변환) 중 하나이며 DHL 은 zod 에서 선택 불가다. ⚠️ **무게 분기는 EFS/EMS 로만 갈리므로 `EMS_PREMIUM` 은 분기를 해제한 국가에서만 적용**된다(고정 캐리어 전용). 캐리어는 외부 API 라우팅이 아니라 **EFS 송장 3번 필드 문자열**로만 번역된다(`payload-builder.ts` `CARRIER_TO_EFS_SERVICE_TYPE`: EFS→`Premium`/EMS→`EMS`/EMS_PREMIUM→`EMSPREMIUM`/DHL→`DHL`, `Shipment.efsServiceType` 은 `VarChar(20)`). ⚠️ **이 토큰은 EFS 가 정하므로 추측 금지** — 모르는 값이면 필드가 다 맞아도 `Service type is invalid` 로 발급이 거절된다(EMS_PREMIUM 을 `EMS Premium` 으로 보냈다가 거절당해 EFS 확인 후 공백 없는 대문자로 정정한 선례) — 일반 주문은 **브랜드별 박스 청구중량**(`orders/brand-weights.ts` `brandChargeableWeights` = Σ max(실무게, L×W×H/6)×수량)으로 갈리므로 **한 주문 안에서 브랜드마다 캐리어가 다를 수 있다**(한 브랜드=한 송장). 어드민 **배송비용** 탭(`klow_admin /seeding-cost`)에서 무게×비용 수기 편집 + 엑셀 업로드 + 캐리어(고정값·무게분기) 관리, 초기 적재 시드 `npm run seed:seeding-rates`(`prisma/data/seeding_rates.json`). 엑셀 업로드는 **두 경로가 병존**한다 — 목록 페이지는 `고객_가격표`(국가×무게 매트릭스) **고정 포맷 전용** 파서고, 국가 상세(`/seeding-cost/[iso2]`)는 캐리어에서 받은 **국가 하나짜리 임의 포맷** 요율표를 AI 로 읽는다(2026-08). 목록 페이지는 **왕복**을 지원한다(2026-08) — `GET /admin/seeding-rates/export` 가 `고객_가격표`(배송비) + `청구수수료`(`ShippingCountry.efsBillingFeeKrw`) 2시트를 **임포트 파서가 그대로 읽는 포맷**으로 내보내고, 고쳐서 같은 업로드 입력에 되올리면 반영된다. 수수료 시트는 **선택**이라 없는 파일(운영팀 원본)은 종전과 동일하게 동작하고, **빈 수수료 칸은 미변경**이다(인라인 편집의 "빈 값→1000" 폴백과 의도적으로 다름). 적용이 국가 단위 전체 교체라 엑셀에서 지운 무게는 사라지므로 미리보기가 삭제될 티어를 먼저 보여주고, ⚠️ **티어가 안 온 국가(수수료만 고친 경우)에는 replace 를 걸지 않는다**(걸면 그 국가 요율표가 전멸). 후자는 **AI 에게 레이아웃(어느 시트·어느 열이 무게/가격·단위·통화)만 묻고 금액은 서버가 원본 셀에서 직접 읽어** 자릿수 환각을 원천 차단하고(`shipping/rate-sheet-ai.service.ts`), 열을 잘못 짚으면 미리보기에서 후보 열을 골라 **AI 재호출 없이** 재추출한다. 적용은 `PUT /admin/seeding-rates/:iso2/tiers`(`replace`/`merge`)이고, 전체 교체가 지울 기존 티어(표준 소형 100g/250g/750g 등)는 미리보기에 먼저 띄운다. **이 AI 추출은 어드민 '해외배송 비교요율' 국가 상세(`/shipping-rates/[iso2]?carrier=EMS|DHL`)에도 같은 엔진·같은 모달(`klow_admin/src/components/AiRateImportModal.tsx`)로 붙어 있다**(`POST|PUT /admin/shipping-rates/:carrier/:iso2/…`, `ShippingRateService.aiImportPreview`/`replaceTiers`) — ⚠️ 이쪽은 캐리어 축이 하나 더 있어 **조회·삭제 범위에 carrier 가 반드시 들어가야** 반대 캐리어 요율이 안 지워진다. **비교요율 목록 페이지도 왕복을 지원한다**(2026-08) — `GET /admin/shipping-rates/export?carrier=` 가 **캐리어당 파일 1개**(시트 1장, 이름은 `xlsx-grid.ts` 의 `CARRIER_SHEET_NAMES`)를 내보내고 같은 업로드 입력에 되올리면 반영되며, 미리보기가 삭제될 티어를 먼저 보여준다. 매트릭스 라이터는 배송비용 export 와 공용(`shipping/rate-matrix-xlsx.ts`)이다. ⚠️ **세 탭(EMS·DHL·배송비용)의 내보내기 파일은 시트명만 다르고 형태가 같다** — 그래서 **탭을 잘못 고른 업로드는 리더가 `WrongSheetError`(400)로 거절**한다(안 막으면 미매칭 0건으로 미리보기가 완벽해 보이는 채 EMS 요율이 DHL 로, 혹은 비교요율이 고객 결제 배송비로 저장된다). 2026-08-13 부터 **국가 설정 탭의 `시딩신고가`가 네 번째 내보내기**로 합류했다 — 이쪽은 매트릭스가 아니라 국가=행 목록형이라 형태는 다르지만, **모든 내보내기 시트명은 `xlsx-grid.ts` 의 `EXPORT_SHEET_NAMES` 한 곳에 모아** 네 탭이 서로를 거절한다. ⚠️ **새 내보내기를 추가하면 반드시 그 배열에도 넣을 것** — 빠뜨리면 그 파일이 다른 탭에서 첫 시트 폴백으로 조용히 먹힌다. 단 **아는 시트명일 때만** 거절하고 시트명이 제각각인 운영팀 원본은 종전대로 첫 시트 폴백을 탄다 — 폴백은 그쪽을 받으려고 있는 것이다. ⚠️ **비교요율의 임포트 리더만 SheetJS 가 아니라 자체 OOXML 파서**(`xlsx-grid.ts` — 한컴셀 `x:` 접두사 파일 대응)라 SheetJS 가 쓴 파일을 그게 되읽는 구조다. 라이터를 건드리면 왕복 스펙(`shipping/__tests__/shipping-rate-export.spec.ts`)이 먼저 깨진다. xlsx 다운로드 응답 헤더는 `common/xlsx-download.ts` `sendXlsx()` 공용(`xlsx-upload.ts` 의 반대 방향 짝, 4곳 사용). **'국가 설정'** 탭은 enabled(배송지원 화이트리스트 — 일반 주문만 게이트, 시딩은 무관)·EFS 제외구역 + **시딩 통관 신고가**(인라인 편집 + 엑셀 왕복 — 아래 별도 항목) 담당.
- **EFS 배송비 후청구 (efs-billing, 2026-07-29 통합):** EFS 정산표엔 **시딩·일반주문 송장이 섞여** 오므로 어드민 **배송비 청구** 탭(`/efs-billing`, 슈퍼관리자 전용)이 둘 다 받는다(구 시딩 전용 하드코딩 `order.isSeeding=true` 제거). 행마다 `kind`(general|seeding)로 구분하고 **청구 산식이 다르다** — 시딩은 `EFS실비 + 국가별 수수료`(바이어 결제 링크는 청구 제외), **일반주문은 `max(0, 실비 + 수수료 − 고객 선결제)`** (고객이 체크아웃에서 배송비를 이미 냈으므로 안 빼면 이중청구). 선결제액 정본은 `pricing/chargeable-brands.ts` `perBrandShareUsd` + `Order.fxRateSnapshot` — **송장 발급이 EFS 27번에 박는 값과 같은 함수**라 송장==청구서가 보장된다(스냅샷 없는 legacy 주문은 `총배송비/브랜드수` 균등분배 폴백 — 2026-07-28 이전 주문 전부). 월 버킷은 픽업 이벤트(코드 03) 기준이고 DB 는 `submittedAt ∈ [월초−60일, 월말]` 로만 좁힌다. 청구가 단일 출처는 순수 함수 `buildStatement`, 엑셀은 **요약/일반주문/시딩 3시트**. `publish` 는 rows·합계·xlsx 를 `EfsBillingStatement` 에 **동결**하고 브랜드는 확정본만 본다 — ⚠️ 구버전 동결 스냅샷엔 `kind` 가 없어 읽는 쪽이 `kind ?? 'seeding'` 로 폴백한다. 자세히는 [`docs/server/modules/efs-billing.md`](./docs/server/modules/efs-billing.md).
- **브랜드 취급 품목 = 통관 분류 정본 (2026-07-31):** `Brand.category`(prisma enum `BrandCategory`: `cosmetics` | `dental_materials`, **nullable — `null` = 아직 안 고름**)가 **EFS 24-6 영문 통관 분류 / 24-8 HS 코드의 단일 출처**다. 품목은 **첫 제품보다 먼저** 정해진다 — 서버가 품목 없이는 제품 생성을 거부하고(`assertBrandCategoryChosen`, 단건 + 일괄 초안 양쪽), klow_brand 스튜디오는 **제품 등록 진입점을 누른 순간** `categoryGate` 로 한 번 가로채 품목 선택 화면(`OnboardingCategoryView`)을 띄운다(2026-08-11 전환 — 그전엔 로그인 직후 온보딩 첫 화면에서 강제했다). ⚠️ **로그인 첫 화면에서 묻지 않는 이유**는 브랜드관을 안 쓰고 **시딩·링크만 쓰는 브랜드**까지 평생 안 쓸 값을 강요당하기 때문이다. 진입점은 빈 브랜드관 그리드의 "상품 추가" 펄스 셀과 "브랜드 소개서로 일괄 등록하기" 두 곳이고, 고르면 **즉시 원래 가려던 단계**(개별 업로드 / 소개서 업로드)로 이어진다. 한 번 고르면 다시 묻지 않고, 관문에서 "브랜드관으로 돌아가기"로 등록 자체를 접을 수 있다(그때는 서버값을 안 건드린다). 제품이 0개여도 **우측 대시보드는 가려지지 않는다** — 가리는 건 사용자가 스스로 들어온 집중 단계(관문·개별 업로드·분석)뿐. 기존 브랜드는 `cosmetics` 로 보존돼 재질문 대상이 아니다. 송장/시딩은 `null` 을 화장품으로 폴백하고 warning 만 남긴다(발급이 막히면 안 되므로). 매핑표는 `klow_server/src/common/constants.ts` 의 `BRAND_CATEGORY_CUSTOMS`(화장품 `Cosmetics`/`3304991000`, 치과재료 `Dental Materials`/`3407002000`) 하나뿐이고, **일반 주문 송장·시딩 송장이 같은 규칙**을 탄다. ⚠️ 제품별 오버라이드는 **없다** — `Product.hsCode`/`customsCategoryEn`·`OrderItem`/`SeedingLink` 통관 스냅샷 컬럼은 전부 **dormant**(빌더가 읽지 않음. 2026-06-19 백필이 전 제품 `hsCode` 를 `3304991000` 으로 박아둬서, 제품값을 우선하면 dental 전환이 먹지 않기 때문). 편집은 klow_brand 스튜디오 **브랜드관 탭 > 브랜드 카테고리** + 어드민 브랜드 폼 **취급 품목**. 화장품이 아닌 브랜드는 제품 등록/편집 폼에서 **화장품 카테고리(`categoryKey`) 셀렉트가 숨고** `categoryKey=null` + `category='Dental Materials'` 로 저장된다(klow_web 쇼핑 카테고리 바에는 미노출 — `mist` 선례). 시딩 통관 품명(`SeedingLink.itemName`)도 품목별 후보로 갈리며 **발급 시점에 동결**된다. ⚠️ klow_brand 자동저장은 `useBrandAutoSave.ts` 의 `buildBrandPayload()` **하드코딩 화이트리스트**를 지나므로 새 브랜드 필드는 반드시 거기 등록해야 전송된다. 마이그레이션 `20260730153005_add_brand_category`(ADD COLUMN + DEFAULT) → `20260730160915_brand_category_nullable`(DROP NOT NULL + DROP DEFAULT — 기존 행은 첫 마이그레이션의 DEFAULT 로 이미 `cosmetics` 가 박혀 보존되고, 이후 신규 행만 `null` 로 들어온다). 둘 다 롤링 배포 안전, 백필 없음.
- **시딩 다인원(선착순) 링크 (2026-08):** `SeedingLink.mode`(single|multi) + `maxClaims`(정원 2~500) + `closedAt`(수동 마감). **single = 링크 1개당 1명(기존), multi = 링크 1개로 정원까지 선착순** — 발급 위저드 첫 질문이 방식 선택이고 multi 면 "링크 몇 개" 대신 "최대 몇 명"을 묻는다(링크는 1개만 발급). 신청 단위가 신규 **`SeedingClaim`**(1신청 = 1명 = 1주문 = 1송장)로 빠져 **단일·다인원이 같은 모델**을 쓰고, 브랜드 목록의 발송대기/배송중/시딩기록은 링크가 아니라 **신청자 1명 = 1행**으로 렌더된다(발급/복사 탭만 링크 1행 + `3/10명 신청` 진행 배지·`지금 마감`). 정원의 진실은 **`SeedingClaim` 라이브 COUNT 하나**뿐이고(카운터 컬럼 없음), `reserveSlot()` 이 `SELECT … FOR UPDATE` 로 링크 행을 잠그고 센다 — 만료 예약이 시계만으로 정원에서 빠져 드리프트가 불가능하다. ⚠️ 락 구간은 3왕복까지만 유지하고(동시 신청이 여기서 직렬화된다) Prisma 기본 5초 대신 `SEEDING_TX_OPTS`(20s)를 쓴다. 고객 결제는 체크아웃 진입이 **10분 자리 예약**이며, 정원이 작을 때 이탈 하나가 링크를 얼리지 않도록 **본인 예약 재사용**(게스트 쿠키로 검증한 `resumeOrderId`) + **결제 실패 시 즉시 반납** + TTL 3중으로 막는다. 송장 취소는 그 신청만 취소하고 **자리를 돌려준다**. **중복 신청 차단 (2026-08)**: 정원을 행 수로만 세므로 그것만으론 한 사람이 폼을 반복 제출해 정원을 독식한다 — 같은 링크 안에서 **이메일·연락처·주소 중 하나라도** 겹치면 `422` 로 막는다(비교 대상은 **정원 점유 중인 신청**뿐이라 취소·만료로 자리가 반납되면 재신청도 열린다). 판정은 `reserveSlot()` 의 **행 잠금 안**에서 하고, 그래서 정원 카운트가 `count()` 가 아니라 행 읽기다(한 문장이 행 수와 중복 판정을 겸해 락 왕복 수를 안 늘린다). ⚠️ 그 한 문장은 **`Order` 를 직접 읽고 claim 을 relation 필터로** 쓴다 — `seedingClaim.findMany({ select: { order: … } })` 로 쓰면 Prisma 가 중첩 relation select 를 **2쿼리로 쪼개** 락 구간이 3왕복이 된다(행이 0건이면 1쿼리로 보여 빈 링크 측정에 속는다). 규칙 단일 출처는 `orders/recipient-match.ts` 로 브랜드 발송대기 탭의 중복 수령 **경고**(`attachSeedingDuplicates`)와 원시 정규화를 공유한다(⚠️ 비교 필드는 의도적으로 다름 — 경고는 이름/주소/우편/연락처 각각, 게이트는 이메일·연락처·주소 합성키). ⚠️ **409 가 아니라 422 인 이유**는 klow_web 이 409 를 "선착순 레이스"로 보고 정원 마감 화면으로 넘기기 때문(중복 신청자에게 "정원이 찼습니다"가 오안내된다) — 서버 `code` 규약은 이미 있으나 klow_web `fetchJson` 이 body 를 문자열로 뭉개 못 읽으므로 `code: 'seeding_duplicate_claim'` 은 payload 에 실어만 둔다. 스키마·마이그레이션·백필 없이 `Order` 컬럼에서 즉석 계산하므로 **기존 신청에 자동 소급**. ⚠️ **claim 은 `SeedingLink.countryCode` 를 절대 쓰지 않는다** — 쓰면 첫 신청자의 국가가 링크에 박혀 이후 신청자가 엉뚱한 나라로 발송된다(정본은 `SeedingClaim.countryCode`). 마이그레이션 `20260803081904_add_seeding_claims_multi` 는 추가 전용이라 **롤링 배포 안전**, 백필 `npm run backfill:seeding-claims`(멱등 — **migrate → 백필 → 코드 → 백필 재실행**). 자세히는 [`docs/server/modules/seeding.md`](./docs/server/modules/seeding.md).
- **시딩 통관 신고 수량·신고가 (2026-08-13):** 예전엔 `createLink` 가 `SeedingLink.quantity` 를 **1로 하드코딩**해 박스에 몇 개를 담든 EFS 송장 24-9 가 항상 `"1"` 이었다(payload-builder 는 정상이었고 일반 주문은 실수량을 잘 실었다 — 시딩 경로만 고정). 이제 **수량**은 `seeding/seeding-display-name.ts` 의 **`seedingLineQuantity(link, selectedSkus)` 단일 출처**로 갈린다: `selectionMode='brand'` 는 발급 폼의 **박스당 수량**(1~99, `selection.mode==='brand'` 일 때만 스테퍼 노출), `selectionMode='customer'` 는 **바이어가 고른 제품 개수**(`selectedSkus.length` — 링크 `quantity` 는 1로 박히고 무시). ⚠️ **수량은 무게·배송비와 무관**하다(발급 폼 실무게가 이미 박스 단위라 `feeKrw`·캐리어 분기 불변). **신고총액**의 정본은 브랜드 입력이 아니라 **`ShippingCountry.seedingDeclaredValueUsd`**(USD 센트, 어드민 **국가 설정** 탭에서 국가별 면세 한도보다 조금 낮게 세팅)이고, claim 시점에 목적국으로 조회해 **26번 총액으로 고정**(수량 무관), 단가 24-11 은 **`floor(총액 ÷ 수량)`** 로 파생한다 — ⚠️ 반올림이면 총액이 한도를 넘는다(1001÷3 → 334×3 = 1002). 폴백 3단(국가 설정 → 링크 발급 시 `$8.50~$12.50` 랜덤 → 즉석 랜덤)이라 **미설정 국가는 기존 동작 그대로**. ⚠️ `ShippingService.list()` 를 공개(`/v1/shipping-countries`)와 어드민이 **함께 쓰므로** 신고가는 `COUNTRY_SELECT`(공개)가 아니라 **`ADMIN_COUNTRY_SELECT` + `listForAdmin()`** 에만 있다(기본 select 에 넣으면 통관가가 그대로 공개된다. `onlyEnabled` 플래그에 얹지 않은 것도 같은 이유 — 우연한 결합이 깨지면 곧 유출). ⚠️ `ShippingService.update()` 는 **명시적 허용목록**이라 새 필드를 등록하지 않으면 PATCH 가 조용히 무시된다(typecheck 로 안 잡힘). ⚠️ `displayLines()` 는 펼친 행의 `quantity` 를 **1로 덮는다** — customer 링크는 `OrderItem.quantity` 가 이미 선택 개수라 복사하면 "3줄 × 각 3개 = 9개" 로 제곱이 된다. ⚠️ 유료 checkout 의 **예약 재사용(재진입) 분기가 `OrderItem` 도 다시 써야** 한다 — 안 그러면 "2개 고르고 이탈 → 3개로 재진입"이 옛 수량으로 발송된다. 국가 조회는 기존 `Promise.all`(캐리어·fx)에 얹어 **트랜잭션(정원 락) 밖**에서 끝낸다. 마이그레이션 `20260813055908_add_seeding_declared_value` 는 nullable ADD COLUMN 이라 **롤링 배포 안전 · 백필 없음**(운영팀이 국가를 채우는 만큼 점진 적용). 편집은 어드민 **국가 설정** 탭의 인라인 칸(달러 입력 → ×100 센트 저장, 빈 값 = 미설정) 또는 **엑셀 왕복**(2026-08-13) — `GET /admin/shipping-countries/export`(⚠️ `@Get(':iso2')` **앞**에 있어야 `iso2='export'` 로 안 먹힌다) 가 `시딩신고가` 시트에 **전 국가 1행씩**(값은 달러) 내보내고, 고쳐서 `POST /admin/shipping-countries/import/{preview,apply}` 에 되올린다(미리보기 → 국가 선택 → 적용, 같은 파일을 재파싱해 본 것과 저장되는 것이 갈라지지 않게 한다). ⚠️ **엑셀에 없는 행은 미변경이지만, 행이 있는데 칸이 비면 `null`(미설정)로 지운다** — 무손실 왕복과 "랜덤 폴백으로 되돌리기"를 위해서고, 배송비용 탭 `청구수수료` 시트의 "빈 칸 = 미변경"과 **의도적으로 반대**다(그쪽은 기본값 ₩1,000 이 있어 '미설정' 상태가 없다). 라이터·파서 한 쌍은 `shipping/seeding-declared-xlsx.ts` 가 소유하고 회귀 가드는 `__tests__/seeding-declared-xlsx.spec.ts`. 초기 적재는 `npm run seed:seeding-declared-values`(기본 dry-run, `-- --apply`, `--overwrite` 없으면 미설정국만 — 어드민 수동 편집 보존, 멱등) + `prisma/data/seeding_declared_values.json`(각국 **소액면세 한도**만 적고 신고가는 스크립트가 `min(한도−$5, $100)` → $5 단위 내림으로 파생). ⚠️ **de minimis 는 대개 관세 한도이고 부가세는 별개**라 `vatFromZero` 축이 따로 있다 — EU(관세 €150 면제·VAT €0부터)·영국·호주·싱가포르·스위스는 신고가를 올릴수록 **수령인 세금만** 늘어 **$10 고정**이고, 한도가 회피 불가능하게 낮은 나라(인도네시아 US$3)도 같은 칸이다. ⚠️ **JSON 은 규제값 스냅샷이라 낡는다** — `confidence:'unverified'`(**US**: $800 de minimis 2025년 중단 / CN·RU·TR: 최근 수차례 변경)는 비워둬 스킵되고, 미수록국도 전부 랜덤 폴백이라 **안 채워도 안전**하다. 자세히는 [`docs/server/modules/seeding.md`](./docs/server/modules/seeding.md) + [`docs/server/modules/shipping.md`](./docs/server/modules/shipping.md).
- **Shipping & EFS 송장:** 한 브랜드 = 한 EFS 송장. 한 주문이 N 개 브랜드 제품을 담으면 결제 배송비도 브랜드별 무게 요율의 합(위 무게별 요율 참고), EFS 송장도 N 장(`Shipment` 1개 + `ShipmentItem` N개 라인). 각 브랜드가 자기 송장만 EFS 창고로 발송하기 위함. payload-builder 가 24번 itemCapsule 을 `{...},{...}` multi-item 으로 묶고, 27번 배송비는 송장별 균등 안분(총 / brandCount). 어드민 발급은 `(orderId, brandId)` 그룹 단위(`/admin/shipments/order/:orderId/brand/:brandId`). 자세히는 `docs/architecture.md` Shipment 섹션 참고.
- **Payment (Eximbay):** 결제 통화는 USD. 흐름은 `POST /v1/orders` (동의 3종 + IP + 시점 fxRate snapshot 저장) → `POST /v1/payment/prepare` (동의·ownership 재검증, fgkey echo) → klow_web 에서 Eximbay JS SDK → `return_url` 303 → `/checkout/redirect` → `POST /v1/payment/verify` (Eximbay 재조회 + fxRateSnapshot 기준 금액 검증 + paymentStatus 멱등 전이). 보강 경로: `POST /webhooks/eximbay` (외부 IP 화이트리스트 `EXIMBAY_WEBHOOK_IPS`). 실패 보고: `POST /v1/payment/report-failure` 가 pending→failed 멱등 전이. 환불은 `payment.refundOrder` 가 Eximbay `/v1/payments/{pgTid}/cancel` 호출. PG 심사 안전망은 약관 동의 4 체크박스(Zod `literal(true)` + service 재가드), 동의 시각·IP·fxRate 저장, 이중언어 약관(`/legal/[slug]?lang=ko`) 으로 구성. 자세한 흐름은 `docs/architecture.md` Payment system 섹션 참고.
- **결제 확정 3중 방어선 (2026-08-17):** `pending → paid` 전이는 `markPaid()` 한 곳뿐인데, 예전엔 거기 도달하는 경로가 둘(클라 verify · 웹훅)이고 **둘 다 실패했을 때 복구하는 장치가 없어** 카드는 승인됐는데 주문이 영원히 `pending` 으로 남았다. ⚠️ **핵심 결함은 `return_url` 이 klow_web 을 가리킨 것**이다 — 303 으로 넘어간 `/checkout/redirect` **클라이언트 JS** 가 `POST /v1/payment/verify` 를 한 번 더 호출해야 확정됐고, 그 호출은 `api.payment.verify(qs).catch(() => {})` 로 **fire-and-forget + 에러 전부 삼킴**이었다(2026-06-22 `1acabdb` "optimistic 이동" 이후). 확정 실패가 흔적 없이 사라지고 손님은 언제나 성공 화면을 봤다. **현장(`channel='onsite'`) QR 결제가 특히** 취약했다: ① QR→인앱 브라우저라 카드앱(3-DS) 왕복에서 컨텍스트를 잃고 ② 부스에선 "승인" 뜨면 손님이 폰을 집어넣으며 ③ `verify` 가 **IP 기준 5회/분**이라 부스 공용 NAT 뒤 6번째 결제부터 429였고 ④ 송장·알림톡을 스킵하는 채널이라 하류에서 아무도 모르고 어드민 목록은 미결제를 기본 숨김하며 **정산(`paymentStatus:'paid'`)에서도 빠져 브랜드 매출이 통째로 누락**된다. 이제 **(1) `POST /payment/return`**(klow_server, `public-payment-return.controller.ts` — Eximbay 리턴 POST 를 서버가 직접 받아 그 요청 안에서 확정하고 klow_web `/checkout/redirect?<원본qs>&klow_verified=1` 로 303. 원본 qs 를 그대로 넘기는 이유는 시딩 복귀·부스 성공화면·결제상품만 카트 제거가 브라우저 `sessionStorage` 에만 있는 정보를 쓰기 때문) **(2) 웹훅**(status_url) **(3) `payment-reconcile` 크론(15분)** 3중이다. 3번은 `POST /v1/payments/retrieve` + `key_field='order_id'` 로 **우리 주문번호만으로** 조회한다(`pgTid` 를 모르는 주문에 `verify` 는 못 쓴다) — `payment.status` 가 `SALE`/`AUTH` 면 **기존 `markPaid` 를 그대로 태워** 금액검증·멱등·onsite `completed`·메일·시딩·송장을 재사용하고, `REGISTERED`/`NONE` 은 건드리지 않는다. ⚠️ **금액 불일치는 자동 확정하지 않는다**(`mismatch` — 통화 데싱크는 사람이 판단할 문제). ⚠️⚠️ `/payment/return` 은 **`main.ts` Origin CSRF 가드 예외**에 반드시 있어야 한다(Eximbay form POST 엔 Origin 이 없어 빠뜨리면 **전 결제 403**), 그리고 `/payment/return`·`/webhooks/eximbay` 는 **`@SkipThrottle()`** 이다(확정 경로가 자기 rate limit 에 걸리면 버그 재발). 곁들여 고친 것: `parseFloat("132,000")===132` 라 콤마 섞인 KRW 금액이 **영구 고착**(웹훅도 같은 `markPaid` 를 타 재시도해도 실패)을 만들던 것을 `parsePgAmount()` 로 정규화, 웹훅 IP 비교의 `::ffff:` 정규화, 운영 부팅 가드를 "비어있지 않음"에서 **샌드박스 IP 검출 + `EXIMBAY_RETURN_BASE_SERVER`/`FRONTEND_URL` https 강제**로(`.env.example` 기본값이 샌드박스 IP 4개라 그대로 배포되면 콜백 전부 403 인데 warn 한 줄만 남았다), 조용하던 분기 전부 Sentry 배선. 과거 고착분은 `npm run backfill:reconcile-payments`(dry-run 기본, `-- --apply`, 멱등, `--onsite`/`--days`/`--order`), 건별 복구는 어드민 주문 상세 **결제 재확인**(`PATCH /admin/orders/:id/reconcile-payment`). **스키마·마이그레이션 없음.** 회귀 잠금은 `payment/__tests__/payment-reconcile.spec.ts`(콤마 금액 + onsite reconcile → paid+completed + mismatch 는 비전이). ⚠️ 새 cron 이라 `test/app.e2e-spec.ts` 기대 목록이 **6개**로 늘었다. 배포 순서는 **klow_server 먼저**(구 결제창은 옛 `return_url` 로 돌아오므로 klow_web 의 폴백 verify 경로를 당분간 남겨뒀다). 자세히는 [`docs/server/modules/payment.md`](./docs/server/modules/payment.md).
- **송장 발급 실패 알림(SMS) + 제한적 자동 재시도 (2026-08-18):** EFS 송장 **자동** 발급 실패가 **완전 무음**이던 것을 고쳤다. ⚠️ **근본 원인은 `executeCreate` 가 EFS 실패를 throw 하지 않고 `status='failed'` row 를 정상 리턴하는 것**이다 — 그래서 결제(`payment.service`)·시딩(`seeding.service`)이 각자 갖고 있던 `createShipmentsForOrderSafe` 의 try/catch 에 **EFS 실패가 애초에 도달하지 않았고**(그 catch 는 송화인 미입력 같은 pre-validation throw 만 잡는다), `ShipmentsService.logger` 는 선언만 되고 한 번도 호출되지 않아 서버 로그에도 흔적이 없었다. 자동 재시도도 **아예 없었다**(큐·cron·attempt counter 전무 — 복구가 100% 어드민 수동 클릭). 이제 **(1) 알림**: 발화의 단일 출처 `ShipmentsService.createForOrderSystem(orderId, reason)` 이 위 중복 래퍼 2개를 합쳐 **반환값의 status 를 직접 검사**하고(절대 throw 안 함), `shipment-alert.service.ts` 가 `SmsService.sendText` 로 **`Admin.shipmentAlertEnabled && phone != null`** 인 어드민에게 문자를 보낸다(**주문 1건 = 문자 1통**, 여러 브랜드 실패는 `Acme 외 2건` 합산). ⚠️ **어드민 수동 발급/재시도는 알림 대상이 아니다**(화면에 `ErrorDetailModal` 이 바로 뜨므로 자기 클릭마다 자기 폰으로 문자가 온다). ⚠️ "이미 전부 발급됨"은 전용 예외 **`ShipmentsAlreadyIssuedException`** 으로 나른다 — 중복 호출에서 정상적으로 나는 예외라 실패로 세면 문자가 스팸이 되는데 **메시지 문자열 비교로 판정하면 문구 한 글자에 깨진다**(어드민 경로는 여전히 같은 문구의 400). **(2) 자동 재시도**: `shipment-retry.cron.ts`(`*/10 * * * *` KST, `SHIPMENT_RETRY_CRON_ENABLED=false` 로만 비활성)가 `retryDue()` 를 부르고 백오프는 **5분→20분→60분**, 상한 3회. ⚠️ **재시도 자격을 `errorMessage` 파싱이 아니라 `executeCreate` 의 두 분기로 가른다** — `efsResult.success===false`(EFS 명시 거부, 재시도해도 동일) → `nextRetryAt=null` / catch 절(네트워크·타임아웃·ack 실패) → 백오프 예약. EFS 회신 문구는 우리가 통제하지 않아 파싱이 언제든 깨지지만 이 축은 구조가 이미 나눠 놓았다. **무한 재시도 방지 3중**: `retryCount<3` · 성공/거부 시 `nextRetryAt=null` · 배치 `LIMIT 20`. ⚠️ **카운터는 호출 前에 올린다**(도중에 죽어도 같은 건을 무한히 다시 집지 않는다). ⚠️ **어드민 수동 `retry()` 는 `retryCount` 를 0 으로 리셋**한다 — 안 하면 3회 소진한 송장은 사람이 원인을 고쳐 준 뒤에도 자동 복구가 영영 안 붙는다. **(3) `pending` 고착 해소**: EFS 호출 중 프로세스가 죽으면 row 가 `pending` 에 남는데 `isReissuableStatus`(failed|cancelled)도 `listPendingGroups`(pending 제외)도 못 잡아 **어느 화면에도 안 뜨고 재발급도 못 하는 고아**가 됐다 — `retryDue()` 가 `updatedAt < now−30분` 인 pending 을 `failed`+`nextRetryAt=now` 로 승격해 **기존 재시도 경로가 주워 가게** 한다(⚠️ 전용 복구 경로를 따로 만들면 국내배송 완료 훅 때처럼 사각지대가 생긴다). **어드민 전화번호**는 `Admin.phone`(digits-only, 검증은 `common/validation/shared.ts` 의 `KrPhone` — 브랜드 전화 OTP 와 **공유**하려고 `brand-auth.ts` 에서 올렸다)이고 편집은 `/admins` **프로필 수정 모달**(SuperAdminGuard)이며, 수신 대상 토글은 출고관리 **'실패' 탭**(`GET|PUT /admin/shipments/alert-recipients`, **AdminGuard** — super 전용이면 operator 가 자기 화면 설정을 못 본다). ⚠️ 두 라우트는 **`@Get(':id')` 앞**에 둬야 한다(`shipping-countries/export` 선례). ⚠️ zod `phone` 에 **`.default()` 금지** + `updateProfile()` **명시적 허용목록** 등록 필수(빠뜨리면 PATCH 가 조용히 무시된다). ⚠️ `setRecipients` 는 **번호 없는 어드민을 400 으로 거절**한다("체크는 켜져 있는데 아무에게도 안 가는" 상태 = 지금 고치려는 무음 실패와 같은 병). 수신자 변경(`PUT`)은 **SuperAdminGuard** 다 — 읽기만 AdminGuard(operator 가 현황은 봐야 한다). 쓰기까지 열면 operator 가 남의 `Admin` 행을 고칠 수 있는 첫 경로가 생기고, 그 권한으로 **전원 수신을 꺼버릴 수 있다**(이 기능이 막으려는 무음 상태로 그대로 되돌아간다). 마이그레이션 `20260818100637_add_admin_phone_and_shipment_retry` 는 **추가 전용 → 롤링 배포 안전 · 백필 없음**. ⚠️ 새 cron 이라 `test/app.e2e-spec.ts` 기대 목록이 **7개**로 늘었다. 회귀 잠금은 `shipments/__tests__/{shipment-retry,shipment-alert}.spec.ts`. 배포 순서는 **klow_server → klow_admin**이고, **배포 직후 `/admins` 에서 번호를 채우고 실패 탭에서 수신자를 체크해야 실제로 문자가 간다**(둘 중 하나라도 비면 조용히 아무에게도 안 간다). 자세히는 [`docs/server/modules/shipments.md`](./docs/server/modules/shipments.md) + [`docs/server/modules/admin-auth.md`](./docs/server/modules/admin-auth.md).
- **Brand 입점 워크플로우 (구독 게이트 도입 후):** `Brand` 모델에 `status`(draft/pending/approved/rejected/withdrawal_pending/withdrawn) + `submittedById`/`submittedAt`/`approvedAt`/`approvedById`/`rejectionReason` + `pgCustomerKey`(결제 준비 게이트 플래그). `Product` 도 `status`(pending/approved/rejected) + `submittedById`/`submittedAt`/`rejectionReason`. **노출·판매는 단일 게이트 — `PURCHASABLE_PRODUCT_WHERE = PUBLIC_PRODUCT_WHERE`** (`product-selects.ts`). 가입 brand (`submittedById !== null`) 는 `BrandSubscription.status='active'` 동안만 노출/판매, 어드민이 직접 만든 brand (`submittedById === null`) 와 legacy (`brandId === null`) 제품은 구독 게이트 면제. 결제 = 자동 승인이라 어드민 검수 큐는 없다 — 대신 `/admin/brand-subscriptions/*` (구독 관찰 + 강제 해지·환불·정책 위반 reject + 제품 단위 차단). `klow_brand` 의 OnboardingGate 에서 송화인 4 + 계좌 3 필드를 저장하면 `submitForReview()` 가 `pgCustomerKey` 를 발급해 응답하고, 그 뒤 **NicePay 포스타트 빌링**(결제창 없음 — `CardEntryForm` 카드 입력 폼)으로 받은 카드정보(cardNo/expYear/expMonth/idNo/cardPw)를 `POST /v1/brand/subscription/start` 로 보내면, 서버가 secretKey 로 AES 암호화한 `encData` 로 `POST /v1/subscribe/regist`(빌키발급) + `POST /v1/subscribe/{bid}/payments`(첫 결제) + `approveApplication()` 까지 한 트랜잭션. 빌키(`bid`)는 `BillingKey.pgBillingKey`, 거래번호(`tid`)는 `SubscriptionInvoice.pgTid`, `provider='nicepay'`. 정기 청구는 KST 자정 cron (`subscription-billing.cron.ts`) 이 `POST /v1/subscribe/{bid}/payments` 로 처리하고 dunning 은 0/1/3/7 일 (4회 실패 시 `past_due` 확정). PG 어댑터는 `nicepay-billing.adapter.ts`. **카드 원문이 서버를 거치므로(PCI) 저장/로깅하지 않는다.** 자세한 흐름은 [`docs/brand-subscription.md`](./docs/brand-subscription.md) 참고 (2026-06 KCP→NicePay 교체 배너).
- **어드민 대시보드 개편 + 브랜드 접속 추적 (2026-08):** 홈 KPI 가 **구조적으로 우상향만 보이던 것**을 걷어냈다 — 주별 **누적** 선차트(`/admin/stats/export-weekly`)와 거기서 파생된 `avgWeeklyGrowthPct` 를 제거하고, **수출·구독 둘 다 주별 비누적 막대 + 4주 이동평균**(`/export-trend`, `/subscription-trend`, 최근 26주, 공용 컴포넌트 `WeeklyTrendChart`)으로 교체했다. **마지막 주는 아직 진행 중이라 `partial:true` + `ma:null`** 로 내려 막대를 연하게 칠하고 평균선을 끊는다(안 그러면 미완성 주가 늘 하락으로 오독된다). 라벨 정정: "누적 총수익"→**브랜드 구독매출**(`revenueTotalKrw` — 구 값은 건당 ₩1,500 하드코딩 시딩 이익이 섞여 있었고 그 상수·파생 `total*Krw` 는 폐기), "가입 브랜드"(실제로는 `BrandUser.count()`)→**구독중인 브랜드수**(`activeCount`, `brandCount` 필드 삭제). 신규 **`/admin/stats/brand-activity`** 는 구독중(active/past_due) 브랜드의 마지막 활동으로 활성(≤7일)/저활성(8~30일)/휴면(>30일 또는 기록 없음) 세그먼트 + "관리 필요" 목록을 낸다. ⚠️ **활동 소스는 브랜드가 직접 만든 타임스탬프 8종으로만 한정**하고 `Promotion.updatedAt`(**방문자 클릭**이 `clickCount` 를 올리며 bump)·`Product.updatedAt`(어드민 편집 + `backfill:product-tags-english` 가 전 제품 bump 한 전례)·`Brand.updatedAt`(어드민 승인/편집)·`SeedingClaim`(바이어 행동)은 **의도적으로 제외** — 넣으면 브랜드가 아무것도 안 했는데 활성으로 뒤집힌다. 접속 축은 `getSession()` 이 5분 스로틀로 찍는 **`BrandUser.lastSeenAt`** 이 정본이다(`BrandSession.lastSeenAt` 도 함께 찍지만 세션은 만료 시 하드 삭제 + TTL 7일이라 그것만으론 "20일 전 접속"과 "미접속"이 구분 안 됨). 마이그레이션 `add_brand_last_seen` 은 **추가 전용이라 롤링 안전 · 백필 불가(전진 채움만)** — `lastLoginAt: null` 은 미접속이 아니라 데이터 없음이라 UI 가 집계 시작일(`loginTrackingSince`)을 대신 표시한다. 자세히는 [`docs/server/modules/stats.md`](./docs/server/modules/stats.md).
- **브랜드 자사몰(카페24) 임베드 버튼 (2026-08-12):** 브랜드가 이미 운영 중인 국내 자사몰 상품 페이지에 **"해외배송으로 구매" 버튼**을 달아 해외 손님을 그 상품의 KLOW PDP(`/product/{id}?brand={slug}`)로 보낸다 — 도착 후엔 **기존 담기/카트/체크아웃 흐름을 그대로** 타므로 신규 결제 경로가 없다. 설치는 2가지 병존: **(A) 스킨에 두 줄** — 버튼 자리 `<div id="klow-buy" data-code="{$product_no}">` + `<script src="{API}/embed/v1.js" data-brand="{slug}" defer>`(매핑된 상품에만 자동 노출) + **(B) 제품별 링크/HTML 스니펫 수동 삽입**(스킨 편집 권한이 없는 브랜드용, 스크립트 불필요 — 둘 다 넣으면 버튼이 2개). ⚠️ **(A)에서 마운트 div 를 "선택"으로 안내하면 안 된다** — 스크립트가 구매 버튼을 자동 탐지하긴 하지만 스킨 복불복이다(실측: 카페24 기본 `base` 스킨은 `btnSubmit sizeL` 이라 후보 셀렉터 전부 미스, 커스텀 `detail_B_b` 만 `.btn_buy` 로 걸림). 또 상품상세 템플릿은 `<!--@layout-->` 모듈 조각이라 `</body>` 가 없어 스크립트는 파일 맨 끝에 넣고, 마운트 div 는 `module="product_detail"` 범위 안(중첩 `module="product_action"` 바깥)에 둬야 `{$product_no}` 가 채워진다. 매핑 정본은 **`Product.externalProductCode`**(`@@unique([brandId, externalProductCode])`) — 카페24 **상품번호(`product_no`)** 를 넣는다(상품상세 URL 에서 스킨 수정 없이 읽히는 유일한 값). ⚠️ URL 형태가 둘이고 **SEO형이 현실의 기본값**이다 — 쿼리형 `?product_no=21` / SEO형 `/product/{상품명}/21/category/1/display/10/`. 브랜드 안내에 쿼리형만 보여주면 자기 주소창에 그 글자가 없어 숫자를 못 찾는다(스크립트는 둘 다 읽는다). ⚠️ **컬럼은 반드시 nullable + default 없음** — `@default("")` 를 주면 미매핑 제품이 전부 `(brandId, '')` 로 같아져 브랜드의 **2번째 제품 생성부터** P2002 로 죽는다(Postgres 는 unique 인덱스에서 NULL 만 서로 다르게 취급). 서버 zod 도 `'' → null` 로 정규화하고 **`.default()` 를 붙이지 않는다**(PATCH 마다 주입돼 남의 코드를 덮는다). 서버 표면은 `/v1` 밖 **`/embed/*`**(공개·GET 전용·쿠키 없음·CORS `*`): `GET /embed/v1.js` + `GET /embed/v1/resolve?brand=&code=`. ⚠️⚠️ **resolve 는 영구히 CORS simple request** 여야 한다 — 전역 `enableCors` 가 OPTIONS 를 가로채면서 비화이트리스트 origin(=모든 브랜드 자사몰)엔 ACAO 를 안 붙이므로, 커스텀 헤더 하나만 추가해도 **전 브랜드 버튼이 동시에 죽는다**(그래서 `main.ts` 는 안 건드리고 컨트롤러에서 `res.setHeader` — `res.append` 는 ACAO 중복으로 브라우저 전면 거부라 금지). 스크립트는 **`.js` 파일이 아니라 `embed-script.ts` 의 문자열 상수**다 — `nest-cli.json` 에 `assets` 설정이 없어 raw `.js` 는 `dist/` 에 안 실리고 **프로덕션에서만 404** 가 난다. `/embed/v1.js` 는 브랜드 스킨에 박혀 우리가 못 고치므로 **v1 영구 유지 + immutable 캐시 금지**(5분), 깨는 변경은 `/embed/v2.js` 신설. resolve 는 `PUBLIC_PRODUCT_WHERE` 를 **`AND: [...]` 로 감싸** 타므로(스프레드로 합치면 `OR`/`AND` 키 충돌로 게이트가 통째로 사라진다) **구독이 끊기면 자사몰 버튼이 전부 자동으로 사라진다**(별도 킬스위치 없음 — CS 가 모르면 "갑자기 버튼이 없어졌어요"를 못 푼다). 상품코드 해결은 `data-code` → `#klow-buy` → **`?product_no=`(주력)** → SEO URL → `window.iProductNo` 사다리이고 **전부 실패하면 침묵**한다(브랜드관 폴백 금지 — 엉뚱한 상품으로 보내는 게 최악). 버튼은 **Shadow DOM(closed) 격리** + `navigator.language` 기준 en/ja/zh/vi/th/id/ru(**한국어 없음** — 한국어 독자는 그 자사몰에서 사면 된다). ⚠️ 카페24는 **PC/모바일 스킨이 별도 템플릿**이라 양쪽에 붙여야 한다(해외 트래픽 대부분이 모바일). 브랜드 UI 는 스튜디오 **링크 탭 > 자사몰** 서브탭(`Cafe24Section`) **한 곳뿐**이다 — 제품 편집 폼에도 같은 입력을 뒀다가 뺐다(두 화면이 같은 값을 서로 다른 규칙·저장 경로로 다뤄 중복 검사 유무와 에러 문구가 갈렸고, 폼 전체저장은 형식 검증 없이 보내 400 하나가 제품 저장 전체를 막았다). 상품코드는 공개 응답에서 벗긴다(`StrippedPricingKeys` — ⚠️ `findOne` 이 `select` 가 아니라 `include` 라 여기 빠뜨리면 그대로 샌다). 곁들여 **klow_web PDP 가 `?brand=` 유입 게스트에게 국가 선택 모달**을 띄우게 고쳤다(그전엔 온보딩 자동 노출이 `/shop`·브랜드관뿐이라 임베드/프로모션 링크로 PDP 에 직접 온 해외 손님이 **US/en 으로 조용히 굳었다** — 게이트는 `onboardedAt` 이 아니라 `country` 유무). 마이그레이션 `add_product_external_code` 는 nullable ADD COLUMN 이라 **롤링 배포 안전 · 백필 없음**. 자세히는 [`docs/server/modules/embed.md`](./docs/server/modules/embed.md).
- **Instagram 연동 (댓글→DM, 2026-07):** 브랜드가 klow_brand **인스타그램** 탭에서 자기 IG 비즈니스/크리에이터 계정을 연동("Instagram API with **Instagram Login**" — Facebook 페이지 불필요)하고, 자기 포스팅 댓글에 **브랜드관 링크(klow.kr/{slug})가 담긴 메시지 템플릿**으로 **일괄 DM(private reply)** 을 보내 댓글 단 사람을 브랜드관으로 유입시킨다. DM 은 댓글 작성자가 먼저 DM 한 적이 없어 일반 DM 불가 → **private reply(`recipient:{comment_id}`)가 유일 경로**(댓글당 평생 1회 · 댓글 후 7일 이내 · 750/hr per-account). 60일 장기토큰은 AES-256-GCM(`META_TOKEN_ENCRYPTION_KEY`) 암호화 저장 + **`InstagramRefreshCron`(매일 KST 03:00)이 만료 전 롤포워드 → 재인증 없이 영구 연동**(브랜드 로그인 세션과 무관). 멱등성은 `InstagramReply.commentId @unique`. 공동작업 게시물은 publish 계정만 접근 가능(collaborator 초대분은 댓글 빈 상태). **⚠️ 실제 브랜드 사용은 Meta App Review + Business Verification(Advanced Access) 통과 후** — 승인 전엔 앱 tester role 테스트 계정만 동작(코드는 동일). 모델 `InstagramConnection`/`InstagramTemplate`/`InstagramReply`. 자세히는 [`docs/instagram-integration.md`](./docs/instagram-integration.md) + [`docs/server/modules/instagram.md`](./docs/server/modules/instagram.md).
- **랜딩 상담 문의 폼 (2026-08-10):** klow_brand 랜딩 최종 CTA "상담 문의"가 `mailto:team@klow.kr` 링크에서 **모달 폼**으로 바뀌었다(`klow_brand/src/components/ContactModal.tsx` — 브랜드명·연락처·이메일 필수 + **문의내용은 선택**). 제출은 공개 엔드포인트 `POST /v1/contact-inquiries`(신규 `klow_server/src/modules/contact/`, 가드 없음 + `THROTTLE_TIGHT` 5회/분)로 가고, 서버는 **저장하지 않고** `EmailService.sendBrandContactInquiry` 로 `CONTACT_INBOX_EMAIL`(기본 `team@klow.kr`)에 Resend 메일만 보낸다 — 문의자 이메일은 `replyTo` 에 실려 받은편지함에서 바로 회신된다(`send()` 에 `replyTo?` 를 이때 추가). 저장이 없으므로 **발송 실패는 500 으로 그대로 올린다**(조용히 성공을 반환하면 문의가 유실). ⚠️ 네 필드 전부 미인증 공개 입력이라 메일 템플릿은 모든 값에 `escapeHtml()` 을 적용하고 `\n → <br>` 치환은 **이스케이프 후**에 한다. 모달은 CTA 클릭(`data-contact-cta` 위임) 외에 **딥링크 `?contact=1`** 로도 열려 블로그·SNS 에 `https://brand.klow.kr/?contact=1` 을 그대로 붙일 수 있다(`?login=1`·`?signup=guide` 와 동일 규약). ⚠️ 랜딩의 로그인 자동 `/studio` 리다이렉트는 `?contact=1` 일 때 **건너뛴다** — 안 그러면 로그인된 브랜드가 블로그 링크를 눌러도 모달을 보기 전에 튕긴다. 마이그레이션·DB 변경 없음. 자세히는 [`docs/server/modules/contact.md`](./docs/server/modules/contact.md).
- **크리에이터 / 릴스 영상 기능 제거 (2026-08-10):** 초기 TikTok형 콘텐츠 커머스 축이던 **크리에이터·릴스 영상**을 3개 레포에서 전면 삭제했다. 서버 `modules/creators`·`modules/videos`(라우트 15개: `/admin/creators*`, `/v1/creators*`, `/admin/videos*`, `/v1/videos*`) + 호출자가 0건이던 `GET /admin/stats`, 어드민 `크리에이터`·`릴스 영상` 탭(+`CreatorForm`/`CreatorEditor`/`VideoForm`/`CreatorPreviewPanel`/`ProductPicker`), klow_web `/discover`·`/feed`·`/videos`·`/creator/[id]`(+훅 7개·`components/{video,creator}`·PDP 의 "Videos featuring this product" 섹션)이 대상이다. 하단 탭바는 **shop/cart/my 3탭**으로 줄었고 온보딩 국가 모달 자동 노출도 `/shop` 한 곳만 남았다. ⚠️ **이름이 겹치는 생존자 3종**: `/v1/brand/creators`(=`curated-influencers` 모듈, klow_brand 크리에이터 찾기 + 어드민 인플루언서 탭), `Product.videoClipUrl`·업로드 `kind:'video'`(제품/브랜드 미디어), klow_web `components/discover/{RecommendedProducts,SectionHeader}`+i18n `discover.recommended.*`(`/shop` 추천 섹션 — 폴더명만 discover 다). `common/validation/creator.ts` 는 큐레이션 스키마만 남아 **`curated-influencer.ts` 로 rename**. 예약 슬러그(`discover`/`feed`/`videos`/`creator`)는 **일부러 남겼다** — 풀면 브랜드가 선점한다. 마이그레이션 `remove_creators_videos` 는 `Creator`/`Video`/`VideoProduct` **DROP TABLE 이라 롤링 배포 비안전**(단일 레플리카 컷오버) + 데이터 영구 삭제. 배포 순서는 프론트 2개 → 서버 코드+마이그레이션.
- **국내(한국) 시딩 — 브랜드 자체배송 (2026-08-13):** 브랜드가 **국내 크리에이터에게도** 샘플을 보낼 수 있게 시딩 배송국에 **대한민국**을 열었다. 국내는 브랜드가 자기 택배를 쓰므로 **EFS 송장을 발급하지 않는다** — `ShippingCarrier` 에 **`DOMESTIC`** 을 추가하고 KR `ShippingCountry` 행(`enabled:false` · `productCarrier:'DOMESTIC'`)을 뒀다. ⚠️ **KR 이 원래 없던 건 코드가 막아서가 아니라 시드 데이터에 없어서**다(KR 은 출발지 `SHIPPER_COUNTRY` 라 요율 엑셀이 전부 수출용). 그 행은 `carriers_by_country.json`(엑셀 산출물)이 아니라 **`prisma/seed/seed-shipping-countries.ts` 의 `DOMESTIC_COUNTRIES` 가 소유**하고 update 절에서도 값을 강제한다 — ⚠️ **마이그레이션에 넣을 수 없다**(Postgres 는 `ALTER TYPE … ADD VALUE` 로 추가한 enum 값을 같은 트랜잭션에서 못 쓰고 Prisma 는 마이그레이션을 트랜잭션으로 감싼다). 한국 claim 도 **진짜 `Shipment` 행을 만들되**(발송대기 조인·발송처리 버튼·중복수령 경고·주문 상태 전이가 전부 이 행을 읽는다 — 없으면 어느 화면에도 안 뜬다) `executeCreate` 가 송화인 검증·payload·EFS 호출을 건너뛰고 바로 `submitted`(`efsTrackingNumber=null`)로 쓴다. 취소도 EFS 를 안 부르고 상태만 되돌리되 **시딩 캐스케이드(주문 취소 + 정원 반납)는 유지**한다. ⚠️ **"송장 발급 후 취소로 발송 기록을 남기는" 방식은 쓰면 안 된다** — `performEfsCancel` 이 시딩 주문을 `cancelled` 로 만들고 정원을 반납해 **발송을 마친 건이 취소됨으로 기록**되고 다인원 링크 자리가 다시 열린다. **브랜드 결제(무료) 링크 전용**이고(`createLink` 가 고객 결제+국내 조합을 400 — 국내는 KLOW 요율이 없고 정산이 EFS 배송완료 코드에 묶여 있어 유료로 열면 조용히 미정산된다), **일반 스토어 체크아웃엔 노출되지 않는다**(`enabled:false`). 브랜드 결제 링크는 국가를 비워 발급하므로 **바이어가 claim 시 한국을 직접 고르고**, 따라서 KR 은 모든 무료 링크의 선택지에 뜬다(링크를 누구에게 보낼지는 브랜드가 정하므로 링크 단위 opt-in 은 두지 않았다). 발송 처리를 누르면 EFS 추적이 없으므로 종착 술어 **`isTerminalShipment`**(EFS=배송완료 코드 / 국내=브랜드 발송 확인)를 통해 기존 `maybeMarkOrderCompleted` 가 주문을 `completed` 로 보내고, 브랜드 화면은 **배송중을 건너뛰고 바로 시딩기록**으로 간다(되돌리기는 대칭). ⚠️ **국내 전용 완료 훅을 따로 만들면 국내·EFS 혼재 주문이 양쪽 어디에도 안 걸려 영영 `shipped` 에 갇힌다** — 술어 한 곳에 두는 게 핵심이다. ⚠️ `ProductCarrierEnum` 에서 **`DOMESTIC` 을 반드시 제외**한다(파생 enum 이라 안 빼면 어드민이 아무 국가나 국내배송으로 바꿔 그 나라 주문이 송장 없이 나간다). ⚠️ `payload-builder` 는 `Record<Exclude<ShippingCarrier,'DOMESTIC'>,…>` + throw 로 **국내 건이 EFS 로 새는 걸 타입으로 차단**한다. `SeedingRate` 요율표 행은 만들지 않고(국내는 KLOW 요율이 없다) `supportedCountries()` 가 교집합 대신 **합집합**으로 국내국을 포함하며 `isDomestic` 플래그를 함께 내린다. 프론트 주의: 발송대기 행의 `selectable`(바코드 인쇄=송장번호 보유)과 `shippable`(발송처리=송장 보유)은 **다른 플래그**이고(합치면 송장번호 없는 국내 건이 발송대기에 갇힌다), 스튜디오 배송 상세의 **EFS 인천 GDC 입고지 안내는 국내 건에서 숨긴다**(안 그러면 브랜드가 박스를 우리한테 보낸다). 마이그레이션 `add_domestic_shipping_carrier` 는 enum 값 추가뿐이라 **롤링 배포 안전**하고 백필도 없지만, 구 인스턴스는 `DOMESTIC` 을 역직렬화 못 하므로 **순서는 마이그레이션 → 코드 배포 → `npm run seed:shipping-countries`** 다. ⚠️ klow_web 에 `ko` locale 이 없어 **한국 신청자는 `/seed` 를 영어로 본다**(별도 과제). 자세히는 [`docs/server/modules/shipments.md`](./docs/server/modules/shipments.md) 국내 자체배송 항목 + [`docs/server/modules/seeding.md`](./docs/server/modules/seeding.md) + [`docs/server/modules/shipping.md`](./docs/server/modules/shipping.md).
- **프로모션(구 캠페인) = 인플루언서 "할인가 브랜드관 링크" 전환 (2026-08-17):** 이 기능이 어필리에이트 추적 도구에서 **브랜드가 제품×국가별 판매가를 직접 정하는 할인 링크**로 바뀌었다. 발급 모달은 이제 **이름 + 제품별·국가별 판매가**만 받는다(SNS 플랫폼·계정주소·메모 입력 제거). 저장 정본은 신규 **`PromotionProductPrice(promotionId, productId, iso2, priceLocal)`** — 값 모양은 `ProductOnsiteCountryPrice` 와 동일(현지통화 major·non-null·상속 국가는 행 없음)하고 프로모션 단위 **replace-all** 이다. 구 `Promotion.{snsUrl,platforms,countries,discountPct,memo}` 5컬럼 + `PromotionPlatform` enum 은 **드롭**했고 기존 할인 데이터는 백필 없이 버렸다. ⚠️⚠️ **병합 규칙이 바뀌었다 — 프로모션 세일가가 국가 핀·국가 할인을 outright 이긴다(`max` 아님).** 구 모델은 `max(국가할인%, 프로모션할인%)`(고객 유리)였지만 이제 브랜드가 금액을 명시 지정하므로 그 값이 정본이고, 취소선은 프로모션 전 정상가를 그대로 쓰며 표시 할인율은 `round((1−청구가/정상가)×100)` 로 파생한다(세일가가 정상가보다 비싸면 **0 클램프** — 음수 % 방지). ⚠️ **세일가를 안 정한 제품은 정상가로 그대로 노출**된다(브랜드관은 전 제품을 계속 보여준다) — 매칭 키가 `brandId` 가 아니라 `productId` 라 `PromotionPricing` 에서 brandId 를 아예 뺐다. ⚠️ **`PricingRow.id` 를 필수로 올렸다** — optional 이면 id 를 빠뜨린 select 가 컴파일을 통과하고 런타임에 "조용히 프로모션 미적용"으로 떨어진다(가격 사고는 조용할수록 나쁘다). ⚠️⚠️ **같은 작업에서 실재 과청구 구멍을 고쳤다**: `orders.service.billingRate` 가드가 국가 핀만 보고 있어서, FX 미해결국에 프로모션 세일가가 있으면 폴백 `rate=1` 로 `¥3,000` 이 **`$3,000` 으로 청구**될 수 있었다 — 가드를 `modules/orders/billing-rate.ts` 로 빼고 `promotionPriceLocal` 인자를 추가했다(현지통화로 해석되는 입력이 늘면 **반드시** 여기 인자로 추가할 것). ⚠️ zod PATCH `prices` 는 **`.optional()`** 이다 — `.default([])` 면 배포 창의 구 klow_brand 탭이 `{status}` 만 보내도 세일가 전체가 조용히 지워진다(`onsiteDiscountPct` 선례). ⚠️ `updateForBrand` 는 세일가만 보낼 때 스칼라 update 가 비어 `updateMany.count` 로 스코핑을 못 하므로 **소유 검증을 `findFirst` 로 먼저** 한다. 조회는 목적국으로 좁힌 **1회**(`prices: { where: { iso2 } }` — 20제품×98국 중 ~20행)이고 `resolvePricingCtx` 는 `onsite:true` 면 쿼리조차 안 쏜다(현장은 프로모션 무관, 구조적으로도 `onsitePriceLine` 이 세일가를 인자로 안 받는다). 정산은 실청구가 역산이라 **할인분이 자동으로 브랜드 부담**. 구 단축링크 **`/r/{code}` 는 컨트롤러째 제거**(302 가 code 를 버려 할인이 애초에 안 걸렸고 브랜드 UI 에도 없었다) — 유입 경로는 pretty 링크 하나다. klow_web 브랜드관 카드에 **`-N%` 칩**을 붙였다(취소선만 있던 유일한 화면). 브랜드 UI 는 `PromotionPriceEditor`(제품 목록) → `PromotionPriceModal`(국가별 입력) 두 컴포넌트이고 **스튜디오 `PriceModal` 이 아니라 현장(onsite) 탭이 템플릿**이다(할인 슬라이더·무료배송·박스규격이 프로모션엔 없다). 프리필은 **할인 전 정가**이고 국가 할인 중인 나라엔 "평소 −N% 적용가"를 병기한다(안 그러면 브랜드가 프로모션 방문자 가격을 실수로 **올린다**). ⚠️ **알려진 갭**: `cart.service` 는 프로모션 code 없이 가격을 매겨 **로그인 서버 카트만 정상가**를 보여준다(청구 정본은 quote). 마이그레이션 `campaign_product_prices` 는 CREATE TABLE + **DROP COLUMN/TYPE 이라 롤링 배포 비안전**(단일 레플리카 컷오버) — 2단계 분할도 불가하다(`snsUrl` 이 default 없는 NOT NULL 이라 신 서버가 INSERT 를 못 한다). 배포 순서는 **(관대화 선행 배포) → klow_server 컷오버 → klow_brand → klow_admin → klow_web**. 회귀 잠금은 `pricing/__tests__/promotion-pricing.spec.ts`(병합 규칙·0 클램프·현장 무관·표시가==청구가) + `orders/__tests__/billing-rate.spec.ts`. 자세히는 [`docs/server/modules/promotions.md`](./docs/server/modules/promotions.md).
- **campaign → promotion 전면 rename (2026-08-18):** 위 항목의 기능을 **이름만** 바꿨다(동작·계약 로직 무변경). ⚠️ **이유는 취향이 아니라 용어 충돌**이다 — `SeedingLink.campaignName`(시딩 발급 배치 라벨)이 이미 "캠페인"을 쓰고 있어, 브랜드 화면에 **"시딩 캠페인"과 "할인 캠페인"이 서로 다른 탭에서 같은 단어로** 공존했다. ⚠️⚠️ **`campaignName`·그 주변 시딩 식별자는 rename 대상이 아니다** (일괄 치환하면 시딩 코드까지 끌려간다 — 실제로 한 번 끌려가서 되돌렸다). 바뀐 것: 테이블 `Campaign`/`CampaignProductPrice`/`CampaignDailyStat` → `Promotion*`, 컬럼 `*.campaignId` → `promotionId`(**`Order` 포함**), enum `CampaignStatus` → `PromotionStatus`, 모듈 `src/modules/campaigns/` → `promotions/`(파일 5개), `src/pricing/campaign.ts` → `promotion.ts`, `common/validation/campaign.ts` → `promotion.ts`, 라우트 `/admin/campaigns`·`/v1/brand/campaigns`·`/v1/campaigns/track` → `promotions`, 공개 쿼리 `?campaign=` → `?promotion=`, 주문·견적 body `campaign` → `promotion`, 어드민 탭 `/campaigns` → `/promotions`(라벨 "프로모션"), klow_brand 탭 `/campaigns` → `/promotions`(**UI 라벨은 "할인 링크"** — 코드·주석의 도메인 용어 "프로모션"과 의도적으로 다르다). ⚠️ **같은 날 "인플루언서 전용" 전제도 걷어냈다** — 받는 사람은 인플루언서로 한정되지 않고 브랜드가 원하는 누구에게나(단골·VIP·뉴스레터 구독자·제휴처) 주는 링크다. klow_brand/klow_admin UI 문구·`Promotion` 모델 주석·`Promotion.name`(→ "링크 표시명")이 전부 이 전제로 다시 쓰였으니 "인플루언서 이름/목록" 류로 되돌리지 말 것 (단 어드민 `/influencers` 검색과 `CuratedInfluencer` 는 진짜 인플루언서 전용이라 무관). **안 바뀐 것**: 공개 유입 링크 `klow.kr/{brand.slug}/{promotion.slug}`(배포된 링크가 그대로 살아있다), `Promotion.code` 값, 저장 데이터 전부. ⚠️ 마이그레이션 `20260818021500_rename_campaign_to_promotion` 은 **`ALTER … RENAME` 만** 쓰는 순수 rename(데이터 이동·손실 0, **롤링 배포 비안전** — 구 서버가 옛 이름을 못 찾으므로 단일 레플리카 컷오버)이고 **인덱스·제약 이름까지 함께 바꾼다**(안 바꾸면 Prisma 가 드리프트로 보고 rename 마이그레이션을 또 만든다). ⚠️⚠️ **이 마이그레이션은 손으로 썼다 — `prisma migrate dev` 는 model rename 을 감지하지 못하고 DROP TABLE + CREATE TABLE 을 생성해 라이브 링크와 `Order` 귀속을 통째로 날린다.** CLAUDE.md 의 "수동 SQL 파일 생성 금지" 규칙에 대한 **의도적 예외**이며, 앞으로도 순수 rename 은 같은 예외를 쓴다(그 외에는 규칙 그대로). ⚠️ **레거시 별칭이 살아있다** — 배포 창의 구 klow_web 이 옛 이름을 보내면 **세일가가 조용히 정상가로 떨어지므로**, 서버가 공개 쿼리 `?campaign=` 과 주문/견적 body `campaign` 을 함께 읽는다(`public-products.controller.ts` 의 `legacyCampaign`, `orders.service.ts` 의 `promotionCode()`, `validation/order.ts` 의 `campaign` 필드). **klow_web 배포 후 이 셋을 함께 제거할 것.** ⚠️ klow_web `useAppStore.campaignCode` 는 **localStorage 영속** 값이라 zustand `version:1` + `migrate` 로 `promotionCode` 에 옮긴다 — 안 하면 지금 할인 링크를 물고 있는 방문자의 code 가 조용히 사라진다. 배포 순서는 **klow_server(마이그레이션 포함) 컷오버 → klow_admin → klow_brand → klow_web**. 자세히는 [`docs/server/modules/promotions.md`](./docs/server/modules/promotions.md).
- **브랜드 수동 번역 — 목업 인라인 편집 (2026-08-25):** 제품 텍스트가 브랜드 입력 → 영문 정본 → `ProductTranslation` MT 캐시(ja/zh/vi/th/id/ru)로 고객에게 나가는데 **그 기계번역 품질이 나쁘다.** 이 코드베이스는 같은 병으로 두 번 데였고(`지성 → Intellect`, `serum → 血清`(혈청), `mist → 안개`) 두 번 다 **고정 프리셋 사전**으로 막았지만, 그건 값이 유한한 필드에만 쓸 수 있는 처방이다. 제품명·주요 고민·핵심 성분·고시 서술형처럼 브랜드가 자유롭게 쓰는 텍스트에는 프리셋을 만들 수 없다. 이제 브랜드가 스튜디오 목업에서 **국가를 고른 채 텍스트를 눌러 올바른 번역을 직접 타이핑**하면 그 값이 고객 화면에 그대로 나간다. 저장은 신규 테이블 **`ProductTranslationOverride`**(`@@unique([productId, locale])` + `entries Json`) — ⚠️ `ProductTranslation` 에 컬럼을 붙이지 **않았다**(그쪽은 스테일마다 upsert 로 덮이는 소모품이라 누가 재번역을 강제하려고 비우면 사람이 쓴 원고가 사라지고, 무엇보다 **캐시 행이 없는 로케일**에 넣으려면 가짜 캐시 행을 지어내야 하는데 그게 곧바로 `t.category === null` 스테일 마커에 걸린다). ⚠️⚠️ **모양이 `{v:1, fields:{<필드키>:{<영문 원문>:<번역문>}}}` 이고 안쪽 키가 영문 원문인 것이 설계의 핵심**이다 — ① 태그·성분을 재정렬하거나 중간을 지워도 짝이 안 밀린다(`recommendedFor` 프리셋 overlay 가 인덱스로 짝지어 생긴 취약점을 구조적으로 회피) ② **원문을 고치면 키가 빗나가 자동으로 MT 로 폴백**한다 = "원문이 바뀌면 수동 번역 서빙을 멈춘다"가 **비교 코드 0줄**로 성립하고 스테일한 번역이 고객에게 나가는 경로가 아예 없다(fail-safe) ③ 지웠던 값을 같은 문구로 다시 넣으면 저절로 되살아난다. **필드 15종**(`common/validation/product.ts` 의 `OVERRIDE_FIELDS` 정본)은 klow_brand `product-fields.ts` 의 `PRODUCT_TEXT_LIMITS` 키와 **일부러 같고**(라벨 재사용), `keyIngredients` 만 `keyIngredientName`/`keyIngredientEffect` **두 키로 나눈다**(네임스페이스가 갈려야 같은 영문이 성분명이자 효과일 때 안 섞인다). ⚠️ `ingredients`(전성분 INCI)는 국제 표준 영문이라 **영구 제외**. 우선순위는 **오버라이드 > 프리셋 > MT > 원문**이고 `localize()` overlay **맨 마지막**에 대입해 필드별 가드 없이 성립한다. ⚠️⚠️ **MT overlay 가 덮기 전에 영문 원문을 붙잡아야 한다**(`captureSource`, 행 루프 첫 문장) — 그 시점의 `r.name`·`r.concerns` 는 아직 영문이고 몇 줄 뒤 사라지는데 그게 조회 키다. ⚠️ 예전의 `if (!t) continue;` 가 **`if (t) { … }`** 로 바뀌었다(오버라이드는 캐시 행이 없어도 적용돼야 한다 — 카테고리 프리셋이 이미 같은 이유로 그 위에 있다). ⚠️ **치환을 `translateAndCache()` 에 두지 말 것**(프리셋 2건과 같은 규칙 — 이미 캐시된 로케일이 영원히 MT 를 서빙한다). ⚠️ 배열은 **영문 스냅샷 길이가 정본**이고 화면에 놓인 값은 **길이가 정확히 같을 때만** 인덱스로 재사용한다. ⚠️ `captureSource` 의 성분 배열은 **빈 값을 안 거른다**(효과가 빈 행 하나에 뒤 행이 전부 밀린다). ⚠️ 캐시와 오버라이드는 **`Promise.all` 병렬 조회**이고, 이 쿼리를 없애겠다고 **`Product` 에 마커 컬럼을 두면 안 된다**(`@updatedAt` 이 6개 로케일 캐시를 전부 무효화해 재번역 폭풍). **읽기·쓰기 어느 쪽도 `Product` 를 쓰지 않는다**(스펙이 단언). 브랜드 라우트 4개(`GET translations` / `PATCH·DELETE translations/:locale` / `POST translations/resolve`)는 brand-applications 가 호스팅하고 `BrandApplicationsModule` 이 `ProductsModule` 을 import 한다(순환 없음 — SeedingModule 선례). ⚠️⚠️ **GET 은 `localize()` 를 부르면 안 된다**(패널 열 때마다 Google 과금 + 캐시 write). ⚠️ `:locale` 은 **`ZodValidationPipe(OverrideLocale)` 로** 검증한다 — 본문에서 `.parse()` 를 부르면 `en` 이 **400 이 아니라 500** 이 된다(실제로 그랬다). ⚠️⚠️ zod `value` 에 **`EnglishText`/`isAsciiPrintable` 를 걸면 안 되고**(정의상 일본어·중국어·태국어다) 반대로 **`src` 는 반드시 ASCII 검증**한다(한글 `src` = blur 영문화 전 값 = 영원히 매칭 안 되는 고아). 이웃 스키마가 전부 ASCII 전용이라 가장 나기 쉬운 복붙 버그다. ⚠️ 길이 상한에 **영문 원문 상한을 그대로 쓰지 않는다**(ru/vi 번역이 길어져 정당한 값이 400). **드리프트 UX**: 서빙은 이미 fail-safe 라 프롬프트는 정리 수단이다. ⚠️⚠️ **고아를 전부 물어보지 않는다** — `driftOf` 는 **스칼라 + 후보 1개**만 보고하고 나머지(태그·성분 삭제분)는 `pruneUnreportable` 이 조용히 지운다. 배열 고아는 "항목이 삭제됨"이라 같은 배열의 다른 항목은 개명이 아니라 별개 개념인데, 예전엔 태그를 지우면 배너가 뜨고 모달이 `うるおいチャージ`(Hydration 번역)를 `Skin texture` 에 붙이라고 제안했다. 덕분에 `candidates` 가 항상 1개가 되어 모달의 선택기·빈 상태가 사라졌다. 프롬프트 타이밍은 — 타이핑 중엔 절대 안 뜨고(자동저장 **성공** 시점에만 드리프트가 갱신된다), 목업 위 **상시 배너** + 제품 편집을 **떠날 때** 모달(`confirmLeave` 뒤, 기존 미저장 확인 다음)이 `(field, staleSrc)` 로 묶어 **전 로케일을 한 화면**에서 유지/초기화하게 한다(원문은 하나인데 오버라이드는 6개 로케일에 흩어져 있어 목업만으로는 국가를 순회해야 한다). "유지"는 값은 두고 키만 새 원문으로 옮긴다(overlay 라 캐시 무효화·백필 없이 즉시 서빙). ⚠️ `useProductAutoSave` 에 **invalidate 가 딱 하나** 생겼다(`qk.productTranslations`) — 드리프트 판정을 서버만 할 수 있어서고, 그 파일의 "setQueryData, invalidate 금지" 규칙에 대한 유일한 예외다. ⚠️ 목업에서 **`previewTrCache` 에 오버라이드를 절대 쓰지 말 것**(필드 네임스페이스가 없고 그리드·브랜드 태그라인·프로모션 프리뷰와 공유돼 같은 문구의 다른 항목으로 샌다) — 옆에 `trx(field, src)` 로 쌓는다. ⚠️ 편집은 **저장된 제품 + `en` 이 아닌 locale** 에서만 켜지고, 원문이 비었거나 한글이 남아 있으면(blur 영문화 전) 막는다. **알려진 갭**: 목업 **그리드**의 제품명은 1-인자 `tr` 이라 오버라이드가 반영되지 않는데 **klow_web 그리드는 반영된다** — "목업 == 실제 화면" 원칙과 어긋나므로 phase 2 에서 브랜드 전 제품 오버라이드를 1회 조회해 물릴 것. `cart.service` 는 `localize()` 를 아예 안 타 서버 카트만 영문이다(기존 MT 에서도 같았다). 마이그레이션 `20260825010947_add_product_translation_override` 는 **`CREATE TABLE` 뿐 → 롤링 배포 안전 · 백필 없음**이고, 스테일 마커가 이동하지 않아 **재번역·Google 과금이 0**이다(카테고리 컬럼 추가 때와 다르다). **cron 9개 불변 · 라우트 +4 · klow_web·klow_admin 무변경.** 배포 순서는 **klow_server → klow_brand**(반대면 404). 회귀 잠금은 `products/__tests__/translation-override.spec.ts`(overlay 14케이스 — 캐시 미스 적용·프리셋 압도·드리프트 폴백·재정렬·네임스페이스·write 없음) + `translation-override-shape.spec.ts`(순수 헬퍼). ⚠️ 기존 `category-overlay.spec.ts`·`skin-type-overlay.spec.ts` 스텁에 **`productTranslationOverride: { findMany }` 를 넣어야** 한다(병렬 조회라 없으면 TypeError). 자세히는 [`docs/server/modules/products.md`](./docs/server/modules/products.md) + [`docs/server/modules/brand-applications.md`](./docs/server/modules/brand-applications.md).
- **브랜드 스토리 (2026-08-25):** 브랜드관 홈은 "무엇을 파는가"를 보여주는 제품 그리드라 **"우리가 누구인가"를 담을 자리가 없었다** — 다수 브랜드가 요청한 소개 페이지를 한 장으로 떼어 놓고, 홈에서는 상품을 가리지 않는 **상단 정중앙의 글자 한 줄**로만 연결한다(배경 없는 text-shadow 광채 — 알약·반투명 판은 어떤 모양이든 사각형 경계로 드러나 "버튼을 얹었습니다" 하고 손드는 꼴이 된다). 구조는 **커버(사진+제목+소개) + 챕터 N개**(≤12)뿐이다 — 블록 타입을 늘리는 순간 편집 화면이 CMS 가 되고 그럼 아무도 안 쓴다. ⚠️ 원래 klow_brand `update02` 브랜치에 **디자인만** 있었고 문서가 `localStorage` 에만 남아 기기를 바꾸면 사라졌다(사진만 이미 R2 에 진짜로 올라가, 파일은 CDN 에 영구히 있는데 포인터만 한 브라우저 안에 있었다). 저장 정본은 **`Brand.story Json?`**(`@default` 없음 — `isBrandStoryPublic` 이 "만든 적 없음(null)"과 "만들었다 비웠음"을 구분해야 한다)이고 **신규 라우트가 없다**: 색·폰트·링크와 **같은 자동저장 큐**(`useBrandAutoSave` → `PUT /v1/brand/applications`)를 탄다. ⚠️⚠️ **그래서 스토리 zod 400 은 그 브랜드의 디자인 저장 전체를 죽인다** — 전체 문서 PUT 의 한 칸이라 색·폰트·링크까지 같이 튕긴다. 문자열 상한은 klow_brand 입력칸 `maxLength` 와 **정확히 같은 값**이고(label 24 / title 60 / subtitle 200 / heading 60 / body 1200) 세 곳이 `STORY_TEXT_LIMITS` 한 표를 본다(입력칸 · 클라 `normalizeBrandStory` 의 slice · 서버 zod). ⚠️⚠️ `updateApplication` 은 **`story === undefined` 면 키를 통째로 뺀다** — 이웃 필드처럼 `?? Prisma.JsonNull` 로 쓰면 story 를 안 싣는 구버전 klow_brand 탭·브라우저 캐시·롤백이 **배경색 하나만 바꿔도 저장된 스토리를 지운다**(회귀 스펙 `brand-applications/__tests__/brand-story.spec.ts` 가 이 분기를 잠근다). 같은 이유로 zod 에 **`.default()` 금지**. ⚠️ `chapters[].id` 는 **받은 그대로 왕복**해야 한다 — klow_brand 가 `key={chapter.id}` 로 그려서, 서버가 id 를 새로 만들면 800ms 자동저장마다 입력칸이 remount 되어 타이핑 중 커서가 날아간다. ⚠️ 공개 노출은 **`PUBLIC_BRAND_DETAIL_SELECT`(단건 전용)** 로만 — `PUBLIC_BRAND_SELECT` 는 목록(`findAll`)이 함께 쓰는데 거기는 **최대 200건**이라, 스토리를 넣으면 shop 캐러셀 응답이 통째로 부푼다. ⚠️⚠️ **라우트(`/{slug}/story`)가 아니라 klow_web 안의 화면 상태**다 — `[brandSlug]/[influencer]` 가 2세그먼트를 할인 링크 랜딩으로 선점했고(스토리가 프로모션 슬러그로 해석돼 정상가 홈이 뜬다), `middleware.ts` STOREFRONT_SEGMENTS 에 'story' 를 더하면 그 단어가 **브랜드 슬러그로 영구 예약**된다(F11). 기존 `view: shop|links` 에 세 번째 값으로 얹지 말 것(2칸 인디케이터가 깨지고 닫고 돌아왔을 때 손님이 보던 탭을 잃는다) — 별개 `storyOpen` 축이다. ⚠️ **스튜디오는 `enabled` 만 보고 진입 글자를 그리지만(빈 스토리도 눌러 들어가 꾸며야 한다) 손님 화면은 `isBrandStoryPublic = enabled && !empty` 라, "켰는데 안 보인다"는 문의는 버그가 아니라 내용이 비어 있는 것**이다. ⚠️ klow_web `src/lib/brand-story.ts`·`fade-mask.ts` 는 klow_brand 의 **의도된 크로스 레포 미러**(읽기 전용 부분집합 — 편집용 옵션 표·훅 없음). ⚠️ **i18n 키를 하나도 추가하지 않았다** — 진입 글자는 브랜드가 정한 문구이고 뒤로가기 aria 는 이미 전 로케일에 있는 `common.back` 이다(그래서 `i18n:fill` 을 돌릴 일이 없고, 큐레이션 값 `id.mist`·`vi.toner`·`vi.serum` 재번역 위험도 없다). ⚠️ **번역 대상이 아니다**(`BrandTranslation` 은 tagline/description 만) — 해외 손님은 당분간 원문을 본다. 마이그레이션 `20260825031807_add_brand_story` 는 **nullable ADD COLUMN 한 줄 → 롤링 배포 안전 · 백필 없음**, 라우트 311개 불변 · cron 9개 불변. 배포 순서는 **klow_server → klow_brand → klow_web**(서버가 먼저가 아니면 `BrandApplicationInput` 에 `.strict()` 가 없어 **400 도 없이 조용히 버려진다** — 브랜드는 "저장됨"을 보고 새로고침하면 사라진다). 같은 브랜치에 **`SoloProductSpotlight`(제품 1개일 때 강조 레이아웃)** 가 함께 들어왔는데 **klow_web 에는 미반영**이라 제품 1개 브랜드에서 스튜디오 목업과 실제 손님 화면이 갈린다(알려진 갭). 자세히는 [`docs/server/modules/brand-applications.md`](./docs/server/modules/brand-applications.md) + [`docs/server/modules/brands.md`](./docs/server/modules/brands.md).
- **브랜드 리뷰 직접 등록 (2026-08-18):** 리뷰 쓰기 경로가 `/admin/reviews` 하나뿐이라 브랜드가 자사몰(스마트스토어 등)에 쌓아둔 리뷰를 옮기려면 매번 운영팀을 거쳐야 했다. 이제 klow_brand 스튜디오에서 **폰 목업의 제품을 클릭 → 편집 패널의 `설정 / 가격` 옆 3번째 `리뷰` 탭**에서 직접 등록/수정/삭제한다(상단 pill 헤더·신규 라우트 없음 — 리뷰는 제품에 딸린 콘텐츠라 제품 편집 문맥 안에 두었고, 그래서 **제품 선택 UI 자체가 없다**). ⚠️ **KLOW 리뷰는 실사용자 리뷰가 아니라 대행 수기 입력**이다(`Review.userId` 는 아무도 안 채우는 死컬럼, `Order` 연결 없음 = 구매확인 개념 자체가 없음, `userName`·`rating`·`createdAt` 을 입력자가 임의 지정). 어드민이 하던 그 입력을 브랜드에 위임한 것이라 정책도 그대로다 — **검수 없이 즉시 노출** + **자기 제품의 모든 리뷰**(어드민 등록분 포함) 편집 가능. 그 덕에 `status`·출처 컬럼이 불필요해 **스키마 변경·마이그레이션·백필이 0건**이다. 신설은 `modules/reviews/brand-reviews.controller.ts` 5개 라우트뿐(`GET ?productId=` / `POST /bulk` / `POST /extract` / `PATCH :id` / `DELETE :id`). ⚠️ **소유권은 `Review.productId → Product.brandId` 조인으로만 판정**하고(`Review` 에 `brandId` 가 없다) 실패는 `Forbidden` 이 아니라 **`NotFound`** 다(id 를 넣어보며 남의 제품 존재를 열거하지 못하게). ⚠️ 브랜드 메서드 4종은 **전부 기존 `refreshProductAggregates` 를 같은 트랜잭션에서 거친다** — `Product.rating`/`reviewCount` 의 단일 출처라 우회하면 평점이 어긋난 채 klow_web 에 노출된다. `update`/`remove` 는 소유 검증만 따로 걸고 본체를 어드민 메서드에 위임하므로 `orNotFound` 까지 함께 탄다(동시 삭제가 500 이 아니라 404). ⚠️ `listForBrand` 는 어드민 `findAll` 을 **쓰지 않는다** — 행마다 `product` 를 조인해 싣는데 화면이 이미 제품 하나로 좁혀져 있어 전부 버려진다(200행 상한이면 수십 KB). 제품 조회에 리뷰를 매달아 1쿼리로 끝내고 `null`=404 / `[]`=200 으로 가른다. ⚠️ zod 는 어드민 것을 재사용하지 않는다: `helpful` 은 omit(표시용 카운트를 브랜드가 부풀리지 못하게), `BrandReviewPatch` 는 **`productId` 를 뺀 base** 에 `patchOf` 를 걸어 리뷰를 남의 제품으로 옮기는 경로를 스키마 단계에서 없앤다, `bulk` 상한은 2000 이 아니라 200. ⚠️ `extract`(스크린샷 OCR)에만 `@Throttle` 5회/분 — 어드민엔 없지만 브랜드는 계정 수가 많고 이 경로가 `gpt-4o`+`detail:'high'` 를 태운다. **UI 는 어드민 `ReviewManager` 의 6열 그리드를 이식하지 않았다** — 편집 패널이 2컬럼 우측(데스크탑 ~660px, 모바일 ~380px)이라 6열이 물리적으로 안 들어간다. 대신 **세로 카드 스택**(작성자·별점 / 내용 / 작성일·사진 3줄)이고, OCR 결과의 **체크박스 선택 단계를 없애 카드로 바로 채운다**(카드마다 삭제 버튼이 있어 선택 UI 가 중복). ⚠️⚠️ 리뷰 탭은 **`ProductForm` 의 형제**로 렌더한다 — 자식으로 넣으면 `<form id={PRODUCT_FORM_ID}>` 안이라 텍스트 input 에서 **Enter 한 번에 제품 폼이 원격 제출**된다(헤더 저장 버튼이 `form=` 로 원격 제출하는 구조). ⚠️ 패널 규칙은 **탭을 옮겨도 어느 쪽도 언마운트하지 않는다**(`contents`/`hidden` 토글) — 폼은 명시적 저장인 가격 편집을, 리뷰는 **gpt-4o OCR 로 뽑은 초안**을 잃는다(후자가 되살리기 훨씬 비싸다). 리뷰는 처음 연 뒤부터 마운트를 유지해(`reviewsMounted`) 안 쓰는 브랜드는 목록 조회조차 하지 않는다. 탭 상태는 `formTab`(폼 step) + `reviewsOpen` **두 사실로 분해**해 폼에 넘길 값이 늘 유효하고 동기화 코드가 없다. ⚠️ 초안 상태는 `ReviewComposer` 가 소유한다 — 부모에 두면 한 글자 칠 때마다 등록된 리뷰 전체(행마다 별 5개 + 썸네일)가 다시 렌더된다(`ReviewListItem` 은 `memo`). ⚠️ `createdAt` 이 빈 문자열이면 payload 에서 **키 자체를 뺀다**(서버 `z.coerce.date()` 가 빈 문자열을 거절 — 초안은 오늘로 저장, 편집은 미변경). ⚠️ 캐시는 **invalidate 가 아니라 제자리 갱신**이다 — `qk.brandProducts` 를 무효화하면 탭 라벨 숫자 하나 때문에 전 제품 × 국가별 가격·현장 핀이 실린 수백 KB 응답을 다시 받는다. 등록만 목록을 재조회하고(서버가 만든 id·createdAt 이 필요) 수정·삭제는 응답으로 캐시를 갈아끼우며, 건수는 `setQueryData` 로 ±한다. (`ProductDTO` 에 `rating` 을 **일부러 안 넣었다** — klow_brand 가 읽지 않으므로 부분 갱신 시 스테일을 걱정할 필요가 없다.) ⚠️ OCR 업로드는 **`preset` 을 넘기지 않는다** — 다운스케일·손실압축이 한글 OCR 을 망쳐 모델이 리뷰를 지어낸다(어드민 실측). 신규 제품 등록 패널엔 리뷰 탭이 없다(`productId` 가 아직 없다). 원문은 한국어 전제 그대로이고(`ReviewTranslation` 이 ko→6개 로케일 lazy 번역), 강제하지 않고 힌트 한 줄로만 안내한다. 배포 순서는 **klow_server → klow_brand**(반대면 리뷰 탭이 404). 자세히는 [`docs/server/modules/reviews.md`](./docs/server/modules/reviews.md).
- **브랜드관 통계에 판매 분석(국가·제품) + 현장 채널 추가 (2026-08-19):** 아래 두 항목의 퍼널이 `방문자 → 장바구니 → 결제` 까지 왔지만 **어디서·무엇이 팔렸는지**는 여전히 어디에도 없었고, 부스(현장) 실적은 정산 화면에만 있었다. klow_brand `/stats` 를 **채널 탭 `[전체][일반][할인링크][현장]` + 기간 `[7][30][90][전체]`** 구조로 바꾸고 국가·제품 랭킹(가로 막대 TOP 5 + 전체 보기)을 붙였다. ⚠️ **신규 수집이 0건이다** — 국가는 `Order.countryCode`(온라인=배송국 / 현장=손님이 고른 **가격 기준국**), 제품·수량은 `OrderItem` 에 이미 있어 **과거 주문까지 즉시 소급**된다(방문·담기는 영원히 소급 불가라 이 비대칭이 아래 모든 결정의 근원이다). 마이그레이션·백필·klow_web 변경 없음, cron 개수 불변(8). ⚠️⚠️ **한 화면에 모집단이 둘이 산다.** 퍼널 = `BrandDailyStat`(브랜드관을 거친 방문자 · 현장 제외 · 환불 미차감 · 방문일 버킷 · **명**), 판매 분석 = `Order`/`OrderItem`(결제 완료 전 주문 · `/shop`·검색·자사몰 유입과 현장 포함 · 환불 제외 · `paidAt` 버킷 · **건/개**). 같은 탭의 "결제 48명"과 "국가 합계 63건"은 **서로 다른 숫자이고 그게 정상**이라, 두 섹션에 모집단 한 줄 설명을 상시 노출한다 — **그 문구를 지우면 브랜드에겐 그냥 숫자가 틀린 화면이 된다.** ⚠️ **채널의 정의도 퍼널과 다르다** — 퍼널 `source` 는 *그 방문자의 그날 첫 진입 경로*, 판매 채널은 *주문의 성질*(`onsite` > `promotionId≠null` > `direct`)이다. 할인 링크로 들어왔지만 체크아웃까지 code 가 안 따라간 주문은 퍼널에선 promotion, 랭킹에선 direct 다. 억지로 맞추려면 방문 원장과 주문을 조인해야 하는데 그 순간 랭킹이 퍼널 모집단으로 쪼그라들어 이 기능의 목적이 사라진다. ⚠️ **현장은 방문·담기를 여전히 수집하지 않는다**(POS 흐름 + 공용 태블릿이 순방문을 1로 누른다 — 아래 항목의 판단 유지). 그래서 현장 탭은 3칸을 `'—'` 로 비우지 않고 **칸 구성 자체가 다르고**(결제 건수·판매 수량·판매 제품 종수 + 주문 기준 `건` 추이 1개) **전환율이 없다**. 신규는 `GET /v1/brand/storefront-stats/sales?days=` 하나(`storefront-sales.service.ts`)이고 **채널별 raw 행을 한 번에** 내려 **탭 전환에 재요청이 없다**(`전체` 는 클라 합산 — 한 주문은 정확히 하나의 (채널, 국가)라 중복이 없다). ⚠️⚠️ **`summary` 를 서버가 함께 내는 건 편의가 아니라 오집계 방지다** — 한 주문에 그 브랜드 제품이 2종이면 `products` 행에 2번 나타나므로 화면이 제품 행을 더해 건수를 만들면 부풀려진다. 건수는 `countries`(또는 `daily`)에서만 나올 수 있고, 그 규칙을 주석이 아니라 **응답 모양으로 강제**했다(화면엔 더할 것이 남아 있지 않다). `all.productCount` 도 채널별 합이 아니라 distinct 다. ⚠️ 일자 버킷 SQL 은 **`AT TIME ZONE 'UTC' AT TIME ZONE 'Asia/Seoul'` 이중 캐스트**(한 번만 걸면 00~09시 KST 결제가 전날로 밀린다 — `kstWeekBucketSql` 과 같은 함정). ⚠️ 국가명은 **서버가 `ShippingCountry.nameKo` 를 붙인다** — 클라 `useOrderableCountries` 는 `enabled=true` 만 담아 **배송지원을 끈 국가**(현장 판매국 상당수)가 빈칸이 된다. ⚠️ 브랜드 귀속이 `OrderItem.productId → Product.brandId` JOIN 이라 **하드 삭제된 제품의 과거 판매는 랭킹에서 사라진다**(`resolveItemBrands` 와 같은 한계). ⚠️ **`days='all'` 은 첫 데이터부터 · 365일 클램프**(`stats-window.ts`, 퍼널·판매 공용). 클램프 이유는 성능이 아니라 정합성이다 — 창이 무한하면 `totals` 와 `Σseries` 가 갈라져 "합계 100, 그래프 합 80"이 된다. **집계 하한(`statsWindowStart`)도 같은 상한만큼** 거슬러 가야 한다(하한 없이 뽑고 창만 자르면 국가 합계에 창 밖 주문이 섞인다). ⚠️ 어드민 라우트는 `'all'` 을 **일부러 안 받는다**(`StorefrontAdminStatsQuery` 분리 — 브랜드 스키마를 그대로 쓰면 `'all'` 이 문법상 통과하며 조용히 오늘 하루만 집계된다). ⚠️ 기간 토글과 채널 탭은 **같은 컴포넌트**(`ChartChrome.SegmentedToggle`)를 감싼다 — 한 줄에 나란히 서므로 클래스를 복사하면 `ChartChrome` 이 없애려고 만들어진 병이 그대로 돌아온다. `RangeToggle` 은 **제네릭**이다(`RangeValue = number|'all'` 을 공통 타입으로 고정하면 `'all'` 을 지원 안 하는 할인 링크 추이 화면이 캐스트 범벅이 된다). 탭→퍼널 키 변환은 `funnelKeyOf()` 한 곳이고 **플래그가 아니라 타입**이라(`FunnelTab`) `onsite` 를 퍼널 탭으로 잘못 켜면 컴파일이 깨진다. ⚠️ **`Order` 에 `paidAt` 인덱스가 없어 기간을 좁혀도 쿼리 비용이 줄지 않는다**(브랜드 제품의 전 기간 `OrderItem` 을 훑는다) — 지금 규모에선 문제가 아니고, 마이그레이션 없는 배포를 지키려고 일부러 미뤘다. 커지면 `@@index([paymentStatus, paidAt])` 부터. 랭킹은 국가·제품 **공용 `RankingList` 1개**(`TrendChart` 가 mini 변형으로 갈렸다 죽은 선례)이고 막대는 **합계가 아니라 1위 대비** 비율이다(합계 대비면 1위가 60%인 흔한 분포에서 나머지가 실오라기가 된다). 어드민 대시보드는 이번 범위 밖(기존 '브랜드관 방문' 섹션 유지). 회귀 잠금은 `storefront-stats/__tests__/storefront-sales.spec.ts`(요약이 제품 행 합산이 아님 · distinct 종수 · dense 제로필 · 창/하한 규칙 · brandId 스코프). 배포 순서는 **klow_server → klow_brand**(반대면 `/stats` 랭킹 섹션이 404). 자세히는 [`docs/server/modules/storefront-stats.md`](./docs/server/modules/storefront-stats.md) + [`docs/storefront-sales-analytics.md`](./docs/storefront-sales-analytics.md).
- **브랜드관 퍼널에 결제 단계 추가 (2026-08-19):** 아래 방문 통계가 **방문자 → 장바구니** 에서 멈춰 있어 브랜드가 "들어와서 담기까지는 하는데 **사는가**"를 볼 수 없었다. `방문자 → 장바구니 → 결제` 로 늘렸다(klow_brand `/stats` 상단 카드 3칸 + 전환율 보조줄, 추이 차트 3종, 어드민 대시보드 동일). 정의는 담기와 **같은 모집단** — 그 브랜드관을 거친 방문자의 결제만 세므로 **실제 주문 건수보다 적고**, 정산·주문 화면 숫자와 다른 게 정상이다. ⚠️⚠️ **집계 지점이 방문·담기와 달리 klow_web 클라가 아니라 서버 `payment.service.markPaid` 다** — 결제 성공 화면 비콘은 이 코드베이스가 이미 크게 데인 패턴이고(결제 확정 자체가 그 fire-and-forget 이었다가 QR·인앱 브라우저에서 유실됐다), `markPaid` 의 `count === 1` 분기는 클라 verify·웹훅·`payment-reconcile` 크론 **세 경로가 모두 모이는 멱등 지점**이다. ⚠️ `recordPurchase` 는 그 블록의 **맨 앞**에 둔다(뒤의 EFS 송장 왕복·알림톡이 느려지거나 죽으면 결제 집계만 조용히 사라진다). 귀속 키는 신규 **`Order.visitorId`**(주문 생성 시 klow_web 이 실어 보내는 방문자 토큰). ⚠️⚠️ **zod 는 형식이 틀려도 400 을 내지 않고 조용히 버린다**(`visitorIdLoose`) — 손님 localStorage 값이라 우리가 사후 통제할 수 없는데 정규식으로 거절하면 **분석용 토큰 하나가 결제 전체를 막는다**(형식 정본은 `common/validation/shared.ts` 의 `VISITOR_ID_RE`, 트래킹 비콘 쪽은 그대로 거절 — 그쪽은 실패해도 손님이 영향을 안 받는다). ⚠️ klow_web 에서 **토큰을 새로 만드는 건 방문 비콘(`getVisitorId`) 하나뿐이고, 담기·결제는 `peekVisitorId`**(없으면 만들지 않음)를 쓴다 — 전자를 쓰면 브랜드관도 브랜드 문맥 PDP 도 안 거친 `/shop`·검색 손님에게까지 토큰을 심는데, 그 담기·결제는 어차피 원장이 없어 서버가 버리므로 통계에 잡히지도 않는 추적 식별자만 남는다. ⚠️⚠️ **원장 조회는 주문일과 그 전날 2일을 본다** — 같은 날만 보면 (a) KST 자정 = 11:00 ET 라 미국 손님의 방문·결제가 날짜를 넘기고 (b) 카트가 localStorage 영속이라 어제 담아둔 손님은 오늘 `/{slug}` 를 안 거쳐, 결제가 대량 누락된다. 버킷은 **주문일이 아니라 원장 행의 날짜**다(주문일로 잡으면 방문 0 인 날에 결제가 찍혀 클램프가 필요해진다). 기존 유니크 인덱스 위 point read 2회라 새 인덱스가 없다. ⚠️⚠️ **결제는 담기를 함의한다** — `carted=false` 인 결제자는 "안 담은 사람"이 아니라 담기 비콘이 유실됐거나(애드블록) 전날 담은 사람이므로(체크아웃은 빈 카트로 불가, 모든 담기가 `addToCart` 하나를 지난다) `cartAdds`/`uniqueCartAdds` 를 함께 올려 `uniquePurchases ≤ uniqueCartAdds ≤ uniqueVisits` 와 `uniqueCartAdds ≤ cartAdds` 를 클램프 없이 성립시킨다. ⚠️ 반대로 `led.carted` 를 결제의 **조건**으로 걸면 안 된다(서버가 아는 사실을 클라 비콘 도달 여부에 종속시켜 **실제로 산 사람이 화면에서 사라진다**). ⚠️⚠️ **순 카운터(`uniqueCartAdds`/`uniquePurchases`)의 판정은 읽은 값이 아니라 조건부 `updateMany`(`where: {…, carted:false}`)가 실제로 1행을 뒤집었는지로 한다** — 읽고→분기→쓰기로 하면 같은 방문자의 동시 담기·동시 결제에서 둘 다 `false` 를 보고 순 카운터를 2 올려, 위 부등식이 깨지고 전환율이 100% 를 넘는다(클램프가 없어 그대로 화면에 나간다). 왕복 수는 종전과 같다. **함의된 담기 flip 이 실패하면 담기 비콘이 이긴 것이므로 카운터를 올리지 않는다.** 제외: 현장(`channel='onsite'` — klow_web 이 `visitorId` 를 안 보내고 서버가 채널로 또 막는다)·시딩·`visitorId` 없는 주문, 그리고 **환불은 차감하지 않는다**(`purchased` 가 불리언이라 정확한 차감이 불가능하고, 차감하면 과거 버킷이 사후에 움직여 어제 본 숫자가 오늘 달라진다). ⚠️ **삼킨 예외의 로그 레벨이 경로마다 다르다** — 방문·담기는 `debug`(고 QPS), **결제는 `warn` + `Sentry.captureException`**(건수가 적고 유실이 곧 "브랜드 매출이 없는 것처럼 보임"이라 조용히 실패하면 아무도 모른다). ⚠️ **`trackingSince` 는 화면에 노출한다** — 차트가 집계 시작 전 구간도 0 으로 평평하게 그려서 안내가 없으면 브랜드가 "그때는 아무도 안 왔구나"로 읽는다(백필 불가 = 0 이 아니라 기록 없음). 창 시작일 비교는 dense 시리즈의 `series[0].date` 로 해 클라에 KST 날짜 계산을 복제하지 않는다. ⚠️ `pruneVisitorDays()` 가 원장과 **같은 커트라인으로 `Order.visitorId` 도 null 로 지운다** — 주문 행은 영구 보존 + 이름·주소를 들고 있어 그대로 두면 익명 토큰이 영구히 실명과 연결돼 이 모듈의 프라이버시 약속을 뒤집는다. ⚠️ 부스에서 만난 손님은 사라질 수 있다(그날 첫 진입이 `?mode=onsite` 면 원장 `source` 가 onsite 라 나중 온라인 결제가 어느 칸에도 안 뜬다), 자사몰 임베드는 2026-08-20 부터 PDP 진입 시점에 이미 `direct` 로 잡힌다(그전엔 "바로구매"로 `/{slug}` 에 push 됐을 때만 잡혔다). 마이그레이션 `20260819060746_add_storefront_purchase_funnel` 은 **ADD COLUMN 3개뿐 → 롤링 배포 안전 · 백필 없음**(cron 개수 불변 = 8개). 회귀 잠금은 `storefront-stats.spec.ts`(31케이스 — 퍼널 부등식·2일 룩백 경계·함의된 담기·다브랜드 주문·동시 담기/결제 경합). ⚠️ 스펙 스텁의 `brandVisitorDay.updateMany` 는 `where` 의 `carted:false`/`purchased:false` 조건을 **실제로 적용**하고 `data` 를 **통째로 병합**해야 한다(조건을 무시하고 count:1 을 돌려주거나 필드를 하드코딩하면 순 카운터 테스트가 아무것도 못 잡은 채 통과한다). ⚠️⚠️ 배포 순서는 **klow_server → klow_web → (2일 뒤) klow_brand/klow_admin** — 프론트를 서버보다 먼저 내보내면 `formatNumberKo` 가 `undefined.toLocaleString()` 로 **throw** 해 klow_brand `/stats` 와 **klow_admin 대시보드 홈 전체**(서버 컴포넌트)가 죽는다. 2일 간격은 첫날 결제만 0 인 화면을 브랜드가 "숫자 틀렸다"로 읽는 걸 피하려는 것. 자세히는 [`docs/server/modules/storefront-stats.md`](./docs/server/modules/storefront-stats.md).
- **제품 상세(PDP) 직접 진입도 방문 통계에 집계 (2026-08-20):** 브랜드 피드백 — **"브랜드관으로 들어가야만 집계된다."** 방문 비콘이 `BrandStorefront` 한 곳에서만 나가서, 브랜드가 자사몰에 단 **카페24 임베드 버튼이 보내는 `/product/{id}?brand={slug}`(PDP) 유입이 통계에서 통째로 사라지고 있었다** — 방문뿐 아니라 **담기·결제까지**. 담기 게이트가 "그날 원장 행이 있을 때만"이고 `Order.visitorId` 도 방문 비콘이 만든 토큰에 달려 있어서, 브랜드가 자기 돈으로 붙인 유입 채널의 성과가 `/stats` 에서 **전부 0** 으로 보였다. ⚠️ **게이트는 `?brand=` 유무 하나**다 — `/shop`·검색을 둘러보다 스쳐간 조회는 안 센다(PDP 가 국가 선택 모달에 이미 쓰는 조건 `useGuestCountryPrompt(brandMode && …)` 과 **같은 잣대**). ⚠️ **새 `source` 값을 만들지 않고 기존 `direct`(일반)에 합쳤다** — 그 덕에 prisma enum(`ALTER TYPE ADD VALUE`)·`REPORTED_SOURCES`·응답 shape·klow_brand `ChannelTabs`/`StorefrontStatsBoard`·klow_admin 이 **전부 무변경**이고 **마이그레이션도 없다**(새 source 를 `REPORTED_SOURCES` 에 넣었다면 `totals.all` 정의가 바뀌어 어드민의 "일반 X · 할인 링크 Y" 줄이 합계와 어긋났을 것이다). ⚠️⚠️ **귀속 키는 `productId` 이고 URL 의 `?brand=` 슬러그가 아니다** — 슬러그는 주소창에서 바꿀 수 있어 `/product/{A사 제품}?brand={B사}` 로 **남의 방문수를 부풀리는 통로**가 된다. 슬러그는 **클라의 게이트로만** 쓰고 서버가 `Product.brandId` 로 해석한다(담기 비콘과 같은 3줄 — PDP 는 brandId 를 알 수 없다. 공개 제품 응답을 `StrippedPricingKeys` 가 벗긴다). zod 는 `brandId ⊕ productId` 를 **`superRefine`** 으로 강제한다(`z.union` 은 400 바디에 중첩 `unionErrors` 를 통째로 싣는다 — issue 1개가 낫다). 라우트는 **추가하지 않았다**(같은 이벤트라 집계 경로가 하나여야 하고, 나누면 klow_web 도 dedupe 를 두 벌 든다). ⚠️ **`track/country` 도 함께 넓혀야 한다** — 임베드 유입은 첫 방문이라 **국가가 null 인 채로 도착하는 게 가장 흔한 경로**이고, 안 넓히면 임베드 방문의 국가가 거의 전부 '미상'으로 남는다. ⚠️ 브랜드관 그리드의 상품 링크에도 `?brand=` 가 붙어 **임베드 URL 과 형태가 같다**(구별 불가) → klow_web 모듈 레벨 **`visitedBrands`** 로 "그 탭에서 그 브랜드를 아직 안 봤을 때만" PDP 방문을 센다. **순방문(명)·전환율은 이 가드가 없어도 정확하다**(원장 유니크 제약이 `uniqueVisits: 0` 을 만든다) — 가드가 지키는 건 `visits`(회)의 의미와 요청 수뿐이니 새 탭·새로고침 누수를 sessionStorage 로 더 메우려 들지 말 것. 슬러그는 `.trim().toLowerCase()` 로 비교한다(브랜드관은 DB 정본 소문자, PDP 는 URL 원문이라 대문자 URL 하나에 가드가 뚫린다). ⚠️⚠️ **담기가 방문 비콘을 기다린다(`pendingVisit`)** — 딥링크 PDP 는 담기 버튼이 활성화되는 순간이 방문 POST 를 쏘는 순간과 거의 같아서, 안 기다리면 담기가 원장 행보다 먼저 도착해 서버가 버린다(**이 기능이 살리려던 바로 그 담기**). `.then(send, send)` 로 실패도 태운다. 언로드 유실은 없다(담기 후 이동이 전부 `router.push`). **국가 비콘도 같은 `afterVisit` 로 줄 선다 (2026-08-21)** — 국가가 방문을 앞지르면 서버가 원장 행을 못 찾아 버리는데 클라는 이미 dedupe 에 찍어 재시도가 없다(로그인 손님의 `useSessionSync` → `syncCountry` 에서 실제로 난다). ⚠️⚠️ **국가 dedupe 의 축은 `productId` 가 아니라 브랜드 문맥이다 (2026-08-21 정정).** 처음엔 PDP 방문 비콘이 국가 시드를 `visitedBrands` 게이트보다 **먼저** 심었다 — 브랜드관에서 국가를 이미 고른 손님이 제품을 클릭할 때마다 서버가 어차피 버릴 `track/country` 를 한 번씩 쏘는 걸 막으려는 것이었다(제품 5개 = 헛 POST 5개 + 헛 쿼리 10개). **그런데 그게 `trackStorefrontProductCountry` 를 통째로 도달 불가능하게 만들었다**: 국가를 **나중에** 고른 손님은 effect 가 재실행되며 게이트 앞에서 키만 찍고 곧바로 return 하므로, 뒤이은 국가 비콘이 자기 키에 막혀 **한 번도 안 나갔다** — 즉 이 항목이 살리려던 바로 그 임베드 유입의 국가가 100% '미상'이었다. 지금은 키가 `id:{brandId}:{cc}` / `slug:{slug}:{cc}` **두 별칭**이고 브랜드관 비콘이 둘 다 찍어 같은 최적화를 대신하며, 표시(`markCountrySent`)는 **반드시 실제 전송과 같은 경로에서만** 한다. **시드를 게이트 위로 되돌리지 말 것.** ⚠️ **`promotionCode` 를 절대 싣지 않는다**(localStorage 영속값이라 예전 링크에 클릭이 영원히 붙는다). 현장(`?mode=onsite`)은 `brandMode` 가 false 라 자동 제외. ⚠️ **의도된 손실**: 임베드 PDP 를 먼저 본 사람은 원장 `source` 가 `direct` 로 **고정**돼, 같은 날 할인 링크로 브랜드관에 와서 결제해도 그 결제가 `promotion` 이 아니라 `direct` 에 잡힌다("할인 링크 추이: 클릭 1"인데 "할인링크 탭: 방문자 0"으로 두 화면이 어긋나 보일 수 있다). `?mode=onsite` 가 이미 만든 것과 같은 성질이고, 고치려면 마지막-쓰기-승이 필요한데 그건 읽기 모델에 감산을 요구해 음수 카운터를 만든다 — **회귀 스펙이 이 동작을 의도된 손실로 명문화**해 뒀으니 "버그네" 하고 고치지 말 것. 불변식은 전부 유지된다(`uniqueCartAdds ≤ uniqueVisits` · `Σ국가 + 미상 == uniqueVisits` · `totals.all == direct + promotion`). ⚠️ 배포 순서는 **klow_server → klow_web**(프론트 2개는 배포 대상 아님) — **klow_web 을 먼저 내면 그 창의 PDP 방문이 통째로 유실된다.** 국가 수집 배포 때는 필드 *추가*라 구 서버가 unknown key 를 조용히 strip 했지만 여기는 **필수 필드 제거**라 구 서버 zod 가 400 을 내고 `api.ts` 의 `.catch(devWarn)` 이 프로덕션에서 삼킨다. **마이그레이션 없음 · cron 8개 불변 · 라우트 수 불변.** ⚠️ 배포 첫날 `uniqueVisits(direct)`·`purchases` 가 **계단처럼 올라간다**(임베드 유입이 새로 들어온다 — 할인 링크 클릭이 봇 제외로 계단처럼 내려갔던 것과 같은 성질). 회귀 잠금은 `storefront-stats/__tests__/storefront-stats.spec.ts` PDP 10케이스(⭐담기 복구 · ⭐결제 복구 · direct 고정 손실 · 프로모션 클릭 미상승 · legacy brandId null 무기록 · 키 없음 무기록) + `common/__tests__/storefront-visit-key.spec.ts`(XOR). 자세히는 [`docs/server/modules/storefront-stats.md`](./docs/server/modules/storefront-stats.md) "제품 상세(PDP)도 수집 지점이다" 절.
- **브랜드관 방문자 국가 지표 (2026-08-20):** `/stats` 의 국가 지표가 **결제한 주문**(`Order.countryCode`)에서만 나와, 브랜드가 **"어느 나라에서 들어오는데 안 사는가"** 를 볼 수 없었다(가격·배송비 때문에 이탈하는 국가, 아직 배송지원을 안 켠 국가를 발견할 방법이 0). 방문 파이프라인엔 국가 축이 **아예 없었고** `docs/storefront-sales-analytics.md` §6 이 "안 하는 것 — 신규 수집 설계 필요"로 명시해 둔 자리다. 이제 방문 비콘이 국가를 실어 나른다. ⚠️ **국가의 정본은 손님이 국가 선택 모달에서 `고른` 값**(`useAppStore.country`)이다 — **GeoIP 를 쓰지 않는다**(이 서버엔 `cf-ipcountry` 류를 읽는 코드가 0건이고 IP·UA 미저장이 이 모듈의 약속, VPN·캐리어 라우팅에 취약). 무엇보다 고른 값은 **이미 가격·언어·배송비를 좌우하는 정본**이라 두 번째 추정 국가를 두면 같은 화면에서 국가가 두 벌이 되고 판매 분석 국가 랭킹과 비교가 불가능해진다. `navigator.language` 추정도 안 쓴다(`OnboardingModal` 이 초기 하이라이트에만 쓰고 저장 안 하는 값 — en-US 브라우저를 쓰는 베트남 손님이 미국으로 잡힌다). ⚠️⚠️ **klow_web 에서 `?? DEFAULT_COUNTRY('US')` 폴백을 걸면 안 된다** — 가격 조회가 미선택을 US 로 폴백하는 건 조회 편의지만 지표에선 **"모른다"를 "미국이다"로 바꾸는 조작**이고, 브랜드가 있지도 않은 미국 수요를 보고 마케팅비를 쓴다. 저장은 **판정 마커 `BrandVisitorDay.countryCode`**(`carted`/`purchased` 와 같은 역할) + **읽기 모델 `BrandVisitorCountryDay(brandId,date,source,countryCode)`**(`visitors` 명) 2축. ⚠️ **원장을 groupBy 하지 않는 이유는 prune 이다** — 원장은 `pruneVisitorDays()` 가 **100일**에 파기하는데 조회 창은 `days='all'` 에서 **365일**까지 간다. 원장 집계로 만들면 `전체` 가 조용히 최근 100일만 가리키는, "지금은 되고 100일 뒤에 틀려지는" 실패가 된다(그래서 **prune 에 새 테이블을 추가하면 안 된다**). `BrandDailyStat` 에 국가를 안 붙인 건 유니크 키에 국가가 들어가면 기존 퍼널 카운터 6개가 전부 국가별로 쪼개져 마이그레이션이 파괴적이 되기 때문. `countryCode` 에 **`ShippingCountry` FK 를 걸지 않는다**(온보딩 목록 115개 vs ShippingCountry 234행이 갈릴 수 있고, FK 면 그 순간 기록 경로가 throw). ⚠️⚠️ **귀속은 첫-쓰기-승**이고 판정은 읽은 값이 아니라 **조건부 `updateMany({where:{countryCode:null}})` 의 `count===1`** 이다 — 읽고→분기→쓰기면 같은 방문자의 국가 이벤트 2건(탭 2개·모달 재선택)이 동시에 인플라이트일 때 둘 다 null 을 보고 `visitors` 를 2 올려 **Σ국가 > uniqueVisits** 가 된다(클램프가 없어 그대로 화면에 나간다). **마지막-쓰기-승으로 바꾸지 말 것** — 국가 변경 시 옛 국가 −1 이 필요한데 읽기 모델이 트랜잭션 없는 increment upsert 라 lost update 에서 **음수 카운터**가 난다. 대가는 **같은 날 국가를 바꾸면 그날 지표엔 반영 안 됨**(다음 KST 날짜부터, `source` 가 "그날 첫 진입 경로"인 것과 같은 규칙). ⚠️ **국가 이벤트는 `visits`/`uniqueVisits` 를 절대 안 건드린다**(`bumpDaily` 미호출 — 올리면 국가를 고른 손님만 방문 2로 세어진다). 그래서 방문 비콘 재발사가 아니라 **신규 비콘 `POST /v1/storefront-stats/track/country`**(120/분, 가드 없음, 늦게 고른 경우 전용)이고, 마운트 시점에 이미 고른 손님은 `track/visit` 의 `countryCode?` 로 함께 와 **흔한 경로에 요청이 안 는다**(klow_web 이 `sentVisits` 와 **별개의 `sentCountries` Set** 으로 dedupe — 방문 키가 이미 들어있는 채로 국가를 보내야 하므로 두 dedupe 는 독립이어야 한다). ⚠️ **'미상' 은 서버가 뺄셈으로 파생한다** — `iso2:null` 행 = `max(0, totals[source].uniqueVisits − Σ국가)`. 클라가 빼게 두면 요약 카드 방문자와 랭킹 합계가 화면에서 어긋나는데 원인을 화면만 보고는 알 수 없다. 0 이면 행을 안 만들고(유령 "미상 0명" 금지) source 별로 각각 낸다. ⚠️ **초기엔 미상이 1위일 것이다**(집계 시작 전 방문은 영원히 미상 · 모달을 닫은 게스트 · 배포 이후에야 채워짐 — **소급 불가**). 조회는 **신규 엔드포인트 없이 기존 퍼널 응답에 `countries[]` 를 얹는다**(모집단·창 규칙·`REPORTED_SOURCES` 필터가 퍼널과 **같아야만** 미상 뺄셈이 성립한다. 탭 전환 재요청 0). 국가명은 **서버가 `ShippingCountry.nameKo` 를 붙인다**(klow_brand 국가 목록은 `enabled=true` 만 담아 그 밖의 국가가 빈칸이 된다 — 판매 분석과 같은 이유, `enabled` 로 거르지 않고 매핑에 없는 iso2 도 `nameKo:null` 로 **행을 살려** 보낸다). zod 는 **`.toUpperCase()` 정규화 필수**(`kr`/`KR` 이 두 행으로 갈리면 랭킹에 같은 나라가 두 번 뜬다) + **`.default()` 금지**. ⚠️⚠️ **`/stats` 에 국가 랭킹이 둘이 됐다** — 위 `방문 국가 TOP`(명 · 브랜드관을 거친 방문자 · 고른 기준국) vs 아래 `국가 TOP`(건 · 결제 완료 전 주문 · 배송국/가격기준국). **이 변경의 최대 혼동 위험**이라 두 숫자를 억지로 맞추려 들지 말 것(화면에 각주를 안 두는 기존 결정 유지 → CS 응대는 문서로). 레이아웃은 1행 `[방문자 추이][방문 국가 TOP]`, 2행 `[결제 추이]` 가로 전체이고 **현장 탭엔 방문 국가 카드가 없다**(방문을 수집하지 않아 빈 랭킹이 "아무 나라도 안 왔다"로 읽힌다). `RankingList` 는 **쪼개지 않고** `countUnit` prop 1개만 추가해 재사용(`개국`), 대신 `mergeByTab` 은 **재사용하지 않는다**(`{channel,orders,quantity}` 에 묶여 있고 여기는 축이 `source` 에 측정값이 하나 — 제네릭화하면 두 호출부가 다 나빠진다). 마이그레이션 `20260820031131_add_storefront_visitor_country` 는 **nullable ADD COLUMN + CREATE TABLE = 추가 전용 → 롤링 배포 안전 · 백필 없음**, **cron 개수 불변(8)**, 라우트 +1(294). 회귀 잠금은 `storefront-stats/__tests__/storefront-stats.spec.ts`(국가 16케이스 — 첫-쓰기-승·동시 귀속 경합·방문수 불변·Σ국가+미상==uniqueVisits·미등록 iso2 생존). ⚠️ 스펙 스텁의 `brandVisitorDay.create` 는 **`countryCode: data.countryCode ?? null` 로 정규화**해야 한다(`updateMany` 스텁이 `r[k]===where[k]` 비교라 undefined 면 `{countryCode:null}` 조건이 영원히 0건이 되어 국가 테스트가 통째로 죽는다 — `carted`/`purchased` 와 같은 이유). 배포 순서는 **klow_server → klow_web → klow_brand**(⚠️ 서버가 먼저인 이유: `StorefrontVisitInput` 이 non-strict 라 구 서버에 `countryCode` 를 보내면 **400 이 아니라 조용히 버려진다**). ⚠️⚠️ **2026-08-20 화면 개편**: `/stats` 가 `요약 카드 → [방문 국가] → [결제]` 세 블록이 됐다. 두 패널 모두 **누적 영역 차트 + 색 점 순위표를 한 카드**에 담는다(App Store Connect 국가/지역 탭 구조) — 색 점이 차트 조각을 가리켜야 하므로 **차트와 표를 다른 카드로 쪼개면 안 된다**. **방문자 추이 차트는 삭제**했다(방문 국가 누적 영역의 높이가 곧 그날 방문자 수라 같은 그림을 두 번 그리는 셈). ⚠️⚠️ **결제 패널의 단위는 `명`이 아니라 `건`이다** — 결제 국가·제품은 주문 원장(`Order`/`OrderItem`)에만 있고 차트·표가 같은 출처여야 색 대응이 성립하기 때문이다. 그래서 요약 카드 "결제 N명"(퍼널)과 결제 패널 "N건"(전 주문)은 다른 숫자이고, 각 패널의 `note` 한 줄이 유일하게 남은 모집단 설명이니 **지우지 말 것**. ⚠️ **색은 항목(국가)에 붙지 순위에 붙지 않는다** — 배정은 탭과 무관한 전체 순위로 하고 채널 탭을 옮겨도 재배정하지 않는다(필터 하나에 살아남은 국가 색이 뒤바뀌면 안 된다). 팔레트는 `SERIES_PALETTE` 5색(흰 배경 기준 인접쌍 CVD ΔE 9.1 / 일반시야 19.6 통과) + 잔여 무채색 `SERIES_OTHER_COLOR`, 6번째부터 '기타' 한 조각으로 접는다. ⚠️ 그 5색 중 셋이 대비 3:1 미만이라 **표가 차트와 반드시 함께 있어야 한다**(relief 조건). ⚠️ **펼침 버튼은 패널이 소유한다**(표마다 두면 결제 패널 아래 같은 버튼이 2개 생긴다). 서버는 퍼널 `countries[]` 에 `points`(일자별 dense)·`prevVisitors`, 판매 `countries[]` 에 `points`·`prevOrders` + `dates[]` 를 싣고 **`onsiteDaily` 는 제거**했다(현장도 국가별 조각으로 그린다). ⚠️ 미상은 **일자별로도** 뺄셈한다(기간 합계만 맞추면 누적 영역의 그날 높이가 그날 방문자 수와 어긋난다). ⚠️ 판매 쪽 `days='all'` 은 **직전 기간을 조회하지 않는다**(창 앞이 존재하지 않고 `paidAt` 인덱스가 없어 헛스캔이 비싸다 → 그 탭은 증감이 전부 `신규`). ⚠️ 직전 기간 쿼리는 **경계가 둘**이어야 한다(하한만 걸면 현재 기간까지 세어 증감이 늘 "그대로"). ⚠️ `TrendChart` 는 `variant='stacked-area'` 분기만 더했고 **파일을 나누지 않았다**(축·툴팁·색 규칙이 두 벌이 되면 곧 갈라진다). 회귀 잠금이 64케이스로 늘었다. 자세히는 [`docs/server/modules/storefront-stats.md`](./docs/server/modules/storefront-stats.md) "방문 국가" 절.
- **브랜드관 방문 통계 + 장바구니 전환 (2026-08-19):** 브랜드가 자기 브랜드관에 **손님이 몇 명 들어왔는지** 볼 방법이 없었다 — 유입을 세는 곳이 `PromotionDailyStat` 하나뿐이었고 그건 **할인 링크로 들어온 트래픽만** 잡아서, SNS 프로필 링크·QR·검색·자사몰 경유로 `/{slug}` 에 직접 온 손님은 어디에도 기록되지 않았다. 신규 모듈 `src/modules/storefront-stats/` 가 진입을 **방문/순방문**으로 세고 **그 방문자가 담았는지**까지 이어 붙이며, 유입 경로를 **일반(`direct`) / 할인 링크(`promotion`)** 둘로 나눈다(`enum StorefrontVisitSource`). ⚠️ **현장(`onsite`, 부스 QR)은 집계 대상이 아니다** — 손님이 이미 눈앞에 있는 **POS 흐름**이지 온라인 유입이 아니고, 부스 **공용 태블릿은 브라우저가 하나**라 손님 여럿이 순방문 1로 눌려 합계를 통해 일반·할인링크 지표까지 오염시킨다. 부스 실적은 `Order.channel='onsite'` 로 **정산·주문 화면에 이미 잡히므로** 빼도 잃는 게 없다. klow_web 이 현장 모드에서 비콘을 아예 안 쏘고, **조회도 `source: { in: ['direct','promotion'] }` 로 거른다**(과거 행·배포 창의 구 프론트가 남긴 `onsite` 행이 **화면 어느 칸에도 없이 합계에만 섞이면** 브랜드가 설명을 들을 방법이 없다). enum 값 `onsite` 는 묘비로 남긴다(Postgres enum 값 제거는 타입 재생성 필요 + 과거 행이 참조). 브랜드관을 렌더하는 라우트는 정확히 3개(`/{slug}` · `/{slug}/{promotionSlug}` · 레거시 `/brand/{id}`)이고 읽는 쿼리는 `mode=onsite` 하나뿐이다(자사몰 카페24 임베드 버튼이 보내는 `/product/{id}?brand=` = **PDP** 도 2026-08-20 부터 방문으로 센다 — 아래 별도 항목). ⚠️⚠️ **같은 작업에서 할인 링크 클릭 집계도 이 파이프라인으로 통일했다** — 예전엔 klow_web **서버 컴포넌트**가 `trackBySlug` 로 세서 **SNS 언펄러(봇)가 링크 하나당 여러 번 긁는 게 전부 클릭**이 됐고, 그건 중립적으로 틀린 게 아니라 **할인 링크 성과를 부풀렸다**(브랜드가 "이 링크에 할인을 계속 줄까"를 그 숫자로 판단한다). 이제 klow_web 이 방문 비콘에 `promotionCode` 를 실어 보내고 `recordVisit` 이 `PromotionsService.recordClickByCode()` 를 부른다 — **카운터 소유권은 promotions 모듈에 남긴다**(Off/중지 게이트가 그 도메인 규칙이라 두 벌이 되면 갈린다). 구 `trackBySlug` 는 집계를 뗀 **`resolveBySlug`**(세일가 code 해석 전용)가 됐고 라우트 경로의 `track` 은 klow_web 과의 계약이라 남은 legacy 다. ⚠️ **전환 시 기존 클릭 시계열이 한 번 계단처럼 내려간다**(봇+새로고침이 빠진다 — 버그 아님)이고 **애드블록 방문자는 이제 아예 안 잡힌다**(정확도↔커버리지 교환). ⚠️ `promotionCode` 는 **라우트가 이번 진입분만** 내려줘야 한다 — `useAppStore.promotionCode` 는 localStorage 영속이라 그걸 쓰면 예전에 링크로 왔던 사람의 클릭이 그 링크에 영원히 붙는다. ⚠️ 클릭은 `source` 로 게이트하지 **않는다**(`?mode=onsite` 로 들어와도 링크는 실제로 눌렸다). ⚠️ 클라 dedupe 키에 code 가 들어간다(같은 탭에서 링크 A→B 면 둘 다 실제 클릭). 저장은 **읽기 모델 `BrandDailyStat(brandId,date,source)`**(차트는 이것만 읽는다) + **판정 원장 `BrandVisitorDay(brandId,date,visitorId)`**(유일한 일이 "그날 처음인가"를 유니크 제약으로 원자적으로 답하는 것) 2테이블. ⚠️ **누적 카운터를 `Brand` 에 두지 않았다** — `@updatedAt` 이라 방문마다 update 하면 **방문자 트래픽이 브랜드를 bump** 해서 `stats.brandActivity()` 가 "아무것도 안 했는데 활성"으로 센다(그 파일이 `Promotion.updatedAt`·`Brand.updatedAt` 을 활동 소스에서 뺀 것과 같은 병). 스펙이 `brand.update` 미호출을 단언한다. ⚠️ **집계 지점은 klow_web 클라이언트**(`BrandStorefront` useEffect)다 — 서버에서 세면 SNS 언펄러가 전부 방문이 되고(`generateMetadata` 가 5분 ISR 을 쓰는 게 그 트래픽의 증거), 순방문 판정에 필요한 브라우저 저장소를 못 읽으며, non-async 서버 컴포넌트인 `[brandSlug]/page.tsx` 를 async 로 바꾸면 최다 트래픽 페이지 TTFB 에 왕복이 얹힌다. ⚠️ **`source` 는 라우트가 prop 으로 내려준다** — pathname 세그먼트로 넘겨짚으면 예약 슬러그·리다이렉트에서 조용히 틀리고, `useAppStore.promotionCode`(localStorage **영속**)를 보면 한 번 할인 링크로 왔던 사람이 나중에 직접 들어와도 영원히 promotion 으로 잡힌다. ⚠️ **순방문 판정을 클라에 맡기지 않는다** — 클라는 불투명 `visitorId`(localStorage 난수)만 보내고 API 에 `unique` 필드가 **아예 없다**. 클라가 계산하면 (a) 진실을 주장하게 되고 (b) 더 중요하게 **"방문한 그 사람이 담았는가"를 서버가 알 수 없어 퍼널이 성립하지 않는다**. 저장소를 못 쓰면(프라이빗 모드) **트래킹을 통째로 건너뛴다** — 매번 임시 id 를 만들면 순방문이 방문수까지 부풀어 오른다. ⚠️⚠️ **담기는 그날 그 브랜드관을 방문한 사람의 담기만 센다**(원장 행이 없으면 아무것도 기록 안 함) — 이게 퍼널 정의 그 자체이고, 덕분에 **`uniqueCartAdds ≤ uniqueVisits` 가 구조적으로 보장**돼 전환율에 클램프가 필요 없다(클램프는 정의가 틀렸다는 신호를 숨길 뿐이다). 귀속은 담기 시점 URL 이 아니라 **그 방문자의 그날 첫 진입 경로**다. ⚠️ `recordCartAdd` 는 `led.carted` 를 **update 前에 값으로 붙잡는다**(뒤에 읽으면 순담기자가 영원히 0). ⚠️ **brandId 는 서버가 `productId` 로 해석**한다 — 공개 제품 응답엔 `brandId` 가 없고(`StrippedPricingKeys` 가 의도적으로 벗긴다) 그 목록도 `CartLine`(persist+migrate 영속 스키마)도 건드리지 않았다. 담기 초크포인트는 `useCartStore.addToCart` 의 `addQty <= 0` early return **바로 다음** — 그 가드가 "브랜드당 5개 상한에 막힌 no-op"을 자동 제외하고, `updateQuantity`·`replaceCart` 는 이 함수를 안 타 유령 이벤트가 없다. ⚠️ 방문 중복 가드는 **모듈 레벨 `Set`**(`useRef` 로는 `?mode=onsite` 토글·view 전환 remount 에서 뚫린다). 공개 트래킹 2라우트는 전역 60/분보다 **완화**(visit 120·cart 60) — 부스 와이파이·통신사 NAT 뒤에서 한 IP가 장소 전체를 대표하므로 조이면 정상 방문자끼리 429 로 막고 그게 조용한 과소집계가 된다(`public-seeding` 선례). 서비스는 **절대 throw 하지 않고** 응답이 늘 `{ok:true}` 이며 없는 브랜드는 FK(P2003)가 검증한다(존재 확인 쿼리 없음). 브랜드 UI 는 헤더 필의 **`/stats`(방문 통계) 독립 페이지**다 — 스튜디오 탭 스트립은 브랜드관을 *편집*하는 자리라 읽기 전용 지표가 낄 자리가 아니다(`/seeding`·`/onsite`·`/settlement` 와 같은 급). ⚠️ 새 최상위 보호 라우트는 **`src/middleware.ts` matcher 에 넣어야** 미인증 접근이 빈 화면을 한 번 그리지 않는다(`/onsite`·`/settlement`·`/promotions` 는 지금도 누락 상태). 어드민은 대시보드 홈 "브랜드관 방문" 섹션. ⚠️ **화면 표기는 퍼널을 "명"으로 읽고 "회"는 보조줄로 내린다** — `방문자`(uniqueVisits)/`담은 사람`(uniqueCartAdds)/`전환율`이 큰 숫자고 `총 N회 방문`/`총 N번 담음`이 그 아래 작은 줄이다. 전환율이 이미 명 기준 파생이라 "회" 지표를 동등하게 두면 어느 게 본선인지 안 보인다(실제로 "방문은 뭐고 순방문은 뭐지" 피드백이 나왔다). **UI 에 "순방문" 이라는 업계 용어를 쓰지 않는다.** 차트도 같은 단위(명)로 그리고, 어드민 표 정렬 키도 `uniqueVisits`(동률은 `visits`)로 앞세운 열과 맞춘다. `TrendChart` 는 `promotions/_components/` → `components/charts/` 로 옮겨 `lines` prop 으로 일반화(1선/3선 공용, 복사본 금지 — 예전에 mini 변형으로 갈라졌다 죽은 이력). 마이그레이션 `20260819025346_add_brand_storefront_stats` 는 CREATE TYPE + CREATE TABLE 2개뿐 → **롤링 배포 안전 · 백필 없음**(과거 방문 기록이 존재하지 않아 `trackingSince` 이전은 0 이 아니라 "데이터 없음"). ⚠️ 새 cron(`storefront-visitor-day-prune`, 원장 100일 파기)이라 `test/app.e2e-spec.ts` 기대 목록이 **8개**로 늘었다. 회귀 잠금은 `storefront-stats/__tests__/storefront-stats.spec.ts`. ⚠️⚠️ **배포 순서가 통일 때문에 뒤집힌다 — klow_web 을 먼저 내보낸다**: `klow_web → klow_server → klow_brand/klow_admin`. 서버를 먼저 올리면 `resolveBySlug` 가 집계를 멈춘 창 동안 **구 klow_web 이 `promotionCode` 를 아직 안 보내 할인 링크 클릭이 통째로 유실된다**(이미 운영 중인 지표다). 반대로 klow_web 을 먼저 올리면 그 창에는 신규 트래킹 POST 가 404 로 조용히 버려질 뿐이고(아직 아무도 안 보는 신규 지표) 클릭은 구 서버가 종전대로 계속 센다 — 잃는 쪽이 훨씬 싸다. 브랜드/어드민 화면은 마지막(하루치가 쌓인 뒤 여는 게 낫다). 자세히는 [`docs/server/modules/storefront-stats.md`](./docs/server/modules/storefront-stats.md).
- **회원가입 → 브랜드 주소(slug) 순서 뒤집기 (2026-08-24):** 예전엔 랜딩 "글로벌 진출하기" 가 **`SlugModal` 로 영문 브랜드명(URL slug)부터** 받아 `sessionStorage`(`klow_brand_pending_slug`)에 넣고, 그 값을 가입 payload(`/v1/brand/auth/{signup,phone/signup}` · 구글 `?slug=`)에 실어 **가입 트랜잭션 안에서 Brand draft 를 함께** 만들었다. 이유는 `BrandUser.brandId === null` orphan 차단이었다. 지금은 **가입을 먼저 끝내고**, 세션이 생긴 뒤 **`/start` 페이지**(모달 아님)에서 slug 를 받아 `POST /v1/brand/applications/init-draft` 로 만든다. ⚠️ **klow_brand 단독 배포 — 서버 변경·마이그레이션·라우트 추가 0건**: 세 가입 스키마의 `slug` 는 원래부터 `.optional()` 이고 `initDraft` 도 "slug 가 안 실려온 엣지/legacy 안전망"으로 이미 있었다(둘 다 서버에 그대로 남아 있고 klow_brand 만 안 쓴다 — 구 프론트가 배포 창에 남아도 안전). ⚠️⚠️ **그 대가로 모든 신규 계정이 `brandId === null` 로 태어나므로, 그 창을 닫는 게 이 변경의 절반이다** — 게이트는 **`(authed)/layout.tsx` 한 곳**뿐이고 `BrandSessionUser.brandId` 를 본다(그전까지 klow_brand UI 가 안 쓰던 필드). 가입 경로가 3개(전화 OTP·구글 OAuth·이메일)에 "중도 이탈 후 재로그인"까지 있어 **각 경로가 목적지를 알게 하면 반드시 하나가 빠진다**; 특히 **구글은 서버가 `BRAND_FRONTEND_URL`+`returnTo` 로 리다이렉트해 클라가 목적지를 통제하지 못한다** — 그래서 세 경로 전부 종전처럼 `/studio` 로 착지시키고 게이트가 흡수한다. ⚠️ 게이트는 **`validated`(=`/me` 가 이번 마운트에서 실제로 끝남)를 함께 봐야 한다** — `useBrandSession` 이 localStorage 로 세션을 낙관적 시드하므로 그것만 믿으면 다른 브라우저에서 브랜드를 만든 사용자가 `/start` 로 헛튕긴다(랜딩의 `/studio` 자동 이동이 같은 이유로 이미 `validated` 를 본다). ⚠️⚠️ **그 대가로 반대 방향 구멍이 열리므로 판정이 비대칭이다 (2026-08-25 수정)** — `ready`(=`!isPending`)는 localStorage seed 만으로 켜지는데 `needsBrandSetup` 은 `validated` 를 요구하니, 그 사이 프레임엔 `redirecting` 이 false 라 **brand 없는 계정이 `/me` 를 기다리는 동안 저장이 안 되는 studio 를 한 번 본다**(가입 직후엔 `setSession` 이 방금 seed 를 써 두므로 **매번** 그렇다 — 실제로 `skeleton → studio 깜빡 → /start` 로 보였다). 그래서 `unsureNoBrand = ready && authed && !validated && user?.brandId === null` 을 **렌더 홀드에만** 더한다 — seed 가 "brand 없음"이라고 말하면 스테일할 수 있으니 `/start` 로 **보내지는 않되**(위 헛튕김 방어 유지) 확정될 때까지 아무것도 그리지 않고, seed 에 `brandId` 가 있으면 종전대로 낙관적 렌더라 일반 사용자는 대기가 늘지 않는다. ⚠️ 리다이렉트 판정(`useEffect`)에는 넣지 말 것 — 넣는 순간 스테일 seed 가 `/start` 로 보내는 그 버그가 돌아온다. 리다이렉트 대기 중엔 `children` 을 안 그린다 — brand 있는 사용자에게 슬러그 폼을 한 프레임 비추거나, brand 없는 계정에게 **아무것도 저장되지 않는 무반응 스튜디오**(`useBrandAutoSave.applyBrandPatch` 가 `if (!brand) return`)를 보여주게 된다. ⚠️⚠️ `/start` 의 `initDraft` 성공 처리는 **`patchSession({...user, brandId})` 를 동기적으로 먼저** 해야 한다 — `invalidateSession()` 만 쓰면 `/me` refetch 를 기다리는 프레임에 게이트가 아직 `null` 을 보고 `/studio` → `/start` 로 되튕겨 **무한 루프**가 된다(`useBrandAutoSave.onSuccess` 가 invalidate 대신 `setQueryData` 를 쓰는 것과 같은 판단). 반면 `qk.brandApplication` 은 **invalidate 로 둔다** — `init-draft` 응답은 raw Brand 라 `APPLICATION_INCLUDE`(products·submittedBy)가 없어 캐시에 심으면 shape 이 어긋난다. ⚠️ slug 충돌은 **가용성 검사와 제출 사이에** 생길 수 있어 최종 판정이 서버 409(`이미 사용 중인 링크입니다`)다 — 토스트만 띄우고 페이지에 머문다. ⚠️ 하드 게이트라 `/start` 에 **로그아웃 탈출구**가 필요하다(설정 페이지와 같은 optimistic logout). ⚠️ 새 최상위 `(authed)` 라우트라 **`src/middleware.ts` matcher 에 `/start/:path*` 를 넣어야** 미인증 접근이 빈 화면을 한 번 안 그린다. 함께 정리된 것: `SlugModal.tsx` **삭제**(`useSlugAvailability`·`CheckMessage` 의 유일 소비자였고 `/start` 로 이전), `/signup`(이메일) 의 **pendingSlug 하드 가드 제거**(그전엔 slug 없이 열면 `/?signup=guide` 로 되돌려 보냈다), studio 의 `pendingSlug`+`initDraft` 자동 호출 안전망과 `useBrandAutoSave` 의 `pendingSlug` 인자 제거, `lib/constants.ts` 의 `PENDING_SLUG_STORAGE_KEY`/`persistPendingSlug`/`peekPendingSlug` 삭제, `api.ts` 의 `signup`/`phoneSignup` payload 와 `googleStartUrl` 에서 `slug` 제거. ⚠️ **`SignupGuideModal` 은 삭제하지 않았다** — `?signup=guide` 를 아직 3곳이 생산한다(`LoginModal` 미가입 번호 · `ForgotPasswordFlow` 미가입 이메일 · **klow_server 구글 `mode=login` 콜백**); 카피만 "브랜드 주소 입력" → "회원가입 시작"으로 고쳤고 `onConfirm` 이 이제 `SignupModal` 을 연다. 부수 효과(의도된 개선): 프로덕션에 남아 있던 `brandId === null` orphan 계정이 다음 로그인에 `/start` 로 유도되어 자동 복구된다. 자세히는 [`docs/server/modules/brand-auth.md`](./docs/server/modules/brand-auth.md).
- **브랜드 커스텀 도메인 — P1 서버 기반 (2026-08-21):** 브랜드가 자기 도메인(`shop.brandA.com`)으로 브랜드관을 여는 기능의 **서버 절반만** 들어왔다. ⚠️ **아직 아무 프론트도 이 API 를 부르지 않는다** — 실제 서빙은 klow_web 미들웨어(P3), 브랜드 등록 UI 는 klow_brand(P4)다. 설계 정본은 [`docs/custom-domain/implementation-plan.md`](./docs/custom-domain/implementation-plan.md), 모듈 문서는 [`docs/server/modules/brand-domains.md`](./docs/server/modules/brand-domains.md). ⚠️ **경계가 핵심이다**: 커스텀 도메인은 **둘러보기·담기까지만** 맡고 **로그인·결제·주문조회는 `klow.kr`**(핸드오프) — 그래서 프록시가 없고 결제창 호출 도메인이 오늘과 같아 PG 전제가 안 바뀐다. 신규 `BrandDomain` 테이블(`host @unique` = 라우팅의 유일한 키, `role primary|redirect`, `status pending|verifying|active|error`) + 라우트 5개(`/v1/brand/domains` CRUD 4 + 공개 `GET /v1/storefront/resolve?host=`). ⚠️⚠️ **Vercel 의 `verified` 는 "접속이 되는가"가 아니라 "소유권이 다투지 않는가"다** — 우리가 소유하지도 않은 도메인이 추가 즉시 `verified:true` 로 온다(실측). `active` 전이는 반드시 **`verified && !misconfigured`** 로 **두 API 를 모두** 본다(`domain-status.ts`). ⚠️⚠️ **apex 판정을 우리가 하지 않는다** — `brandA.co.kr` 은 레이블 3개지만 apex 라(Public Suffix List) 레이블 수를 세면 첫 한국 고객부터 잘못된 DNS 를 안내한다. 정본은 Vercel 응답의 `apexName === name` 이고, `domain-host.ts` 는 **정규화·거부만** 한다. ⚠️ **DNS 값도 하드코딩 금지** — `getDomainConfig` 의 `rank:1` 권장값을 저장해 그대로 띄운다(`rank:2` 는 legacy 라 같이 띄우지 않고, CNAME 후행 점은 제거). ⚠️ 브랜드는 **호스트를 하나만** 입력하고 그게 apex 면 `www` 가 `role='redirect'` 로 자동 동반 등록된다 — **거꾸로(서브도메인 입력 → apex 자동 등록)는 절대 하지 않는다**(브랜드의 기존 홈페이지를 뺏는다). 페어 실패는 primary 를 롤백하지 않고 삭제는 페어 단위다. ⚠️⚠️ **`PUBLIC_BRAND_WHERE` 에 구독 게이트가 없다는 걸 이 작업에서 처음 발견했다**(status `notIn` 뿐 — 구독 게이트는 제품 쪽 `isPurchasable()` 에만 있었다). 그래서 **브랜드 축 규칙의 단일 소유자를 `brands/brand-selects.ts` 로** 세웠다 — select(`BRAND_SERVICEABLE_SELECT`) · JS 술어(`isBrandServiceable`, 별칭 `canServeStorefront`) · Prisma where(`BRAND_SERVICEABLE_WHERE` / `BRAND_NOT_SERVICEABLE_WHERE`) 세 형태를 한 블록에 두고 `product-selects.ts` 도 이걸 호출한다(**네 벌이던 규칙이 한 벌로**). ⚠️ 하나를 고치면 나머지 둘도 고칠 것. ⚠️ Prisma 짝이 없던 동안 `cleanupOrphans` 가 부정형을 손으로 써서 **pending·rejected·withdrawal_pending 브랜드가 정리 대상에서 통째로 빠져** 있었다(서빙은 막혔는데 Vercel 등록만 영원히 남는다) — 테스트 스텁이 정본 상수를 참조 동일성으로 확인해 손으로 쓴 절이 들어오면 즉시 throw 한다. ⚠️⚠️ **`main.ts` 의 Origin 정적 배열이 술어로 바뀌었다** — 분류·정책표는 **`common/origin-policy.ts`** 가 소유하고 스펙이 잠근다(`origin-exempt.ts` 선례 — `bootstrap()` 안 익명 콜백에 두면 테스트가 닿지 않는다). CSRF 가드(`allowsStateChange`)와 `enableCors`(`corsPolicyFor`, 이제 **delegate**)가 **같은 분류 결과를 나눠 쓴다**(갈리면 반드시 사고). 새 오리진 분류는 표에 한 줄만 추가한다. ⚠️⚠️ **브랜드 오리진의 상태변경은 분류가 아니라 경로로 좁힌다**(2026-08-22 보안 점검) — `allowsStateChange(c, path)` 가 `brand` 에만 `BRAND_STATE_CHANGE_PATHS`(트래킹 비콘 3개 + 부수효과 없는 `/v1/orders/quote`) 정확 일치를 요구한다. 분류만 보고 전 경로를 열면, JSON preflight 는 ACAC 부재로 막히지만 **`application/x-www-form-urlencoded` 같은 simple request 는 preflight 를 안 타** 쿠키가 실린 채 도달해(운영 `SameSite=None`) 악성·침해된 브랜드 사이트가 klow.kr 로그인 손님 명의로 **blind 상태변경**을 낼 수 있다. 커스텀 도메인 화면에서 새 POST 를 부르게 되면 그 목록에 등록해야 한다(fail-closed). `isVerifiedOrigin` 은 **동기**(express·cors 가 sync)라 서비스가 `Set<string>` 스냅샷 + TTL 60초 백그라운드 갱신을 들고, 생성·삭제·상태전이는 **그 자리에서 await 갱신**한다. ⚠️ **정확 일치만 한다 — 와일드카드·서브도메인 확장 금지**(넣으면 전 브랜드 도메인이 CSRF 우회로). ⚠️ 이게 없으면 비콘이 아니라 **브랜드관 화면이 통째로 빈다**(`BrandStorefront` 가 `'use client'` 라 브라우저가 `api.klow.kr` 를 직접 친다). **credentials 는 오리진별로 갈린다** — klow.kr 계열만 `true`, 커스텀 도메인은 `false`(세션을 안 쓰는 설계를 서버가 fail-closed 로 강제). `common/origin-exempt.ts` 는 **무변경**이고 그 스펙이 그대로 통과해야 한다. ⚠️ `/v1/storefront/resolve` 에만 **`@SkipThrottle()`** — 부르는 쪽이 손님이 아니라 Vercel 엣지라 IP 가 하나로 뭉치는데 전역이 60/분 per IP 라, 429 가 나면 미들웨어 fail-open 으로 **전 브랜드 도메인이 동시에** KLOW 홈이 된다. ⚠️ **반영 지연 최대 2분**(응답 캐시 60s + 미들웨어 캐시 60s) — 구독 해지·도메인 삭제 후에도 그만큼 계속 서빙된다(설계상 수용). ⚠️ **`VERCEL_PROJECT_ID` 는 환경별로 다른 값**이다(`klow-web` / `klow-web-staging`) — 스테이징이 운영 id 를 들면 테스트 도메인이 운영 사이트에 꽂힌다. 부팅 fail-closed 가드는 **일부러 안 붙였다**(미설정 시 503 명시 거부). Vercel 호출은 `@vercel/sdk` 가 아니라 **native fetch**(`instagram.client.ts` 관례, API 버전 5개는 2026-08-21 실측 확정: add v10 / get·verify·remove v9 / config v6). ⚠️⚠️ **`cleanupOrphans` 의 유예 시계는 `BrandDomain.updatedAt` 이 아니다**(2026-08-22 정정 — 그전엔 그랬고 그래서 **60일 유예가 사실상 0일**이었다). `active` 행은 다시 쓰이지 않아(폴링 cron 이 `pending`/`verifying` 만 집는다) 그 값이 **활성화된 순간에 얼어붙어**, 오래 잘 돌던 도메인이 구독이 `past_due` 로 떨어지는 **첫 사이클에 즉시** Vercel 에서 제거됐다 — 유예가 막으려던 바로 그 상황(재결제 후 DNS 재설정 강요)이다. 정본은 `brands/brand-selects.ts` 의 **`brandUnserviceableSinceWhere()` + JS 짝 `brandUnserviceableSince()`** 이고 시계는 **원인 축의 행**이 쥔다(`status` 로 배타적인 세 분기 — 승인 축이면 `Brand.updatedAt` / 구독 축이면 `BrandSubscription.updatedAt` / 구독 행이 없으면 폴백). 두 시계 모두 무관한 편집으로 **늘어날 수는 있어도 줄지 않아** 안전한 방향으로만 틀린다. 마이그레이션 없음(§9 F33). ⚠️ 유예 절은 `BRAND_NOT_SERVICEABLE_WHERE` 옆에 **`AND:` 로 얹고**(relation 필터를 한 객체에 두 번 못 쓴다) 런타임 날짜가 박힌 절은 팩토리 옆 `unserviceableSinceOf()` 가 되꺼내 스텁이 정본 JS 짝으로 판정한다. 마이그레이션 `20260821052240_add_brand_domains` 는 `CREATE TYPE ×2 + CREATE TABLE` 뿐 → **롤링 배포 안전 · 백필 없음**. ⚠️ 새 cron(`brand-domain-verify`, 5분)이라 `test/app.e2e-spec.ts` 기대 목록이 **9개**로 늘었다. ⚠️ 호스트 표준형은 **`canonicalHost()` 를 쓰기·읽기가 공유**한다 — 조회 쪽에서 손으로 다시 쓰면 IDNA 가 빠져 한글 도메인이 저장(punycode)과 조회(U-label)로 갈린다. 회귀 잠금은 `brand-domains/__tests__/{domain-host,domain-status,verified-origin,domain-pairing,resolve-host}.spec.ts` + `common/__tests__/origin-policy.spec.ts`. **배포는 P0 과 같은 창에서 klow_server 단독**(프론트가 아직 아무것도 부르지 않는다). 전체 배포 절차는 [`docs/deploy-custom-domain-runbook.md`](./docs/deploy-custom-domain-runbook.md).
- **브랜드 커스텀 도메인 — P4 브랜드 등록 UI (2026-08-21):** 위 P1~P3 로 기능은 다 동작하는데 **브랜드가 도메인을 등록할 화면이 없어** 운영팀 수동 호출 없이는 시작조차 못 하던 것을 마무리했다. `klow_brand` 단독 · **서버·web·admin 무변경 · 마이그레이션 없음 · 신규 서버 라우트 없음**(`/settings/:path*` 가 이미 미들웨어 matcher 에 있어 그쪽도 무변경). 위치는 **설정 > 도메인 연결**(`settings/_components/DomainSection.tsx`, `PhoneSection` 처럼 `CollapsibleCard` 껍데기 + 안쪽 매니저 — 접히면 언마운트라 **목록 조회가 안 나가고 폴링이 자동으로 멈춘다**). ⚠️⚠️ **폴링 대상은 `GET /v1/brand/domains` 이지 `POST :id/check` 가 아니다** — 후자는 6회/분 상한이고 1건이 Vercel API 를 2~3회 태워, 10초 폴링이면 천장에 닿아 **브랜드의 수동 "지금 확인" 클릭이 429** 가 된다(cron 이 5분마다 갱신하므로 목록만 다시 읽으면 결과가 따라온다). `pending`·`verifying` 행이 없으면 폴링을 끈다. ⚠️⚠️ **`verifying` 은 "SSL 발급 중"이 아니라 소유권 TXT 챌린지 대기**다(계획 문서가 그렇게 잘못 적고 있었다 — "발급 중"으로 안내하면 브랜드가 기다리기만 하고 TXT 를 영영 안 넣는다). ⚠️⚠️ **`lastError` 를 "에러가 있다"의 신호로 쓰지 말 것** — `refreshOne` 이 확인할 때마다 덮어써서 **정상 `pending` 에도** `'DNS 레코드가 아직 확인되지 않았습니다'` 가 들어 있다(톤은 `status` 가 정하고 `lastError` 는 `error` 행의 사유 줄로만 쓴다. 안 지키면 갓 만든 도메인이 전부 고장난 것처럼 보인다). ⚠️ **`recordValue` 는 빈 문자열일 수 있다** — 그때 `76.76.21.21` 류 폴백을 화면에서 채우면 F3 위반(벤더가 타겟을 옮긴 날 신규 연결이 전부 조용히 실패)이라 **"확인 중…"** 을 그리고 폴링이 채우게 둔다. ⚠️ **apex 판정을 클라가 하지 않는다(F2)** — `lib/domain.ts`(서버 `domain-host.ts` 의 **의도된 크로스 레포 미러**, 정규화·거부·상한만)에 apex 함수가 없고, `pair:null` 의 두 의미(서브도메인이라 없음 / 상한 초과로 서버가 생략)는 **서버가 준 `recordType === 'A'`** 로 가른다(판정이 아니라 서버 `apexName` 결과를 읽는 것). 미러 정합은 서버 `domain-host.spec.ts` 케이스 32건(punycode·over-match 금지 `myklow.kr`/`klow.kr.brand.com`/`notvercel.app` 포함)으로 확인. ⚠️ **`useDomainAvailability` 훅을 만들지 않았다** — 서버에 도메인 availability 엔드포인트가 없어(중복은 `POST` 409 로만 안다) `useSlugAvailability` 를 베끼면 비동기 취소 로직이 빈 껍데기로 남는다 → 순수 `checkDomainInput()` + **기존** `useDebounced`. `CheckBadge` 는 재사용하되 **`CheckMessage` 는 안 한다**(슬러그 전용 한국어를 하드코딩하고 `storefrontLabel(slug)` 를 부른다). ⚠️⚠️ **이 API 의 에러 본문은 세 모양**이다 — A `{code,message}` / B Nest 기본(**502 는 message 가 Vercel 영문 디버그 문자열**, 429 는 `ThrottlerException: …`) / C zod `{error,issues}`(message 키 없음). `extractApiError` 만 쓰면 벤더 영문이 브랜드 화면에 뜨고 `describeApiError` 만 쓰면 403 `subscription_required` 의 좋은 문구가 뭉개진다 → **`code` 유무로 가른다**(로컬 헬퍼 `domainErrorMessage`). ⚠️ **구독 게이트는 안내만 하고 입력은 열어 둔다** — 서버 게이트가 `approved && (어드민 생성 브랜드 || 구독 active)` 인데 `BrandDTO` 에 `submittedById` 가 없어 그 예외를 구분할 수 없고, 잠그면 어드민 생성 브랜드는 도메인을 붙일 방법이 아예 없다(어드민 도메인 UI 가 없다). 최종 게이트는 서버 403. ⚠️ **"지금 확인" 버튼 라벨을 바꾸지 말 것** — 서버가 저장하는 `lastError` 문구 여러 곳이 그 문자열을 그대로 인용한다. ⚠️ `lib/storefront.ts` 는 **`storefrontUrl`·`storefrontLabel` 둘만** `customDomain?` 을 받는다(값 규칙 `https://{domain}/` — **슬러그 세그먼트 없음**, 미들웨어 rewrite 와 대칭). ⚠️⚠️ **`lib/onsite.ts` 의 `onsiteStoreUrl()` 에는 영원히 넘기지 말 것** — 미들웨어가 커스텀 도메인에서 `?mode=onsite` 를 **떼어 내고 307** 해서 부스 QR 이 에러 없이 조용히 일반 모드로 떨어진다(§6-2 링크 도메인화가 가장 먼저 밟을 지뢰). 화면은 **핸드오프 경계**(둘러보기·담기만 / 로그인·결제는 klow.kr)와 **반영 지연 최대 2분**을 연결 전에 안내한다. ⚠️ 배포 순서는 **P3(klow_web) 이후 klow_brand 단독** — 먼저 내면 브랜드가 도메인을 연결했는데 사이트가 안 뜬다. 자세히는 [`docs/custom-domain/implementation-plan.md`](./docs/custom-domain/implementation-plan.md) §5 + [`docs/server/modules/brand-domains.md`](./docs/server/modules/brand-domains.md).
- **브랜드 커스텀 도메인 — P3 서빙 개시 (2026-08-21):** 위 P1(서버)·P2(수신부)를 이어 **커스텀 도메인이 실제로 동작**하게 됐다. `klow_web` 단독 · **서버 변경·마이그레이션·신규 i18n 키 없음**. 신규 `src/middleware.ts` 가 ① 본 도메인 즉시 bypass ② `GET /v1/storefront/resolve` 로 Host→슬러그 해석 ③ 경로 rewrite/redirect ④ **커스텀 도메인 전 응답에 `X-Robots-Tag: noindex, follow`** 를 한다. ⚠️⚠️ **`credentials:'omit'`(F8)은 위생이 아니라 화면이 뜨는 조건이다** — `origin-policy.ts` 가 브랜드 오리진에 `credentials:false` 를 주므로 `Access-Control-Allow-Credentials` 가 안 붙고, `'include'` 요청은 쿠키만 빠지는 게 아니라 **브라우저가 CORS 실패로 응답 자체를 막아 브랜드관이 통째로 빈 화면**이 된다(서버가 fail-closed 로 의도한 것). ⚠️ 그래서 `isCustomDomain` 은 `'use client'` 인 `lib/host.ts` 가 아니라 **지시자 없는 `lib/host-classify.ts`** 가 소유한다 — `api.ts`·미들웨어(edge)가 같이 써야 하는데 `'use client'` 모듈을 그쪽에서 import 하면 client reference 가 되어 터진다. ⚠️ **브랜드 도메인에서 통과시킬 경로는 명시 허용목록**(`STOREFRONT_SEGMENTS` = product·cart·track·legal·faq·customer-center)이고 **모르는 최상위 경로는 404** 다 — 예약어면 전부 통과시키던 초기 구현은 기본값의 방향이 틀렸다(새 라우트를 추가하면 **아무도 미들웨어를 안 고쳐도 전 브랜드 도메인에서 세션 없이 열린다**). `RESERVED_BRAND_SLUGS` 는 klow_server 미러라 klow_web 이 편집할 수 없는 파일이기도 하다. **새 최상위 라우트를 만들면 허용목록이나 klow.kr 전용 목록에 등록할 것.** ⚠️⚠️ **`/{seg}` 는 어떤 경로로도 pass-through 하면 안 된다(F11)** — pass-through 된 `shop.brandA.com/brandB` 는 `[brandSlug]` 에 그대로 걸려 **브랜드 B 의 브랜드관이 브랜드 A 도메인에 렌더**된다. **2세그먼트도 마찬가지다**(정본 문서가 "pass-through → 404" 라고 잘못 적고 있었다 — `[brandSlug]/[influencer]` 는 `isBrandSlugAllowed` 만 보고 통과시켜 **브랜드 B 의 세일가까지 적용**하고 `source='promotion'` 비콘으로 **브랜드 B 의 할인 링크 통계를 오염**시킨다). fail-open 은 **루트 `/` 에만** 허용된다. ⚠️ rewrite 는 반드시 **`req.nextUrl.clone()`** — `new URL(path, req.url)` 은 **쿼리를 버려** `?purchased=`(결제 후 브랜드 도메인 카트 정리, 언제나 `/` 로 도착한다)가 통째로 죽는다. ⚠️ 리다이렉트는 **307**(308·301 은 브라우저가 영구 캐시하는데 슬러그·도메인은 가변 DB 상태다). ⚠️ **쿠키가 필요한 경로(`/login`·`/signup`·`/my`·`/orders`·`/seed/*`·`/checkout/onsite`)와 `/shop` 은 미들웨어가 307 로 klow.kr 에 넘긴다** — `/seed/*` 는 예약 슬러그라 그냥 두면 렌더되는데 `klow_order` 게스트 쿠키에 의존하고 **자기 도메인에서 결제창을 연다**(핸드오프로 피하려던 바로 그것). **`/track/{id}?t=` 는 의도된 예외**(쿠키가 아니라 URL 서명 토큰). `/shop` 은 쿠키와 무관하지만 **KLOW 마켓플레이스(경쟁 브랜드 목록)** 라 함께 막고, 같은 이유로 하단 탭바를 커스텀 도메인에서 **통째로 숨긴다**(카트 접근은 브랜드관 FAB). ⚠️ **`?mode=onsite` 는 쿼리를 보존하는 순간 실제로 도달하므로 미들웨어가 떼어 낸다.** ⚠️ 송신부는 `location.assign`(**새 탭 금지** — 인앱 브라우저 컨텍스트 유실)이고 **`peekVisitorId()`**(`getVisitorId()` 금지 — 없으면 만들어 원장 없는 토큰이 된다), 진입점은 **카트 결제 버튼과 PDP 바로구매 둘 다**(하나만 고치면 나머지가 세션 없는 `/checkout` 으로 들어간다). ⚠️ **국가 미선택이면 넘기지 않고 국가 모달을 연다** — 수신부가 `setCountry()`(≠`syncCountry`)를 불러 **`onboardedAt` 까지 찍으므로** `?? 'US'` 로 넘기면 klow.kr 에서 모달이 영구 억제된 채 US 가격으로 결제한다. ⚠️⚠️ **`encodeHandoff` 가 자기 정화(self-sanitizing)다 — "encode 가 만든 것은 decode 가 반드시 받는다"가 불변식**이다. `decodeHandoff` 는 fail-closed 라 **선택 필드 하나만 어긋나도 payload 전체를 버리는데** 송신부가 싣는 값 중 셋이 통제 밖이다(`o = location.host` 는 **로컬·스테이징에서 포트가 붙어 실제로 전량 폐기됐다** · `p` 는 localStorage · `l` 은 persist 스키마). 증상이 "프로모션만 빠짐"이 아니라 **손님이 klow.kr `/cart` 로 떨어져 장바구니가 사라진 것처럼 보이는** 것이라 조용하다. 술어를 export 해 송신부가 검사하게 하면 **검사를 잊는 두 번째 호출부**가 생기므로 `handoff.ts` 가 직접 떨어낸다. ⚠️ `location.assign` 은 진짜 문서 언로드라 `storefront-track.ts` 의 **"언로드 유실 걱정 없음(전부 `router.push`)" 전제가 깨진다** — 방문 비콘이 인플라이트면 담기 전송이 **시작조차 안 한** 상태라 `keepalive` 가 못 구하고 `peekVisitorId()` 도 `null` 일 수 있다(→ **결제 단계 퍼널 영구 0**) → `flushPendingVisit(300ms)` 를 기다린 뒤 이동한다. ⚠️ 루트 레이아웃 컴포넌트(`CustomDomainMount`)에서 **`useSearchParams()` 금지**(전 페이지가 CSR 로 deopt) — `window.location.search` 를 마운트 effect 에서 읽는다(`useCheckoutGate` 선례). ⚠️ 미들웨어 본문은 **try/catch → `next()`** 로 감싼다(throw 하면 **klow.kr 포함 전 호스트·전 경로가 500**). resolve 캐시는 양성·음성 **둘 다 60초**(서버가 60초다) · **실패는 캐시하지 않고 stale 만 읽으며**(fail-closed 면 API 가 흔들릴 때 전 브랜드 도메인이 동시에 죽는다) · `Host` 가 공격자 통제 값이라 **상한 256 + 축출 + fetch 전 문법 검사**. ⚠️⚠️ **`RESOLVE_TIMEOUT_MS` 는 3초이고, resolve 의 느린 쪽 왕복보다 짧으면 기능이 통째로 죽는다** — 1초였을 때 스테이징에서 **모든** 요청이 abort 돼 커스텀 도메인이 한 번도 안 떴다(서버·DNS·Vercel 은 전부 정상이었다). 실측 왕복이 **1.2~1.6초**이고 그중 ~0.75초는 Railway+Neon 기본 지연이라 **서버를 최적화해도 1초 밑으로 안 내려간다.** 판별자는 `/{seg}` 가 404 가 아니라 **503** 이라는 것 — "슬러그가 없다"가 아니라 "해석 자체를 못 했다"는 신호다(§9 F30). ⚠️⚠️ **메타데이터 세그먼트 판정은 정확 일치**(+`-{hash}` 접미)다 — `startsWith` 로 보면 `icon` 접두 하나에 `/iconic` 같은 **실재 가능한 슬러그**가 pass-through 되어 F11 이 뚫린다(2026-08-22 수정, §9 F32). ⚠️⚠️ **`host-classify.ts` 의 `HOST_RE` 는 서버 `domain-host.ts` 보다 좁으면 안 된다** — TLD 를 `[a-z]{2,63}` 로만 받으면 **punycode TLD(`xn--…`)를 거부**해, 서버가 받아 `active` 까지 간 한글 도메인을 미들웨어가 영원히 못 연다(resolve 를 아예 안 부른다). 핸드오프 `o` 도 같이 버려진다(2026-08-22 수정, §9 F31). ⚠️ 결제 진입점은 **`useGoCheckout()` 하나**다(카트 버튼·PDP 바로구매 공용) — 사본 2개를 "둘 다 바꿔야 한다"는 주석으로 지키면 세 번째 결제 버튼이 생기는 날 깨진다. ⚠️ 자격증명 정책의 소유자는 **전송 계층**이다(`api.ts` 의 `canAuthenticate()`) — 세션 훅이 주소창을 다시 묻지 않는다. ⚠️⚠️ **미들웨어가 import 하는 모듈에 부수효과를 들이지 말 것** — onsite 상수를 `useOnsiteStore`(`'use client'` + 최상위 `create()`)에서 가져왔더니 **zustand 가 통째로 edge 번들에 들어가** 77.4 → 82.1 kB 가 됐다(값 없는 `lib/onsite-entry.ts` 로 분리해 복귀). 회귀 잠금은 **`npm run check:handoff`**(klow_web 에 테스트 러너가 없어 tsx 로 도는 순수 함수 10케이스 — punycode 왕복이 F31 을 잠근다, 의존성 추가 0). 배포 순서는 **P0+P1(server) → P2(web) → P3(web)** 이고 P3 는 **klow_web 단독**이다. ⚠️ Vercel 에 도메인 추가 시 **"Redirect to primary domain" 이 붙으면 기능이 통째로 죽는다.** 자세히는 [`docs/custom-domain/implementation-plan.md`](./docs/custom-domain/implementation-plan.md) §4.
- **브랜드 커스텀 도메인 — P2 핸드오프 수신부 (2026-08-21):** 커스텀 도메인은 **둘러보기·담기까지만** 맡고 **로그인·결제·주문조회는 `klow.kr`** 이 맡는다(핸드오프). 경계를 건너는 순간 **오리진이 바뀌어 `localStorage` 가 따라오지 않으므로**, 커스텀 도메인의 "결제하기"가 `klow.kr/handoff?h=<base64url>` 로 top-level 이동하고 klow_web 이 상태를 복원한 뒤 `/checkout` 으로 보낸다. 이 PR 은 **받는 쪽만** — 아직 아무도 `/handoff` 를 링크하지 않아 **klow.kr 사용자에게 불가시**다(송신부는 P3). ⚠️ **배포 순서는 P2(수신) → P3(송신)** — 뒤집으면 손님이 없는 `/handoff` 에서 404 를 본다. 넘기는 것은 **상태 4개 + 복귀 주소 1개**: 카트 라인 `[productId, quantity]` · 가격 기준국 · `promotionCode` · `visitorId` · 복귀 host. ⚠️⚠️ **payload 에 가격을 싣지 않는다** — 싣는 순간 서명·비밀키·만료·재생방지가 따라오는데, 담기는 값이 전부 이미 클라 소유·조작 가능하고 청구 정본은 서버 견적/주문이라 **지키는 것이 0** 이다. 예외는 복귀 host(`o`) 하나이고, 그건 나중에 성공 화면의 **href 로 렌더**되므로 서명이 아니라 **`GET /v1/storefront/resolve` 재검증**으로 막는다(스킴은 클라가 `https://` 로 조립, 디코더가 대문자·포트·경로·`//evil.com`·`javascript:` 를 구조적으로 거부, 실패·미등록은 전부 "저장 안 함" — **fail-open 금지**). ⚠️⚠️ **`SessionSyncMount`(`layout.tsx:77`)은 `{children}` 앞 형제라 effect 가 페이지보다 먼저 돈다** — 옛 카트로 `merge` 를 동시에 쏘고 서버 프로필 국가로 `store.country` 를 덮어, 페이지 안에서 순서를 아무리 지켜도 못 푼다. 그래서 `useSessionSync` 에 **`usePathname() !== '/handoff'` 스위치**를 넣고 `/handoff` 가 같은 일(프로필 국가 **await** 업로드 → `cart.merge` → `replaceCart` → `repriceCartForCountry`)을 직접 한다. ⚠️ "이미 동기화됨" 플래그는 **일부러 안 뒀다** — `/checkout` 에서 훅이 한 번 더 도는 게 `sameCart` early-return 으로 no-op 이면서 핸드오프 merge 실패 시 **자가치유**가 되기 때문(플래그를 두면 부분 실패가 그 문서에서 영구 차단된다). ⚠️ `commitCountry()` 를 쓰지 않는 이유도 같다 — 그쪽 프로필 업로드는 fire-and-forget 이라 캐시된 `user.country` 가 스테일인 채 넘어가 `/checkout` 에서 국가가 되돌아간다. ⚠️ 복원에 **`addToCart` 를 쓰지 않는다**(담기 비콘이 재발사돼 `cartAdds` 가 두 배 — 순담기자는 flip-once 라 **전환율만 조용히 망가진다**). ⚠️ **`PUT /v1/cart/merge` 는 브랜드당 5개 상한을 검사하지 않아**(상한은 `cart.upsert` 와 주문 생성에만 있다) 합친 뒤 `brandRemainingCapacity` 로 클램프해야 결제 버튼에서 raw 400 이 안 뜬다. ⚠️ `visitorId` 는 klow.kr 에 **없을 때만 심고, 있으면 덮지 말고** `sessionStorage['klow-handoff-vid']` 에 둬 **주문 생성만** `handoffVisitorId() ?? peekVisitorId()` 를 쓴다(퍼널 원장이 `(brandId, date, visitorId)` 라 다른 vid 로 들어가면 **그 브랜드의 결제 단계가 영구 0**). ⚠️ 결제 후 브랜드 도메인 카트 정리는 `/checkout/redirect` 가 `removeProducts` 와 **같은 블록**에서 `setBrandReturn(…?purchased=)` 를 붙이는 방식이고(그 블록이 "결제 성공 + 이 상품들이 팔렸다"를 아는 유일한 지점), 저장값이 **다른 오리진일 때만** 붙는다 — `isCustomDomain()` 은 자기 `location.host` 를 보는 함수라 여기선 못 쓰고, 값 기준 판정 `isExternalHref()` 를 쓴다. ⚠️ **한 벌만 두기로 한 것 3가지**(두 번째 사본을 만들지 말 것): 서버 카트 머지 절차 `lib/cart-sync.ts`("묘비는 서버가 삭제를 확인했을 때만 비운다"가 `useSessionSync`·`/handoff` 로 갈라지면 한쪽만 고쳐진다) · "남의 오리진인가" `lib/host.ts isExternalHref`(`?purchased=` 부착과 성공 화면 `<Link>` 분기가 같은 값을 `startsWith('https://')` 와 host 비교로 따로 보고 있었다) · 주문이 쓸 방문자 토큰 `visitorIdForOrder`(`handoffVisitorId` 를 export 하지 않아 담기 비콘 등에 끼워 넣는 두 번째 호출부가 **구조적으로** 불가능하다). ⚠️ 본 도메인 판정은 **존 접미사 규칙**(`host === apex || host.endsWith('.'+apex)`)이라 `www.`·`staging.klow.kr` 이 자동으로 덮이고, 서버 `domain-host.ts` 가 `klow.kr`·`*.klow.kr` 을 커스텀 도메인으로 **거부**하는 것과 맞물린다. ⚠️ **`npm run i18n:fill` 은 값이 en 과 같은 로케일 문자열을 "누락"으로 보고 다시 기계번역한다** — 이번에 `labels.category` 큐레이션 값 3개(`id.mist`/`vi.toner`/`vi.serum`)가 `Kabut`·`Huyết thanh`(혈청)로 되돌아가 손으로 복구했다. **fill 을 돌릴 때마다 그 셋을 확인할 것.** 마이그레이션·서버 변경 **없음**(`klow_web` 단독). 자세히는 [`docs/custom-domain/implementation-plan.md`](./docs/custom-domain/implementation-plan.md) §3.
- **컨시어지(concierge) 기능 제거 (2026-08-11):** "아마존에도 없는 K-뷰티를 대신 구해준다"는 초기 요청 폼(사진/제품명/브랜드/메모 → 어드민이 상태만 pending/replied/completed 로 바꾸는 read-mostly 흐름)을 3개 레포에서 전면 삭제했다. 서버 `modules/concierge`(라우트 5개: `POST /v1/concierge-requests` 공개 + `/admin/concierge-requests` GET·GET :id·PATCH·DELETE) + `common/validation/concierge.ts`, 어드민 `컨시어지 요청` 탭(+`lib/api/concierge.ts`·`lib/concierge-status.ts`), klow_web `/concierge`·`ConciergeBanner`(`/shop` 상단 배너)·i18n `concierge` 네임스페이스가 대상이다. **문의 창구는 `/customer-center` 로 단일화** — 로그인 화면의 "비밀번호 재설정"·"로그인이 안 되나요?" 링크가 `/concierge` 를 가리키고 있어 거기로 옮겼다(문구·i18n 키는 그대로). ⚠️ **klow_web `api.upload` 도 같이 제거**했다 — 유일한 호출자가 컨시어지 사진 업로드였고, 공개 웹앱이 `/admin/upload` 를 치는 유일한 경로이기도 했다(어드민·브랜드 업로드는 무관). 예약 슬러그 `concierge` 는 **일부러 남겼다**(klow_web·klow_server 의 `reserved-slugs.ts` 두 파일 동기 유지 — 풀면 브랜드가 `klow.kr/concierge` 를 선점한다). 마이그레이션 `remove_concierge` 는 `ConciergeRequest` **DROP TABLE + `ConciergeStatus` DROP TYPE 이라 롤링 배포 비안전**(단일 레플리카 컷오버) + 데이터 영구 삭제. 다른 모델의 relation 참조는 0건이라 연쇄 수정 없음. 배포 순서는 프론트 2개 → 서버 코드+마이그레이션.
- **EFS 31번 세금식별코드 — 국가별 규칙 테이블화 + 멕시코(MX) RFC (2026-08-18):** 멕시코 세관이 수취인 **RFC**(Registro Federal de Contribuyentes)를 통관 요건으로 요구해 결제·시딩 폼에서 받기 시작했다. ⚠️ **새 기능이 아니라 기존 CN 전용 구현의 일반화**다 — 31번 슬롯은 이미 `Order.recipientTaxId`(`VarChar(20)`) → `payload-builder` 31번으로 연결돼 있었고 빌더는 국가를 안 보고 컬럼을 그대로 흘린다. 그래서 **마이그레이션도 백필도 없다**(RFC 13자 < 20). 막고 있던 건 **`recipientTaxId` 의 `regex(CN_RESIDENT_ID)` 가 국가 무관하게 zod 필드 레벨에 박혀 있던 것** 하나로, 멕시코 RFC 가 무조건 400 이었다(같은 선언이 `order.ts`×2 + `seeding.ts`×2 로 4벌 중복). 이제 정본은 **`common/validation/shared.ts` 의 `TAX_ID_RULES`** 한 곳 — `{pattern, maxLength, required, message, missing}` 이고 CN = 거민신분증 18자리, MX = RFC `^[A-ZÑ&]{3,4}\d{6}[A-Z0-9]{3}$`(법인 12 / 개인 13자, 미등록 개인은 제네릭 `XAXX010101000` / 외국인 `XEXX010101000`). ⚠️ **필드 스키마엔 정규식을 두지 않는다** — 국가마다 형식이 달라 필드 레벨 정규식은 배송국이 바뀌는 순간 오탐이 된다. `recipientAddressFields.recipientTaxId` 가 `.trim().toUpperCase()` 정규화만 하고(RFC 정본이 대문자, CN 은 `[\dXx]` 라 무회귀), 판정은 배송국을 아는 **두 소비자**가 같은 테이블로 한다: `CreateOrderInput.superRefine`(body 에 `countryCode` 가 있는 흐름)과 **`assertEfsCountryFields`**(시딩 claim/checkout·어드민 수화인 수정 — 국가가 body 에 없다). ⚠️ **후자는 존재 여부뿐 아니라 형식까지 봐야 한다** — 안 그러면 그 세 경로만 검증 없이 통과해 EFS 가 발급을 거절한다. `required` 는 "값이 아예 없을 때 막느냐"만 가르고(false 여도 값이 오면 형식은 검사) 현재 CN·MX 둘 다 true. ⚠️ **klow_web `src/lib/tax-id.ts` · klow_admin `src/lib/constants.ts` 는 의도된 크로스 레포 미러**(`common/constants.ts` 선례) — 갈리면 클라가 통과시킨 값을 서버가 400 으로 막는다. klow_web 미러는 `required` 를 갖지 않는다(입력 화면은 규칙이 있으면 언제나 필수로 걷는다). ⚠️ `inputMode` 도 규칙 테이블에 있다 — CN 은 `numeric` 이지만 RFC 는 영숫자라 그대로 두면 모바일에서 문자를 못 친다. ⚠️⚠️ **MX 는 `ShippingCountry.enabled=false` 라 일반 체크아웃이 아직 차단돼 있다**(234개국 중 13개국만 enabled) — 결제 경로는 어드민 '국가 설정' 탭에서 MX 를 켜야 도달하고, **그건 세 배포가 끝난 뒤에 한다**(그러면 배포 창을 아예 안 겪는다). 반면 **시딩은 `enabled` 를 안 보고 요율표로만 판정**해 MX 71티어가 이미 있어 **살아있는 경로**다(실제로 RFC 없이 나간 MX 시딩 주문이 1건 존재). 배포 순서는 **klow_server → klow_web → klow_admin, 연달아** — 서버가 먼저 올라간 창 동안 구 프론트의 멕시코 시딩 claim 이 400 이고, klow_admin 배포 전까지는 기존 MX 주문의 수화인 수정도 400 이다(고객 대면 아님). 회귀 잠금은 `common/__tests__/tax-id.spec.ts`(형식 정오답·대문자 정규화·CN/US/JP 무회귀·`assertEfsCountryFields` 형식 검사) + `shipments/__tests__/payload-builder.spec.ts`(31번 필드 = `fields[30]`, 규칙국 무입력 시 warning 만 남기고 발급은 계속). ⚠️ EFS 스펙 원문의 31번 비고는 "발송인의 등록 번호"지만 **EFS 확인 결과 수취인 값이 맞다**(CN·MX 동일 운용). 자세히는 [`docs/server/modules/orders.md`](./docs/server/modules/orders.md) 국가별 필수 필드.

## Admin UI Convention — Toast Feedback (required)

어드민(`klow_admin`)의 모든 **등록 / 수정 / 삭제 / 오류**는 반드시 토스트로 사용자에게 표시한다. 조용히 리다이렉트하거나 인라인 텍스트로만 표시하지 않는다. (배경: 브랜드 미선택으로 서버가 `400 brand too_small`을 돌려줬는데 인라인 에러만 떠서 원인 파악이 늦어졌던 사례.)

- 토스트 시스템: `klow_admin/src/components/Toast.tsx` — `<ToastProvider>`가 `app/layout.tsx`에 이미 붙어있고, 컴포넌트에서는 `useToast()`로 `success / error / info`를 호출한다.
- CRUD 폼은 `klow_admin/src/hooks/useFormState.ts`가 이미 토스트를 자동으로 띄우므로 폼마다 따로 배선할 필요 없음.
- `useFormState`를 쓰지 않는 플로우(예: `reviews` 목록, `ReviewManager`)는 `useToast()`를 직접 호출해 동일한 규칙을 지킨다.
- 서버 에러 메시지는 `lib/api/client.ts`가 `Error`로 던지므로 `e.message`를 그대로 토스트에 넘기면 충분(추가 파싱 불필요). `API 400: ` 접두까지 벗기려면 같은 파일의 `extractApiError(e, fallback)`.

자세한 규칙은 `docs/architecture.md`의 **Admin UI Conventions** 섹션 참고.

## Local Development

```bash
# Terminal 1 — backend
cd klow_server && npm run start:dev    # http://localhost:4000

# Terminal 2 — admin
cd klow_admin && npm run dev           # http://localhost:3000

# Terminal 3 — public webapp
cd klow_web && npm run dev             # http://localhost:3001

# Terminal 4 — brand onboarding
cd klow_brand && npm run dev           # http://localhost:3002
```

## Prisma Migrations

- **반드시 `npx prisma migrate dev --name <이름>`만 사용한다.** `migrate deploy`, 수동 SQL 파일 생성, `db push` 등은 사용하지 않는다.
- 이 명령은 interactive 프롬프트가 필요하므로, non-interactive 환경에서 실패하면 사용자에게 직접 실행을 요청한다.

## When Working Here

- **Documentation tasks** (architecture, planning, cross-repo design notes) → write in `docs/` from this workspace root.
- **Code changes** → `cd` into the relevant subproject first. Treat each subproject as a fully isolated repo.
- **Verifying changes across repos** → run each project independently; they communicate over HTTP, not shared imports.

For the full backend architecture, see [`docs/architecture.md`](./docs/architecture.md).
